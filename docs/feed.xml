<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:base="https://techai-explained.netlify.app/">
  <title>TechAI Explained</title>
  <subtitle>Complex tech. Simple explanations.</subtitle>
  <link href="https://techai-explained.netlify.app/feed.xml" rel="self"/>
  <link href="https://techai-explained.netlify.app/"/>
  <updated>2026-03-18T00:00:00Z</updated>
  <id>https://techai-explained.netlify.app/</id>
  <author>
    <name>TechAI Explained Team</name>
  </author>
  <entry>
    <title>Building Real-Time Dashboards with Server-Sent Events</title>
    <link href="https://techai-explained.netlify.app/articles/real-time-dashboards-sse/"/>
    <updated>2026-03-04T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/real-time-dashboards-sse/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Real-time dashboards show data the instant it changes — no refresh button, no polling intervals. Server-Sent Events (SSE) is the simplest way to build them. It&#39;s native to every browser, works through every proxy, and requires zero client-side libraries.&lt;/p&gt;
&lt;p&gt;This tutorial builds a live metrics dashboard from scratch.&lt;/p&gt;
&lt;h2&gt;What We&#39;re Building&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌──────────────────────────────────────────────────┐
│              LIVE METRICS DASHBOARD              │
│                                                  │
│  Active Users: 1,247 ▲         Requests/sec: 842│
│  Error Rate:   0.3%  ▼         P95 Latency: 45ms│
│                                                  │
│  ┌──────────────────────────────────────┐        │
│  │ Live Request Log                     │        │
│  │ 10:42:01 GET  /api/orders    12ms ✅ │        │
│  │ 10:42:01 POST /api/users     45ms ✅ │        │
│  │ 10:42:02 GET  /api/search   203ms ⚠️ │        │
│  │ 10:42:02 GET  /api/orders    8ms  ✅ │        │
│  └──────────────────────────────────────┘        │
│                                                  │
│  All data updates in real-time via SSE           │
│  No WebSocket. No polling. No libraries.         │
└──────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h2&gt;The Server: Node.js + Express&lt;/h2&gt;
&lt;h3&gt;Basic SSE Endpoint&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import express from &#39;express&#39;;

const app = express();
app.use(express.static(&#39;public&#39;));

// Track connected clients
const clients: Set&amp;lt;express.Response&amp;gt; = new Set();

app.get(&#39;/events&#39;, (req, res) =&amp;gt; {
  // SSE headers
  res.writeHead(200, {
    &#39;Content-Type&#39;: &#39;text/event-stream&#39;,
    &#39;Cache-Control&#39;: &#39;no-cache&#39;,
    &#39;Connection&#39;: &#39;keep-alive&#39;,
    &#39;X-Accel-Buffering&#39;: &#39;no&#39;,  // Disable NGINX buffering
  });

  // Send initial connection event
  res.write(`event: connected&#92;ndata: ${JSON.stringify({
    time: new Date().toISOString()
  })}&#92;n&#92;n`);

  // Register this client
  clients.add(res);
  console.log(`Client connected. Total: ${clients.size}`);

  // Remove on disconnect
  req.on(&#39;close&#39;, () =&amp;gt; {
    clients.delete(res);
    console.log(`Client disconnected. Total: ${clients.size}`);
  });
});

// Broadcast to all connected clients
function broadcast(event: string, data: object) {
  const message = `event: ${event}&#92;ndata: ${JSON.stringify(data)}&#92;n&#92;n`;
  clients.forEach(client =&amp;gt; client.write(message));
}

app.listen(3000, () =&amp;gt; console.log(&#39;Dashboard server on :3000&#39;));
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Generating Metrics&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Simulate real-time metrics (replace with actual data sources)
setInterval(() =&amp;gt; {
  broadcast(&#39;metrics&#39;, {
    activeUsers: Math.floor(1200 + Math.random() * 100),
    requestsPerSec: Math.floor(800 + Math.random() * 100),
    errorRate: (Math.random() * 0.8).toFixed(2),
    p95Latency: Math.floor(30 + Math.random() * 40),
    timestamp: Date.now(),
  });
}, 1000);

// Simulate request log entries
setInterval(() =&amp;gt; {
  const methods = [&#39;GET&#39;, &#39;POST&#39;, &#39;PUT&#39;, &#39;DELETE&#39;];
  const paths = [&#39;/api/orders&#39;, &#39;/api/users&#39;, &#39;/api/search&#39;, &#39;/api/products&#39;];
  const statusCodes = [200, 200, 200, 200, 200, 201, 400, 500];

  broadcast(&#39;request&#39;, {
    method: methods[Math.floor(Math.random() * methods.length)],
    path: paths[Math.floor(Math.random() * paths.length)],
    status: statusCodes[Math.floor(Math.random() * statusCodes.length)],
    duration: Math.floor(Math.random() * 200),
    timestamp: Date.now(),
  });
}, 200);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;The Client: Vanilla JavaScript&lt;/h2&gt;
&lt;h3&gt;Connecting to SSE&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-html&quot;&gt;&amp;lt;!DOCTYPE html&amp;gt;
&amp;lt;html lang=&amp;quot;en&amp;quot;&amp;gt;
&amp;lt;head&amp;gt;
  &amp;lt;title&amp;gt;Live Dashboard&amp;lt;/title&amp;gt;
  &amp;lt;style&amp;gt;
    body { font-family: monospace; background: #0d1117; color: #e6edf3; padding: 2rem; }
    .metrics { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1rem; margin-bottom: 2rem; }
    .metric { background: #161b22; border: 1px solid #30363d; border-radius: 8px; padding: 1.5rem; }
    .metric .value { font-size: 2rem; font-weight: bold; color: #58a6ff; }
    .metric .label { color: #8b949e; font-size: 0.9rem; }
    .log { background: #161b22; border: 1px solid #30363d; border-radius: 8px; padding: 1rem; max-height: 400px; overflow-y: auto; }
    .log-entry { padding: 0.3rem 0; border-bottom: 1px solid #21262d; font-size: 0.85rem; }
    .status-ok { color: #3fb950; }
    .status-err { color: #f85149; }
    .status-warn { color: #d29922; }
    #connection-status { margin-bottom: 1rem; }
    .connected { color: #3fb950; }
    .disconnected { color: #f85149; }
  &amp;lt;/style&amp;gt;
  &amp;lt;script async src=&amp;quot;https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-8350556723025693&amp;quot; crossorigin=&amp;quot;anonymous&amp;quot;&amp;gt;&amp;lt;/script&amp;gt;
&amp;lt;/head&amp;gt;
&amp;lt;body&amp;gt;
  &amp;lt;h1&amp;gt;⚡ Live Dashboard&amp;lt;/h1&amp;gt;
  &amp;lt;div id=&amp;quot;connection-status&amp;quot; class=&amp;quot;connected&amp;quot;&amp;gt;● Connected&amp;lt;/div&amp;gt;

  &amp;lt;div class=&amp;quot;metrics&amp;quot;&amp;gt;
    &amp;lt;div class=&amp;quot;metric&amp;quot;&amp;gt;&amp;lt;div class=&amp;quot;value&amp;quot; id=&amp;quot;active-users&amp;quot;&amp;gt;—&amp;lt;/div&amp;gt;&amp;lt;div class=&amp;quot;label&amp;quot;&amp;gt;Active Users&amp;lt;/div&amp;gt;&amp;lt;/div&amp;gt;
    &amp;lt;div class=&amp;quot;metric&amp;quot;&amp;gt;&amp;lt;div class=&amp;quot;value&amp;quot; id=&amp;quot;rps&amp;quot;&amp;gt;—&amp;lt;/div&amp;gt;&amp;lt;div class=&amp;quot;label&amp;quot;&amp;gt;Requests/sec&amp;lt;/div&amp;gt;&amp;lt;/div&amp;gt;
    &amp;lt;div class=&amp;quot;metric&amp;quot;&amp;gt;&amp;lt;div class=&amp;quot;value&amp;quot; id=&amp;quot;error-rate&amp;quot;&amp;gt;—&amp;lt;/div&amp;gt;&amp;lt;div class=&amp;quot;label&amp;quot;&amp;gt;Error Rate&amp;lt;/div&amp;gt;&amp;lt;/div&amp;gt;
    &amp;lt;div class=&amp;quot;metric&amp;quot;&amp;gt;&amp;lt;div class=&amp;quot;value&amp;quot; id=&amp;quot;p95&amp;quot;&amp;gt;—&amp;lt;/div&amp;gt;&amp;lt;div class=&amp;quot;label&amp;quot;&amp;gt;P95 Latency&amp;lt;/div&amp;gt;&amp;lt;/div&amp;gt;
  &amp;lt;/div&amp;gt;

  &amp;lt;h2&amp;gt;Live Request Log&amp;lt;/h2&amp;gt;
  &amp;lt;div class=&amp;quot;log&amp;quot; id=&amp;quot;request-log&amp;quot;&amp;gt;&amp;lt;/div&amp;gt;

  &amp;lt;script&amp;gt;
    const statusEl = document.getElementById(&#39;connection-status&#39;);
    const logEl = document.getElementById(&#39;request-log&#39;);

    function connect() {
      const source = new EventSource(&#39;/events&#39;);

      source.addEventListener(&#39;connected&#39;, (e) =&amp;gt; {
        statusEl.textContent = &#39;● Connected&#39;;
        statusEl.className = &#39;connected&#39;;
      });

      source.addEventListener(&#39;metrics&#39;, (e) =&amp;gt; {
        const data = JSON.parse(e.data);
        document.getElementById(&#39;active-users&#39;).textContent = data.activeUsers.toLocaleString();
        document.getElementById(&#39;rps&#39;).textContent = data.requestsPerSec;
        document.getElementById(&#39;error-rate&#39;).textContent = data.errorRate + &#39;%&#39;;
        document.getElementById(&#39;p95&#39;).textContent = data.p95Latency + &#39;ms&#39;;
      });

      source.addEventListener(&#39;request&#39;, (e) =&amp;gt; {
        const data = JSON.parse(e.data);
        const time = new Date(data.timestamp).toLocaleTimeString();
        const statusClass = data.status &amp;lt; 300 ? &#39;status-ok&#39; :
                           data.status &amp;lt; 500 ? &#39;status-warn&#39; : &#39;status-err&#39;;
        const entry = document.createElement(&#39;div&#39;);
        entry.className = &#39;log-entry&#39;;
        entry.innerHTML = `&amp;lt;span&amp;gt;${time}&amp;lt;/span&amp;gt; `
          + `&amp;lt;span&amp;gt;${data.method.padEnd(6)}&amp;lt;/span&amp;gt; `
          + `&amp;lt;span&amp;gt;${data.path.padEnd(20)}&amp;lt;/span&amp;gt; `
          + `&amp;lt;span&amp;gt;${String(data.duration).padStart(4)}ms&amp;lt;/span&amp;gt; `
          + `&amp;lt;span class=&amp;quot;${statusClass}&amp;quot;&amp;gt;${data.status}&amp;lt;/span&amp;gt;`;
        logEl.prepend(entry);

        // Keep only last 100 entries
        while (logEl.children.length &amp;gt; 100) logEl.removeChild(logEl.lastChild);
      });

      source.onerror = () =&amp;gt; {
        statusEl.textContent = &#39;● Reconnecting...&#39;;
        statusEl.className = &#39;disconnected&#39;;
        // EventSource auto-reconnects — no manual code needed!
      };
    }

    connect();
  &amp;lt;/script&amp;gt;
&amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Key point:&lt;/strong&gt; &lt;code&gt;EventSource&lt;/code&gt; auto-reconnects when the connection drops. No retry logic, no exponential backoff — the browser handles it natively.&lt;/p&gt;
&lt;h2&gt;Production Patterns&lt;/h2&gt;
&lt;h3&gt;Pattern 1: Event ID for Resume&lt;/h3&gt;
&lt;p&gt;If the connection drops, the client can resume from where it left off using &lt;code&gt;Last-Event-ID&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Server: include event IDs
let eventCounter = 0;

function broadcast(event: string, data: object) {
  eventCounter++;
  const message = `id: ${eventCounter}&#92;nevent: ${event}&#92;ndata: ${JSON.stringify(data)}&#92;n&#92;n`;
  clients.forEach(client =&amp;gt; client.write(message));
}

// On reconnect, the browser sends Last-Event-ID header
app.get(&#39;/events&#39;, (req, res) =&amp;gt; {
  const lastId = parseInt(req.headers[&#39;last-event-id&#39;] || &#39;0&#39;);

  // Send any missed events since lastId
  missedEvents
    .filter(e =&amp;gt; e.id &amp;gt; lastId)
    .forEach(e =&amp;gt; res.write(`id: ${e.id}&#92;nevent: ${e.event}&#92;ndata: ${e.data}&#92;n&#92;n`));
  // Then continue with live events...
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Pattern 2: Heartbeat Keep-Alive&lt;/h3&gt;
&lt;p&gt;Proxies and load balancers may close idle connections. Send a heartbeat:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Server: send a comment (ignored by EventSource) every 15s
setInterval(() =&amp;gt; {
  clients.forEach(client =&amp;gt; client.write(&#39;: heartbeat&#92;n&#92;n&#39;));
}, 15000);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Pattern 3: Channel-Based Subscriptions&lt;/h3&gt;
&lt;p&gt;Let clients subscribe to specific event types:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;app.get(&#39;/events&#39;, (req, res) =&amp;gt; {
  const channels = (req.query.channels as string || &#39;metrics,requests&#39;).split(&#39;,&#39;);

  // Store client with their subscriptions
  const client = { res, channels: new Set(channels) };
  clients.add(client);

  req.on(&#39;close&#39;, () =&amp;gt; clients.delete(client));
});

function broadcast(event: string, data: object) {
  clients.forEach(client =&amp;gt; {
    if (client.channels.has(event)) {
      client.res.write(`event: ${event}&#92;ndata: ${JSON.stringify(data)}&#92;n&#92;n`);
    }
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Client subscribes to specific channels:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-javascript&quot;&gt;const source = new EventSource(&#39;/events?channels=metrics,alerts&#39;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Pattern 4: Scaling with Redis Pub/Sub&lt;/h3&gt;
&lt;p&gt;For multiple server instances behind a load balancer, use Redis to broadcast events across all instances:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import Redis from &#39;ioredis&#39;;

const redisSub = new Redis();
const redisPub = new Redis();

// Publish events to Redis (from any server instance)
function publishEvent(event: string, data: object) {
  redisPub.publish(&#39;dashboard-events&#39;, JSON.stringify({ event, data }));
}

// Each server instance subscribes and forwards to its SSE clients
redisSub.subscribe(&#39;dashboard-events&#39;);
redisSub.on(&#39;message&#39;, (channel, message) =&amp;gt; {
  const { event, data } = JSON.parse(message);
  clients.forEach(client =&amp;gt; {
    client.write(`event: ${event}&#92;ndata: ${JSON.stringify(data)}&#92;n&#92;n`);
  });
});
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
SCALING SSE WITH REDIS
&lt;p&gt;Client A ──► Server 1 ◄──┐
Client B ──► Server 1     │
├── Redis Pub/Sub
Client C ──► Server 2 ◄──┘
Client D ──► Server 2&lt;/p&gt;
&lt;p&gt;Any server publishes to Redis.
All servers receive and forward to their connected clients.
Every client sees every event.&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;SSE vs Polling vs WebSocket for Dashboards&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Approach&lt;/th&gt;
&lt;th&gt;Latency&lt;/th&gt;
&lt;th&gt;Complexity&lt;/th&gt;
&lt;th&gt;Server Load&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Polling (1s)&lt;/td&gt;
&lt;td&gt;Up to 1s delay&lt;/td&gt;
&lt;td&gt;Lowest&lt;/td&gt;
&lt;td&gt;Highest (constant requests)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Long Polling&lt;/td&gt;
&lt;td&gt;Near-instant&lt;/td&gt;
&lt;td&gt;Medium&lt;/td&gt;
&lt;td&gt;Medium&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;SSE&lt;/td&gt;
&lt;td&gt;Instant&lt;/td&gt;
&lt;td&gt;Low&lt;/td&gt;
&lt;td&gt;Low (one connection)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;WebSocket&lt;/td&gt;
&lt;td&gt;Instant&lt;/td&gt;
&lt;td&gt;Highest&lt;/td&gt;
&lt;td&gt;Low&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;For dashboards that only need server-to-client data flow, SSE is the optimal choice. It&#39;s simpler than WebSocket, more efficient than polling, and works natively in every browser.&lt;/p&gt;
&lt;h2&gt;When to Choose Something Else&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Bidirectional communication&lt;/strong&gt; (chat, collaboration) → WebSocket&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Binary data streaming&lt;/strong&gt; (video, audio) → WebSocket&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Millions of concurrent connections&lt;/strong&gt; → WebSocket with custom protocol&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Server-to-server streaming&lt;/strong&gt; → gRPC&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For everything else — dashboards, notifications, live feeds, LLM streaming — SSE is the right default.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Prompt Engineering for Backend Developers</title>
    <link href="https://techai-explained.netlify.app/articles/prompt-engineering-backend-developers/"/>
    <updated>2026-03-05T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/prompt-engineering-backend-developers/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Prompt engineering for backend developers is different from the creative prompting you see on social media. You&#39;re not trying to write poems — you&#39;re trying to get structured, reliable, deterministic outputs from an API that&#39;s fundamentally probabilistic. This guide covers the patterns that work in production code.&lt;/p&gt;
&lt;h2&gt;The Three Layers of a Prompt&lt;/h2&gt;
&lt;p&gt;Every production prompt has three layers: the system prompt, the context, and the user instruction.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────┐
│  SYSTEM PROMPT (sets behavior and constraints)  │
│  &quot;You are a JSON API that classifies support    │
│   tickets. Always respond with valid JSON.&quot;     │
├─────────────────────────────────────────────────┤
│  CONTEXT (domain data, examples, retrieved docs)│
│  &quot;Categories: billing, technical, account,      │
│   feature-request. Priority: low, medium, high&quot; │
│  &quot;Example: &#39;Can&#39;t login&#39; → technical, high&quot;     │
├─────────────────────────────────────────────────┤
│  USER INSTRUCTION (the actual task)             │
│  &quot;Classify this ticket: &#39;My invoice shows       │
│   the wrong amount for March&#39;&quot;                  │
└─────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;response = client.chat.completions.create(
    model=&amp;quot;gpt-4o&amp;quot;,
    messages=[
        {&amp;quot;role&amp;quot;: &amp;quot;system&amp;quot;, &amp;quot;content&amp;quot;: system_prompt},
        {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: f&amp;quot;{context}&#92;n&#92;n{instruction}&amp;quot;},
    ],
    temperature=0,          # Deterministic output
    response_format={&amp;quot;type&amp;quot;: &amp;quot;json_object&amp;quot;},
)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Pattern 1: System Prompt as a Contract&lt;/h2&gt;
&lt;p&gt;The system prompt is your API contract. It defines what the model does and — critically — what it doesn&#39;t do.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;SYSTEM_PROMPT = &amp;quot;&amp;quot;&amp;quot;You are a ticket classification API.

ROLE: Classify customer support tickets into categories and priorities.

OUTPUT FORMAT: Always respond with valid JSON matching this schema:
{
  &amp;quot;category&amp;quot;: &amp;quot;billing&amp;quot; | &amp;quot;technical&amp;quot; | &amp;quot;account&amp;quot; | &amp;quot;feature-request&amp;quot;,
  &amp;quot;priority&amp;quot;: &amp;quot;low&amp;quot; | &amp;quot;medium&amp;quot; | &amp;quot;high&amp;quot;,
  &amp;quot;confidence&amp;quot;: 0.0 to 1.0,
  &amp;quot;reasoning&amp;quot;: &amp;quot;one sentence explaining the classification&amp;quot;
}

RULES:
- If the ticket mentions money, billing, charges, or invoices → category: billing
- If the ticket mentions login, errors, crashes, or bugs → category: technical
- If the ticket is ambiguous, choose the most likely category and set confidence &amp;lt; 0.7
- Never invent categories outside the four listed above
- Never include information not present in the ticket
&amp;quot;&amp;quot;&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Key principles:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Be explicit about output format&lt;/strong&gt; — JSON schema, field names, valid values&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Define edge cases&lt;/strong&gt; — what happens with ambiguous input?&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Set boundaries&lt;/strong&gt; — &amp;quot;never invent categories,&amp;quot; &amp;quot;never include information not present&amp;quot;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Keep it focused&lt;/strong&gt; — one task per system prompt&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Pattern 2: Few-Shot Examples&lt;/h2&gt;
&lt;p&gt;Show the model what correct output looks like. This is the single most effective technique for improving reliability.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;FEW_SHOT_EXAMPLES = &amp;quot;&amp;quot;&amp;quot;
EXAMPLE 1:
Ticket: &amp;quot;I was charged twice for my subscription this month&amp;quot;
Output: {&amp;quot;category&amp;quot;: &amp;quot;billing&amp;quot;, &amp;quot;priority&amp;quot;: &amp;quot;high&amp;quot;, &amp;quot;confidence&amp;quot;: 0.95, &amp;quot;reasoning&amp;quot;: &amp;quot;Double charge is a billing issue requiring urgent resolution&amp;quot;}

EXAMPLE 2:
Ticket: &amp;quot;The dashboard loads slowly when I have more than 100 items&amp;quot;
Output: {&amp;quot;category&amp;quot;: &amp;quot;technical&amp;quot;, &amp;quot;priority&amp;quot;: &amp;quot;medium&amp;quot;, &amp;quot;confidence&amp;quot;: 0.9, &amp;quot;reasoning&amp;quot;: &amp;quot;Performance issue in the dashboard with large datasets&amp;quot;}

EXAMPLE 3:
Ticket: &amp;quot;Can you add dark mode to the mobile app?&amp;quot;
Output: {&amp;quot;category&amp;quot;: &amp;quot;feature-request&amp;quot;, &amp;quot;priority&amp;quot;: &amp;quot;low&amp;quot;, &amp;quot;confidence&amp;quot;: 0.95, &amp;quot;reasoning&amp;quot;: &amp;quot;User requesting a new UI feature for the mobile application&amp;quot;}

EXAMPLE 4:
Ticket: &amp;quot;My account settings page doesn&#39;t show my updated email&amp;quot;
Output: {&amp;quot;category&amp;quot;: &amp;quot;account&amp;quot;, &amp;quot;priority&amp;quot;: &amp;quot;medium&amp;quot;, &amp;quot;confidence&amp;quot;: 0.8, &amp;quot;reasoning&amp;quot;: &amp;quot;Account data not reflecting recent changes, could also be technical&amp;quot;}
&amp;quot;&amp;quot;&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Guidelines for examples:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;3-5 examples&lt;/strong&gt; is the sweet spot — fewer is insufficient, more wastes tokens&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cover edge cases&lt;/strong&gt; — include an ambiguous example with lower confidence&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Show the exact output format&lt;/strong&gt; — the model mirrors the pattern precisely&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Vary the inputs&lt;/strong&gt; — different lengths, different vocabularies, different categories&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Pattern 3: Structured Output Enforcement&lt;/h2&gt;
&lt;p&gt;Don&#39;t hope the model returns valid JSON. &lt;strong&gt;Force it.&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from pydantic import BaseModel, Field
from enum import Enum

class Category(str, Enum):
    BILLING = &amp;quot;billing&amp;quot;
    TECHNICAL = &amp;quot;technical&amp;quot;
    ACCOUNT = &amp;quot;account&amp;quot;
    FEATURE_REQUEST = &amp;quot;feature-request&amp;quot;

class Priority(str, Enum):
    LOW = &amp;quot;low&amp;quot;
    MEDIUM = &amp;quot;medium&amp;quot;
    HIGH = &amp;quot;high&amp;quot;

class TicketClassification(BaseModel):
    category: Category
    priority: Priority
    confidence: float = Field(ge=0.0, le=1.0)
    reasoning: str = Field(max_length=200)

# OpenAI structured outputs
response = client.beta.chat.completions.parse(
    model=&amp;quot;gpt-4o&amp;quot;,
    messages=[
        {&amp;quot;role&amp;quot;: &amp;quot;system&amp;quot;, &amp;quot;content&amp;quot;: SYSTEM_PROMPT},
        {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: ticket_text},
    ],
    response_format=TicketClassification,
)

result = response.choices[0].message.parsed
# result.category is guaranteed to be a valid Category enum
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;With structured outputs, invalid JSON is &lt;strong&gt;impossible&lt;/strong&gt;. The model is constrained to only produce valid tokens matching your schema.&lt;/p&gt;
&lt;h2&gt;Pattern 4: Chain of Thought for Complex Reasoning&lt;/h2&gt;
&lt;p&gt;When the task requires reasoning, ask the model to think step-by-step before giving the final answer.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;SYSTEM_PROMPT = &amp;quot;&amp;quot;&amp;quot;You are a code review assistant that identifies security vulnerabilities.

For each code snippet, follow this process:
1. IDENTIFY what the code does (2-3 sentences)
2. LIST potential security concerns (bullet points)
3. CLASSIFY each concern as: critical, warning, or info
4. PROVIDE a fixed version of the code if critical issues exist

Format your response as JSON:
{
  &amp;quot;summary&amp;quot;: &amp;quot;what the code does&amp;quot;,
  &amp;quot;concerns&amp;quot;: [
    {&amp;quot;issue&amp;quot;: &amp;quot;description&amp;quot;, &amp;quot;severity&amp;quot;: &amp;quot;critical|warning|info&amp;quot;, &amp;quot;line&amp;quot;: 0}
  ],
  &amp;quot;fixed_code&amp;quot;: &amp;quot;corrected code or null if no critical issues&amp;quot;,
  &amp;quot;overall_risk&amp;quot;: &amp;quot;low|medium|high&amp;quot;
}
&amp;quot;&amp;quot;&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The explicit step-by-step process produces more accurate analysis than &amp;quot;find security vulnerabilities.&amp;quot;&lt;/p&gt;
&lt;h2&gt;Pattern 5: Prompt Templates with Variables&lt;/h2&gt;
&lt;p&gt;Build prompts programmatically. Never concatenate strings manually.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;from string import Template

REVIEW_TEMPLATE = Template(&amp;quot;&amp;quot;&amp;quot;
Review the following $language code for $review_type issues.

File: $filename
Context: This file is part of a $project_type that handles $domain.

&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;$code&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;
Focus on:
$focus_areas

Respond with JSON matching the review schema.
&amp;quot;&amp;quot;&amp;quot;)

prompt = REVIEW_TEMPLATE.substitute(
    language=&amp;quot;Python&amp;quot;,
    review_type=&amp;quot;security&amp;quot;,
    filename=&amp;quot;auth/login.py&amp;quot;,
    project_type=&amp;quot;web API&amp;quot;,
    domain=&amp;quot;user authentication&amp;quot;,
    code=file_content,
    focus_areas=&amp;quot;- SQL injection&#92;n- Authentication bypass&#92;n- Credential handling&amp;quot;,
)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Pattern 6: Retry with Reformulation&lt;/h2&gt;
&lt;p&gt;When the model fails, don&#39;t just retry — reformulate.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;async def classify_with_retry(ticket: str, max_retries: int = 3):
    for attempt in range(max_retries):
        try:
            result = await classify_ticket(ticket)
            parsed = TicketClassification.model_validate_json(result)
            return parsed
        except ValidationError as e:
            if attempt == max_retries - 1:
                raise

            # Reformulate with the error
            result = await client.chat.completions.create(
                messages=[
                    {&amp;quot;role&amp;quot;: &amp;quot;system&amp;quot;, &amp;quot;content&amp;quot;: SYSTEM_PROMPT},
                    {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: ticket},
                    {&amp;quot;role&amp;quot;: &amp;quot;assistant&amp;quot;, &amp;quot;content&amp;quot;: result},
                    {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;:
                        f&amp;quot;Your response had validation errors: {e}. &amp;quot;
                        f&amp;quot;Please fix and respond with valid JSON.&amp;quot;},
                ],
            )
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Temperature and Sampling&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Temperature&lt;/th&gt;
&lt;th&gt;Behavior&lt;/th&gt;
&lt;th&gt;Use Case&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;0&lt;/td&gt;
&lt;td&gt;Deterministic (same input → same output)&lt;/td&gt;
&lt;td&gt;Classification, extraction, structured data&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;0.3-0.5&lt;/td&gt;
&lt;td&gt;Slight variation, mostly consistent&lt;/td&gt;
&lt;td&gt;Summarization, code review&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;0.7-1.0&lt;/td&gt;
&lt;td&gt;Creative, varied responses&lt;/td&gt;
&lt;td&gt;Content generation, brainstorming&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;For backend APIs, use temperature 0.&lt;/strong&gt; You want the same ticket to get the same classification every time.&lt;/p&gt;
&lt;h2&gt;Common Mistakes&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Vague system prompts&lt;/strong&gt; — &amp;quot;You are a helpful assistant&amp;quot; tells the model nothing useful&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No output format specification&lt;/strong&gt; — hoping for JSON without specifying the schema&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Temperature too high&lt;/strong&gt; — creative randomness is the enemy of reliable APIs&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No input validation&lt;/strong&gt; — sending empty strings or 100K-token documents&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Not measuring quality&lt;/strong&gt; — prompt engineering without evaluation is guessing&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Prompt in production without tests&lt;/strong&gt; — create a test suite of 50+ input/expected-output pairs&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Evaluation: The Missing Step&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;test_cases = [
    {
        &amp;quot;input&amp;quot;: &amp;quot;I was charged $99 instead of $49&amp;quot;,
        &amp;quot;expected_category&amp;quot;: &amp;quot;billing&amp;quot;,
        &amp;quot;expected_priority&amp;quot;: &amp;quot;high&amp;quot;,
    },
    {
        &amp;quot;input&amp;quot;: &amp;quot;App crashes when uploading files over 10MB&amp;quot;,
        &amp;quot;expected_category&amp;quot;: &amp;quot;technical&amp;quot;,
        &amp;quot;expected_priority&amp;quot;: &amp;quot;high&amp;quot;,
    },
]

results = []
for case in test_cases:
    output = classify_ticket(case[&amp;quot;input&amp;quot;])
    results.append({
        &amp;quot;correct_category&amp;quot;: output.category == case[&amp;quot;expected_category&amp;quot;],
        &amp;quot;correct_priority&amp;quot;: output.priority == case[&amp;quot;expected_priority&amp;quot;],
    })

accuracy = sum(r[&amp;quot;correct_category&amp;quot;] for r in results) / len(results)
print(f&amp;quot;Category accuracy: {accuracy:.1%}&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Run this eval suite every time you change a prompt. Prompts are code — they need tests.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Terraform Modules and State Management at Scale</title>
    <link href="https://techai-explained.netlify.app/articles/terraform-modules-state-management/"/>
    <updated>2026-03-06T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/terraform-modules-state-management/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Terraform is simple when you have one environment and one person running &lt;code&gt;terraform apply&lt;/code&gt;. It gets complicated fast when you have 5 environments, 20 engineers, and 200 resources. This guide covers the patterns that keep Terraform manageable at scale: module design, state management, and multi-environment strategies.&lt;/p&gt;
&lt;h2&gt;Module Design Principles&lt;/h2&gt;
&lt;p&gt;A Terraform module is a reusable package of infrastructure. Good modules are the difference between copy-pasting HCL across environments and having a single source of truth.&lt;/p&gt;
&lt;h3&gt;The Three-Tier Module Structure&lt;/h3&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
TIER 1: ROOT MODULES (Environments)
  live/
  ├── dev/        ── calls Tier 2 modules with dev values
  ├── staging/    ── calls Tier 2 modules with staging values
  └── production/ ── calls Tier 2 modules with prod values
&lt;p&gt;TIER 2: COMPOSITION MODULES (Stacks)
modules/stacks/
├── api-stack/       ── composes Tier 3 modules into a full API
├── data-stack/      ── composes Tier 3 modules into a data platform
└── networking/      ── composes Tier 3 modules into VPC + DNS&lt;/p&gt;
&lt;p&gt;TIER 3: RESOURCE MODULES (Building blocks)
modules/resources/
├── aks-cluster/     ── single AKS cluster with best practices
├── postgresql/      ── single PostgreSQL instance
├── redis/           ── single Redis cache
└── storage-account/ ── single storage account with policies&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Writing a Good Resource Module&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-hcl&quot;&gt;# modules/resources/postgresql/main.tf

variable &amp;quot;name&amp;quot; {
  description = &amp;quot;Database instance name&amp;quot;
  type        = string
  validation {
    condition     = can(regex(&amp;quot;^[a-z][a-z0-9-]{2,58}[a-z0-9]$&amp;quot;, var.name))
    error_message = &amp;quot;Name must be 4-60 chars, lowercase alphanumeric with hyphens.&amp;quot;
  }
}

variable &amp;quot;sku&amp;quot; {
  description = &amp;quot;Compute tier and size&amp;quot;
  type        = string
  default     = &amp;quot;GP_Standard_D2s_v3&amp;quot;
}

variable &amp;quot;storage_mb&amp;quot; {
  description = &amp;quot;Storage size in MB&amp;quot;
  type        = number
  default     = 32768
}

variable &amp;quot;high_availability&amp;quot; {
  description = &amp;quot;Enable zone-redundant HA&amp;quot;
  type        = bool
  default     = false
}

variable &amp;quot;tags&amp;quot; {
  description = &amp;quot;Resource tags&amp;quot;
  type        = map(string)
  default     = {}
}

resource &amp;quot;azurerm_postgresql_flexible_server&amp;quot; &amp;quot;this&amp;quot; {
  name                = var.name
  resource_group_name = var.resource_group_name
  location            = var.location
  version             = &amp;quot;16&amp;quot;
  sku_name            = var.sku
  storage_mb          = var.storage_mb

  high_availability {
    mode = var.high_availability ? &amp;quot;ZoneRedundant&amp;quot; : &amp;quot;Disabled&amp;quot;
  }

  tags = merge(var.tags, {
    managed_by = &amp;quot;terraform&amp;quot;
    module     = &amp;quot;postgresql&amp;quot;
  })
}

output &amp;quot;id&amp;quot; {
  description = &amp;quot;PostgreSQL server ID&amp;quot;
  value       = azurerm_postgresql_flexible_server.this.id
}

output &amp;quot;fqdn&amp;quot; {
  description = &amp;quot;Fully qualified domain name&amp;quot;
  value       = azurerm_postgresql_flexible_server.this.fqdn
}

output &amp;quot;connection_string&amp;quot; {
  description = &amp;quot;Connection string (without password)&amp;quot;
  value       = &amp;quot;postgresql://${azurerm_postgresql_flexible_server.this.fqdn}:5432&amp;quot;
  sensitive   = true
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Module Design Rules&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;One responsibility&lt;/strong&gt; — a module creates one logical resource (even if it&#39;s multiple Terraform resources)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Sensible defaults&lt;/strong&gt; — modules should work with minimal inputs&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Validate inputs&lt;/strong&gt; — use &lt;code&gt;validation&lt;/code&gt; blocks to catch mistakes early&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Output everything useful&lt;/strong&gt; — IDs, FQDNs, connection strings&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tag everything&lt;/strong&gt; — enforce tagging at the module level&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;State Management&lt;/h2&gt;
&lt;p&gt;Terraform state is the mapping between your HCL code and real cloud resources. Corrupt or lose the state, and Terraform can&#39;t manage your infrastructure.&lt;/p&gt;
&lt;h3&gt;Remote State with Locking&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Never&lt;/strong&gt; use local state in a team. Always use remote state with locking:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-hcl&quot;&gt;# backend.tf
terraform {
  backend &amp;quot;azurerm&amp;quot; {
    resource_group_name  = &amp;quot;rg-terraform-state&amp;quot;
    storage_account_name = &amp;quot;stterraformstate&amp;quot;
    container_name       = &amp;quot;tfstate&amp;quot;
    key                  = &amp;quot;production/api-stack.tfstate&amp;quot;
    use_oidc             = true  # No stored credentials!
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-hcl&quot;&gt;# AWS equivalent
terraform {
  backend &amp;quot;s3&amp;quot; {
    bucket         = &amp;quot;my-terraform-state&amp;quot;
    key            = &amp;quot;production/api-stack.tfstate&amp;quot;
    region         = &amp;quot;us-east-1&amp;quot;
    dynamodb_table = &amp;quot;terraform-locks&amp;quot;  # Locking!
    encrypt        = true
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;State File Per Stack&lt;/h3&gt;
&lt;p&gt;One state file per environment per stack. Never put all resources in one state file:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
GOOD: Separate state files
──────────────────────────
&lt;p&gt;production/networking.tfstate    (VPC, DNS, certificates)
production/data.tfstate          (databases, caches)
production/api.tfstate           (AKS, app services)&lt;/p&gt;
&lt;p&gt;staging/networking.tfstate
staging/data.tfstate
staging/api.tfstate&lt;/p&gt;
&lt;p&gt;Benefits:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Blast radius is limited (bad apply only affects one stack)&lt;/li&gt;
&lt;li&gt;Parallel applies across stacks&lt;/li&gt;
&lt;li&gt;Different teams can own different stacks&lt;/li&gt;
&lt;li&gt;Faster plan/apply (fewer resources to check)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;BAD: One giant state file
─────────────────────────&lt;/p&gt;
&lt;p&gt;production/everything.tfstate    (500 resources)&lt;/p&gt;
&lt;p&gt;Problems:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;One bad apply can destroy everything&lt;/li&gt;
&lt;li&gt;Slow plan (checks all 500 resources)&lt;/li&gt;
&lt;li&gt;Everyone blocks each other with the state lock&lt;/li&gt;
&lt;/ul&gt;
&lt;/div&gt;
&lt;h3&gt;State Operations You Need to Know&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# List everything in state
terraform state list

# Show a specific resource&#39;s state
terraform state show azurerm_postgresql_flexible_server.this

# Move a resource (refactoring without destroy/recreate)
terraform state mv &#92;
  module.old_name.azurerm_postgresql_flexible_server.this &#92;
  module.new_name.azurerm_postgresql_flexible_server.this

# Import existing resource into state
terraform import &#92;
  azurerm_postgresql_flexible_server.this &#92;
  /subscriptions/.../flexibleServers/my-postgres

# Remove from state (Terraform forgets, resource still exists)
terraform state rm azurerm_postgresql_flexible_server.this
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Multi-Environment Strategy&lt;/h2&gt;
&lt;h3&gt;Directory-Based Environments&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;infrastructure/
├── modules/
│   └── stacks/
│       └── api-stack/
│           ├── main.tf
│           ├── variables.tf
│           └── outputs.tf
├── live/
│   ├── dev/
│   │   ├── main.tf        # calls module with dev values
│   │   ├── terraform.tfvars
│   │   └── backend.tf
│   ├── staging/
│   │   ├── main.tf
│   │   ├── terraform.tfvars
│   │   └── backend.tf
│   └── production/
│       ├── main.tf
│       ├── terraform.tfvars
│       └── backend.tf
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Each environment calls the same module with different variables:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-hcl&quot;&gt;# live/production/main.tf
module &amp;quot;api_stack&amp;quot; {
  source = &amp;quot;../../modules/stacks/api-stack&amp;quot;

  environment        = &amp;quot;production&amp;quot;
  aks_node_count     = 5
  aks_node_size      = &amp;quot;Standard_D4s_v3&amp;quot;
  database_sku       = &amp;quot;GP_Standard_D4s_v3&amp;quot;
  high_availability  = true
  redis_sku          = &amp;quot;Premium&amp;quot;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-hcl&quot;&gt;# live/dev/main.tf
module &amp;quot;api_stack&amp;quot; {
  source = &amp;quot;../../modules/stacks/api-stack&amp;quot;

  environment        = &amp;quot;dev&amp;quot;
  aks_node_count     = 2
  aks_node_size      = &amp;quot;Standard_D2s_v3&amp;quot;
  database_sku       = &amp;quot;B_Standard_B1ms&amp;quot;
  high_availability  = false
  redis_sku          = &amp;quot;Basic&amp;quot;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;CI/CD for Terraform&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# .github/workflows/terraform.yml
name: Terraform

on:
  pull_request:
    paths: [&#39;infrastructure/**&#39;]
  push:
    branches: [main]
    paths: [&#39;infrastructure/**&#39;]

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  plan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [dev, staging, production]
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3

      - uses: azure/login@v2
        with:
          client-id: $
          tenant-id: $
          subscription-id: $

      - name: Terraform Plan
        working-directory: infrastructure/live/$
        run: |
          terraform init
          terraform plan -out=plan.tfplan

      - name: Comment PR with Plan
        if: github.event_name == &#39;pull_request&#39;
        uses: borchero/terraform-plan-comment@v2
        with:
          working-directory: infrastructure/live/$

  apply:
    if: github.ref == &#39;refs/heads/main&#39;
    needs: plan
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [dev, staging, production]
      max-parallel: 1  # Apply environments sequentially
    environment: $  # Requires approval for prod
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - uses: azure/login@v2
        with:
          client-id: $
          tenant-id: $
          subscription-id: $

      - name: Terraform Apply
        working-directory: infrastructure/live/$
        run: |
          terraform init
          terraform apply -auto-approve
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Common Mistakes&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;No state locking&lt;/strong&gt; — two engineers run &lt;code&gt;apply&lt;/code&gt; simultaneously, state gets corrupted&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Monolith state file&lt;/strong&gt; — 500 resources in one file; one mistake destroys everything&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Hardcoded values in modules&lt;/strong&gt; — modules should accept variables, not contain environment-specific values&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No &lt;code&gt;terraform plan&lt;/code&gt; in CI&lt;/strong&gt; — applying without reviewing the plan is infrastructure roulette&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Ignoring drift&lt;/strong&gt; — schedule &lt;code&gt;terraform plan&lt;/code&gt; runs to detect manual changes&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Get modules, state, and CI/CD right, and Terraform scales to thousands of resources across dozens of environments. Get them wrong, and you&#39;ll spend more time fixing Terraform than building infrastructure.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Building Internal Developer Platforms with Backstage</title>
    <link href="https://techai-explained.netlify.app/articles/backstage-internal-developer-platforms/"/>
    <updated>2026-03-07T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/backstage-internal-developer-platforms/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Platform engineering is the practice of building a self-service layer on top of your infrastructure so developers can ship without filing tickets. Backstage, created by Spotify and donated to the CNCF, is the leading open-source framework for building these Internal Developer Portals (IDPs).&lt;/p&gt;
&lt;p&gt;This guide walks you through setting up Backstage from scratch and configuring the four capabilities that matter most.&lt;/p&gt;
&lt;h2&gt;What Backstage Actually Does&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────────────┐
│                  BACKSTAGE PORTAL                       │
│                                                         │
│  ┌─────────────┐ ┌──────────────┐ ┌─────────────────┐  │
│  │  SOFTWARE    │ │ SCAFFOLDER   │ │   TECHDOCS      │  │
│  │  CATALOG     │ │ (Templates)  │ │                 │  │
│  │             │ │              │ │  Docs-as-code   │  │
│  │  Every svc, │ │  Create new  │ │  rendered from  │  │
│  │  its owner, │ │  services    │ │  markdown in    │  │
│  │  its APIs,  │ │  from golden │ │  each repo      │  │
│  │  its deps   │ │  paths       │ │                 │  │
│  └─────────────┘ └──────────────┘ └─────────────────┘  │
│                                                         │
│  ┌─────────────┐ ┌──────────────┐ ┌─────────────────┐  │
│  │  KUBERNETES  │ │  CI/CD       │ │  SEARCH         │  │
│  │  PLUGIN      │ │  PLUGIN      │ │                 │  │
│  │             │ │              │ │  Find anything  │  │
│  │  Pod status, │ │  Pipeline   │ │  across all     │  │
│  │  logs,       │ │  status,    │ │  services,      │  │
│  │  deployments │ │  history    │ │  docs, APIs     │  │
│  └─────────────┘ └──────────────┘ └─────────────────┘  │
└─────────────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h2&gt;Step 1: Create a Backstage App&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx @backstage/create-app@latest
# Enter app name: my-developer-portal
cd my-developer-portal
yarn dev
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This starts Backstage at &lt;code&gt;http://localhost:3000&lt;/code&gt; with a default catalog.&lt;/p&gt;
&lt;h2&gt;Step 2: The Software Catalog&lt;/h2&gt;
&lt;p&gt;The catalog is the foundation — a registry of every service, library, API, and resource in your organization.&lt;/p&gt;
&lt;h3&gt;Registering a Service&lt;/h3&gt;
&lt;p&gt;Each service has a &lt;code&gt;catalog-info.yaml&lt;/code&gt; in its repo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# catalog-info.yaml (in the service&#39;s repository)
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: order-api
  description: REST API for order management
  tags:
    - typescript
    - rest-api
    - production
  annotations:
    github.com/project-slug: my-org/order-api
    backstage.io/techdocs-ref: dir:.
  links:
    - url: https://grafana.internal/d/order-api
      title: Grafana Dashboard
      icon: dashboard
spec:
  type: service
  lifecycle: production
  owner: team-commerce
  system: commerce-platform
  providesApis:
    - order-api
  consumesApis:
    - payment-api
    - inventory-api
  dependsOn:
    - resource:orders-database
    - resource:redis-cache
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Auto-Discovery&lt;/h3&gt;
&lt;p&gt;Don&#39;t register services manually. Use auto-discovery to scan your GitHub org:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# app-config.yaml
catalog:
  providers:
    github:
      myOrg:
        organization: &#39;my-org&#39;
        catalogPath: &#39;/catalog-info.yaml&#39;
        filters:
          branch: &#39;main&#39;
          repository: &#39;.*&#39;
        schedule:
          frequency: { minutes: 30 }
          timeout: { minutes: 3 }
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Backstage scans every repo in your org every 30 minutes, discovering and registering any service with a &lt;code&gt;catalog-info.yaml&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;System and Domain Modeling&lt;/h3&gt;
&lt;p&gt;Group services into systems and domains for a birds-eye view:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# system: commerce-platform
apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: commerce-platform
  description: All services powering the e-commerce experience
spec:
  owner: team-commerce
  domain: shopping

# domain: shopping
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: shopping
  description: The end-to-end shopping experience
spec:
  owner: group:engineering-leadership
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 3: Software Templates (Golden Paths)&lt;/h2&gt;
&lt;p&gt;Templates let developers create new services in minutes with all the right defaults — CI/CD, monitoring, security, and documentation pre-configured.&lt;/p&gt;
&lt;h3&gt;Creating a Template&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# templates/node-service/template.yaml
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: node-service
  title: Node.js Microservice
  description: Create a production-ready Node.js service with Express, TypeScript, CI/CD, and monitoring
  tags:
    - nodejs
    - typescript
    - recommended
spec:
  owner: team-platform
  type: service

  parameters:
    - title: Service Details
      required: [name, description, owner]
      properties:
        name:
          title: Service Name
          type: string
          pattern: &#39;^[a-z][a-z0-9-]*$&#39;
          description: Lowercase with hyphens (e.g., order-api)
        description:
          title: Description
          type: string
        owner:
          title: Owner Team
          type: string
          ui:field: OwnerPicker
          ui:options:
            catalogFilter:
              kind: Group

    - title: Infrastructure
      properties:
        database:
          title: Database
          type: string
          enum: [none, postgresql, mongodb]
          default: none
        cache:
          title: Cache
          type: string
          enum: [none, redis]
          default: none
        messageQueue:
          title: Message Queue
          type: string
          enum: [none, rabbitmq, kafka]
          default: none

  steps:
    - id: fetch-template
      name: Fetch Template
      action: fetch:template
      input:
        url: ./skeleton
        values:
          name: &#39;$&#39;
          description: &#39;$&#39;
          owner: &#39;$&#39;
          database: &#39;$&#39;
          cache: &#39;$&#39;

    - id: publish
      name: Create Repository
      action: publish:github
      input:
        allowedHosts: [&#39;github.com&#39;]
        repoUrl: &#39;github.com?owner=my-org&amp;amp;repo=$&#39;
        description: &#39;$&#39;
        defaultBranch: main

    - id: register
      name: Register in Catalog
      action: catalog:register
      input:
        repoContentsUrl: &#39;$&#39;
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: &#39;$&#39;
      - title: Open in Catalog
        icon: catalog
        entityRef: &#39;$&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;What happens when a developer uses this template:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Fill out the form (name, owner, database choice)&lt;/li&gt;
&lt;li&gt;Template generates a full project from the skeleton&lt;/li&gt;
&lt;li&gt;GitHub repo is created with CI/CD, Dockerfile, and monitoring pre-configured&lt;/li&gt;
&lt;li&gt;Service is automatically registered in the Backstage catalog&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;Time to production-ready service: ~3 minutes&lt;/strong&gt; instead of 2-3 days.&lt;/p&gt;
&lt;h2&gt;Step 4: TechDocs&lt;/h2&gt;
&lt;p&gt;TechDocs renders Markdown documentation from each service&#39;s repository directly in Backstage. Documentation lives next to the code, always current.&lt;/p&gt;
&lt;h3&gt;Enable in a Service&lt;/h3&gt;
&lt;p&gt;Add to the service&#39;s repo:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# docs/
# ├── index.md
# ├── architecture.md
# └── runbook.md

# mkdocs.yml (in repo root)
site_name: Order API
nav:
  - Home: index.md
  - Architecture: architecture.md
  - Runbook: runbook.md

plugins:
  - techdocs-core
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The &lt;code&gt;backstage.io/techdocs-ref: dir:.&lt;/code&gt; annotation in &lt;code&gt;catalog-info.yaml&lt;/code&gt; tells Backstage to build docs from this repo. Developers see rendered documentation directly in the service&#39;s Backstage page.&lt;/p&gt;
&lt;h2&gt;Step 5: Essential Plugins&lt;/h2&gt;
&lt;h3&gt;Kubernetes Plugin&lt;/h3&gt;
&lt;p&gt;Shows pod status, deployments, and logs for each service:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# catalog-info.yaml annotation
metadata:
  annotations:
    backstage.io/kubernetes-id: order-api
    backstage.io/kubernetes-namespace: production
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Developers see:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Pod count and status (Running, Pending, CrashLoopBackOff)&lt;/li&gt;
&lt;li&gt;Recent deployments with rollout status&lt;/li&gt;
&lt;li&gt;Resource usage (CPU, memory)&lt;/li&gt;
&lt;li&gt;Pod logs (without needing &lt;code&gt;kubectl&lt;/code&gt; access)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;CI/CD Plugin (GitHub Actions)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;metadata:
  annotations:
    github.com/project-slug: my-org/order-api
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Shows workflow runs, build status, and deployment history directly in Backstage.&lt;/p&gt;
&lt;h3&gt;API Documentation Plugin&lt;/h3&gt;
&lt;p&gt;Register APIs alongside services:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: order-api
  description: Order management REST API
spec:
  type: openapi
  lifecycle: production
  owner: team-commerce
  definition:
    $text: ./openapi.yaml
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Renders interactive OpenAPI documentation in Backstage.&lt;/p&gt;
&lt;h2&gt;Measuring Platform Success&lt;/h2&gt;
&lt;p&gt;Track these metrics to know if your IDP is working:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Metric&lt;/th&gt;
&lt;th&gt;Target&lt;/th&gt;
&lt;th&gt;How to Measure&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Time to first deploy&lt;/td&gt;
&lt;td&gt;&amp;lt; 30 min&lt;/td&gt;
&lt;td&gt;Track template usage → first pipeline run&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Catalog coverage&lt;/td&gt;
&lt;td&gt;&amp;gt; 90%&lt;/td&gt;
&lt;td&gt;Services in Backstage / total services&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Self-service rate&lt;/td&gt;
&lt;td&gt;&amp;gt; 80%&lt;/td&gt;
&lt;td&gt;Infra requests via templates / total requests&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Developer satisfaction&lt;/td&gt;
&lt;td&gt;&amp;gt; 4/5&lt;/td&gt;
&lt;td&gt;Quarterly survey&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Onboarding time&lt;/td&gt;
&lt;td&gt;&amp;lt; 1 week&lt;/td&gt;
&lt;td&gt;New hire → first PR merged&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;Common Mistakes&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Empty catalog on launch&lt;/strong&gt; — pre-populate before announcing to the org&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Too many templates&lt;/strong&gt; — start with 2-3 golden paths, not 20 options&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No ownership data&lt;/strong&gt; — a catalog without owners is just a list&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Building alone&lt;/strong&gt; — platform engineering is a product; talk to your developer-customers&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Perfection before launch&lt;/strong&gt; — ship with the catalog + one template; iterate from there&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The portal is only as good as the data in it and the golden paths it provides. Start small, measure adoption, and expand based on what developers actually need.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>OpenTelemetry from Zero to Production</title>
    <link href="https://techai-explained.netlify.app/articles/opentelemetry-zero-to-production/"/>
    <updated>2026-03-08T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/opentelemetry-zero-to-production/</id>
    <content xml:lang="en" type="html">&lt;p&gt;OpenTelemetry (OTel) is the universal standard for observability. It gives you traces, metrics, and logs — vendor-neutral, language-agnostic, and supported by every major observability platform. But going from &amp;quot;install the SDK&amp;quot; to &amp;quot;debug a production incident in 30 seconds&amp;quot; requires understanding the full pipeline.&lt;/p&gt;
&lt;p&gt;This guide takes you from zero instrumentation to a production-ready observability stack.&lt;/p&gt;
&lt;h2&gt;The OpenTelemetry Architecture&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
YOUR APPLICATION                    BACKEND
──────────────                      ───────
&lt;p&gt;┌─────────────────┐    ┌──────────────┐    ┌─────────────┐
│  OTel SDK       │    │  OTel        │    │  Jaeger     │
│                 │───►│  Collector   │───►│  (Traces)   │
│  Auto-instrument│    │              │    ├─────────────┤
│  Custom spans   │    │  Receives    │    │ Prometheus  │
│  Custom metrics │    │  Processes   │───►│  (Metrics)  │
│                 │    │  Exports     │    ├─────────────┤
└─────────────────┘    └──────────────┘    │  Loki       │
───►│  (Logs)     │
└──────┬──────┘
│
┌──────┴──────┐
│  Grafana    │
│ (Dashboards)│
└─────────────┘&lt;/p&gt;
&lt;/div&gt;
&lt;p&gt;Three components:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;SDK&lt;/strong&gt; — instruments your code and collects telemetry&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Collector&lt;/strong&gt; — receives, processes, and exports telemetry data&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Backends&lt;/strong&gt; — store and visualize the data (Jaeger, Prometheus, Grafana)&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Step 1: Auto-Instrumentation (Node.js)&lt;/h2&gt;
&lt;p&gt;Auto-instrumentation captures traces from HTTP frameworks, database clients, and other libraries without changing your code.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm install @opentelemetry/sdk-node &#92;
  @opentelemetry/auto-instrumentations-node &#92;
  @opentelemetry/exporter-trace-otlp-http &#92;
  @opentelemetry/exporter-metrics-otlp-http
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Create the instrumentation file — loaded before your app:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// instrumentation.ts
import { NodeSDK } from &#39;@opentelemetry/sdk-node&#39;;
import { getNodeAutoInstrumentations } from &#39;@opentelemetry/auto-instrumentations-node&#39;;
import { OTLPTraceExporter } from &#39;@opentelemetry/exporter-trace-otlp-http&#39;;
import { OTLPMetricExporter } from &#39;@opentelemetry/exporter-metrics-otlp-http&#39;;
import { PeriodicExportingMetricReader } from &#39;@opentelemetry/sdk-metrics&#39;;
import { Resource } from &#39;@opentelemetry/resources&#39;;
import {
  ATTR_SERVICE_NAME,
  ATTR_SERVICE_VERSION,
} from &#39;@opentelemetry/semantic-conventions&#39;;

const sdk = new NodeSDK({
  resource: new Resource({
    [ATTR_SERVICE_NAME]: &#39;order-api&#39;,
    [ATTR_SERVICE_VERSION]: &#39;1.2.0&#39;,
    &#39;deployment.environment&#39;: process.env.NODE_ENV || &#39;development&#39;,
  }),
  traceExporter: new OTLPTraceExporter({
    url: &#39;http://otel-collector:4318/v1/traces&#39;,
  }),
  metricReader: new PeriodicExportingMetricReader({
    exporter: new OTLPMetricExporter({
      url: &#39;http://otel-collector:4318/v1/metrics&#39;,
    }),
    exportIntervalMillis: 15000,
  }),
  instrumentations: [
    getNodeAutoInstrumentations({
      &#39;@opentelemetry/instrumentation-http&#39;: { enabled: true },
      &#39;@opentelemetry/instrumentation-express&#39;: { enabled: true },
      &#39;@opentelemetry/instrumentation-pg&#39;: { enabled: true },
      &#39;@opentelemetry/instrumentation-redis&#39;: { enabled: true },
    }),
  ],
});

sdk.start();

process.on(&#39;SIGTERM&#39;, () =&amp;gt; {
  sdk.shutdown().then(() =&amp;gt; process.exit(0));
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Start your app with instrumentation:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;node --require ./instrumentation.js dist/index.js
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That&#39;s it. Every HTTP request, database query, and Redis call is now traced automatically.&lt;/p&gt;
&lt;h2&gt;Step 2: Auto-Instrumentation (.NET)&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;dotnet add package OpenTelemetry.Extensions.Hosting
dotnet add package OpenTelemetry.Instrumentation.AspNetCore
dotnet add package OpenTelemetry.Instrumentation.Http
dotnet add package OpenTelemetry.Instrumentation.SqlClient
dotnet add package OpenTelemetry.Exporter.OpenTelemetryProtocol
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Program.cs
builder.Services.AddOpenTelemetry()
    .ConfigureResource(r =&amp;gt; r
        .AddService(&amp;quot;order-api&amp;quot;, serviceVersion: &amp;quot;1.2.0&amp;quot;)
        .AddAttributes(new Dictionary&amp;lt;string, object&amp;gt;
        {
            [&amp;quot;deployment.environment&amp;quot;] = builder.Environment.EnvironmentName,
        }))
    .WithTracing(tracing =&amp;gt; tracing
        .AddAspNetCoreInstrumentation()
        .AddHttpClientInstrumentation()
        .AddSqlClientInstrumentation(o =&amp;gt; o.SetDbStatementForText = true)
        .AddOtlpExporter(o =&amp;gt;
        {
            o.Endpoint = new Uri(&amp;quot;http://otel-collector:4317&amp;quot;);
        }))
    .WithMetrics(metrics =&amp;gt; metrics
        .AddAspNetCoreInstrumentation()
        .AddHttpClientInstrumentation()
        .AddRuntimeInstrumentation()
        .AddOtlpExporter(o =&amp;gt;
        {
            o.Endpoint = new Uri(&amp;quot;http://otel-collector:4317&amp;quot;);
        }));
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 3: Custom Spans and Metrics&lt;/h2&gt;
&lt;p&gt;Auto-instrumentation captures infrastructure-level telemetry. Custom instrumentation captures your business logic.&lt;/p&gt;
&lt;h3&gt;Custom Spans (Traces)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { trace, SpanStatusCode } from &#39;@opentelemetry/api&#39;;

const tracer = trace.getTracer(&#39;order-service&#39;);

async function processOrder(order: Order): Promise&amp;lt;ProcessedOrder&amp;gt; {
  return tracer.startActiveSpan(&#39;process-order&#39;, async (span) =&amp;gt; {
    span.setAttribute(&#39;order.id&#39;, order.id);
    span.setAttribute(&#39;order.item_count&#39;, order.items.length);
    span.setAttribute(&#39;order.total&#39;, order.total);

    try {
      // Child span for inventory check
      const inventory = await tracer.startActiveSpan(
        &#39;check-inventory&#39;,
        async (childSpan) =&amp;gt; {
          childSpan.setAttribute(&#39;inventory.warehouse&#39;, &#39;us-east-1&#39;);
          const result = await inventoryService.check(order.items);
          childSpan.setAttribute(&#39;inventory.available&#39;, result.allAvailable);
          childSpan.end();
          return result;
        }
      );

      // Child span for payment
      const payment = await tracer.startActiveSpan(
        &#39;process-payment&#39;,
        async (childSpan) =&amp;gt; {
          childSpan.setAttribute(&#39;payment.method&#39;, order.paymentMethod);
          const result = await paymentService.charge(order.total);
          childSpan.setAttribute(&#39;payment.transaction_id&#39;, result.txId);
          childSpan.end();
          return result;
        }
      );

      span.setStatus({ code: SpanStatusCode.OK });
      return { orderId: order.id, status: &#39;completed&#39; };
    } catch (error) {
      span.setStatus({ code: SpanStatusCode.ERROR, message: error.message });
      span.recordException(error);
      throw error;
    } finally {
      span.end();
    }
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Custom Metrics&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { metrics } from &#39;@opentelemetry/api&#39;;

const meter = metrics.getMeter(&#39;order-service&#39;);

// Counter — tracks totals
const ordersProcessed = meter.createCounter(&#39;orders.processed&#39;, {
  description: &#39;Total orders processed&#39;,
  unit: &#39;orders&#39;,
});

// Histogram — tracks distributions
const orderDuration = meter.createHistogram(&#39;orders.duration&#39;, {
  description: &#39;Order processing duration&#39;,
  unit: &#39;ms&#39;,
});

// Up-down counter — tracks current values
const activeOrders = meter.createUpDownCounter(&#39;orders.active&#39;, {
  description: &#39;Currently processing orders&#39;,
});

// Usage
async function processOrder(order: Order) {
  activeOrders.add(1);
  const startTime = Date.now();

  try {
    const result = await doProcessing(order);
    ordersProcessed.add(1, {
      &#39;order.status&#39;: &#39;success&#39;,
      &#39;order.type&#39;: order.type,
    });
    return result;
  } catch (err) {
    ordersProcessed.add(1, { &#39;order.status&#39;: &#39;failure&#39; });
    throw err;
  } finally {
    orderDuration.record(Date.now() - startTime);
    activeOrders.add(-1);
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 4: The Collector&lt;/h2&gt;
&lt;p&gt;The OTel Collector is a proxy that receives telemetry, processes it, and exports to backends.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# otel-collector-config.yaml
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    send_batch_size: 1024
    timeout: 5s

  memory_limiter:
    check_interval: 1s
    limit_mib: 512
    spike_limit_mib: 128

  attributes:
    actions:
      - key: environment
        value: production
        action: upsert

exporters:
  otlp/jaeger:
    endpoint: jaeger:4317
    tls:
      insecure: true

  prometheus:
    endpoint: 0.0.0.0:8889

  loki:
    endpoint: http://loki:3100/loki/api/v1/push

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [memory_limiter, batch, attributes]
      exporters: [otlp/jaeger]

    metrics:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [prometheus]

    logs:
      receivers: [otlp]
      processors: [memory_limiter, batch]
      exporters: [loki]
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
COLLECTOR PIPELINE
&lt;p&gt;Receivers ──► Processors ──► Exporters&lt;/p&gt;
&lt;p&gt;OTLP/gRPC ─┐   ┌─ batch ─────┐   ┌─► Jaeger (traces)
├──►│  memory     ├──►├─► Prometheus (metrics)
OTLP/HTTP ─┘   │  attributes │   └─► Loki (logs)
└─────────────┘&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Step 5: Deploy the Full Stack&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# docker-compose.yml
services:
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    command: --config=/etc/otel-collector-config.yaml
    volumes:
      - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml
    ports:
      - &amp;quot;4317:4317&amp;quot;   # OTLP gRPC
      - &amp;quot;4318:4318&amp;quot;   # OTLP HTTP

  jaeger:
    image: jaegertracing/all-in-one:latest
    ports:
      - &amp;quot;16686:16686&amp;quot; # Jaeger UI

  prometheus:
    image: prom/prometheus:latest
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - &amp;quot;9090:9090&amp;quot;

  loki:
    image: grafana/loki:latest
    ports:
      - &amp;quot;3100:3100&amp;quot;

  grafana:
    image: grafana/grafana:latest
    ports:
      - &amp;quot;3000:3000&amp;quot;
    environment:
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Production Checklist&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;[ ] &lt;strong&gt;Sampling&lt;/strong&gt; — don&#39;t trace 100% of requests in production; use tail sampling or probability sampling&lt;/li&gt;
&lt;li&gt;[ ] &lt;strong&gt;Resource attributes&lt;/strong&gt; — set service name, version, and environment on every signal&lt;/li&gt;
&lt;li&gt;[ ] &lt;strong&gt;Collector as a sidecar or DaemonSet&lt;/strong&gt; — don&#39;t send directly to backends from your app&lt;/li&gt;
&lt;li&gt;[ ] &lt;strong&gt;Memory limits&lt;/strong&gt; — configure &lt;code&gt;memory_limiter&lt;/code&gt; processor to prevent OOM&lt;/li&gt;
&lt;li&gt;[ ] &lt;strong&gt;Alerting rules&lt;/strong&gt; — create Prometheus alerts for error rate spikes and latency thresholds&lt;/li&gt;
&lt;li&gt;[ ] &lt;strong&gt;Dashboard templates&lt;/strong&gt; — use Grafana&#39;s RED (Rate, Error, Duration) dashboard per service&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Debugging with Traces&lt;/h2&gt;
&lt;p&gt;When a request fails, the trace shows exactly what happened:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
TRACE: POST /api/orders (500 Internal Server Error)
&lt;p&gt;order-api ────────────────────────────────────── 342ms (ERROR)
│
├─ check-inventory ────────────────── 45ms ✅
│    └─ pg.query SELECT ... ───── 12ms ✅
│
├─ process-payment ────────────────── 287ms ❌
│    └─ http POST stripe.com ──── 285ms ❌
│         Status: 402 Payment Required
│         Error: Card declined
│
└─ (create-shipment — never reached)&lt;/p&gt;
&lt;p&gt;ROOT CAUSE: Payment failed due to declined card.
Time to diagnose: 15 seconds (not 15 minutes).&lt;/p&gt;
&lt;/div&gt;
&lt;p&gt;That&#39;s the power of distributed tracing. Every request tells a story. OpenTelemetry makes sure you can read it.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>AI Code Generation: When to Trust It and When to Rewrite</title>
    <link href="https://techai-explained.netlify.app/articles/ai-code-generation-trust-framework/"/>
    <updated>2026-03-09T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/ai-code-generation-trust-framework/</id>
    <content xml:lang="en" type="html">&lt;p&gt;AI code generation has reached an inflection point. The tooling is good enough that rejecting it entirely makes you slower. But trusting it blindly fills your codebase with subtle bugs, security holes, and unmaintainable patterns. The skill that matters now isn&#39;t whether you use AI to generate code — it&#39;s knowing &lt;strong&gt;when to accept, when to modify, and when to rewrite&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;This guide provides a practical decision framework based on real-world patterns.&lt;/p&gt;
&lt;h2&gt;The Trust Spectrum&lt;/h2&gt;
&lt;p&gt;Not all AI-generated code deserves the same level of scrutiny. Place every generation on this spectrum:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
LOW RISK                                          HIGH RISK
────────────────────────────────────────────────────────────
Accept      Review       Modify       Scrutinize    Rewrite
as-is       quickly      parts        line-by-line   entirely
&lt;p&gt;│           │            │            │              │
Boilerplate Tests        Business     Security       Crypto,
CRUD ops    Utilities    logic        Auth/AuthZ     Financial
Config      Adapters     API design   Data access    Compliance
Formatting  Conversions  State mgmt   Input parsing  Legal logic&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Category 1: Accept As-Is (Boilerplate &amp;amp; Config)&lt;/h2&gt;
&lt;p&gt;AI excels at generating code you&#39;d copy from documentation or Stack Overflow anyway. There&#39;s minimal risk because the patterns are well-established and easily verified.&lt;/p&gt;
&lt;h3&gt;Examples That AI Nails&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Express server setup — accept as-is
import express from &#39;express&#39;;
const app = express();
app.use(express.json());
app.use(express.urlencoded({ extended: true }));
app.use(cors({ origin: process.env.ALLOWED_ORIGINS?.split(&#39;,&#39;) }));
app.listen(process.env.PORT || 3000);
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# Docker Compose for local dev — accept as-is
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: myapp
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: devpass
    ports:
      - &amp;quot;5432:5432&amp;quot;
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// DTO mapping — accept as-is
public static OrderResponse ToResponse(this Order order) =&amp;gt; new()
{
    Id = order.Id,
    CustomerName = order.Customer.Name,
    Items = order.Items.Select(i =&amp;gt; new OrderItemResponse
    {
        ProductName = i.Product.Name,
        Quantity = i.Quantity,
        UnitPrice = i.UnitPrice,
    }).ToList(),
    Total = order.Total,
    CreatedAt = order.CreatedAt,
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Why it&#39;s safe:&lt;/strong&gt; These patterns are deterministic. There&#39;s one right way to set up an Express server or write a DTO mapper. The AI has seen millions of examples. Verify it compiles, move on.&lt;/p&gt;
&lt;h2&gt;Category 2: Review Quickly (Tests &amp;amp; Utilities)&lt;/h2&gt;
&lt;p&gt;AI generates decent test code and utility functions, but common issues include: incomplete edge cases, wrong assertions, and outdated API usage.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// AI-generated test — review for completeness
describe(&#39;calculateDiscount&#39;, () =&amp;gt; {
  it(&#39;applies 10% discount for orders over $100&#39;, () =&amp;gt; {
    expect(calculateDiscount(150)).toBe(15);
  });

  it(&#39;applies no discount for orders under $100&#39;, () =&amp;gt; {
    expect(calculateDiscount(50)).toBe(0);
  });

  it(&#39;handles zero amount&#39;, () =&amp;gt; {
    expect(calculateDiscount(0)).toBe(0);
  });

  // ⚠️ AI often MISSES these:
  // - Negative amounts
  // - Boundary value ($100 exactly)
  // - Non-numeric input
  // - Very large numbers (overflow)
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Review checklist for AI tests:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;[ ] Boundary values tested (off-by-one is AI&#39;s most common bug)&lt;/li&gt;
&lt;li&gt;[ ] Error cases included (null, undefined, negative, overflow)&lt;/li&gt;
&lt;li&gt;[ ] Assertions are correct (not just &lt;code&gt;toBeTruthy()&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;[ ] Test descriptions match what&#39;s actually being tested&lt;/li&gt;
&lt;li&gt;[ ] No hardcoded values that should be constants&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Category 3: Modify Parts (Business Logic)&lt;/h2&gt;
&lt;p&gt;AI can scaffold business logic, but it makes assumptions about your domain that are often wrong. Accept the structure, rewrite the details.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// AI-generated order processing — NEEDS MODIFICATION
async function processOrder(order: Order): Promise&amp;lt;ProcessedOrder&amp;gt; {
  // ✅ Structure is good
  validateOrder(order);
  const inventory = await checkInventory(order.items);
  const payment = await chargePayment(order.total, order.paymentMethod);
  const shipment = await createShipment(order);

  return {
    orderId: order.id,
    status: &#39;completed&#39;,
    trackingNumber: shipment.tracking,
  };
}

// ⚠️ Problems:
// 1. No transaction/saga pattern — if shipment fails, payment isn&#39;t refunded
// 2. No idempotency check — processing the same order twice charges twice
// 3. Inventory check and reservation are separate — race condition
// 4. No partial fulfillment logic — what if only some items are in stock?
// 5. Error handling is missing entirely
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;The fix pattern:&lt;/strong&gt; Keep the function signature and flow. Add the domain-specific error handling, transaction management, and edge cases that AI doesn&#39;t know about.&lt;/p&gt;
&lt;h2&gt;Category 4: Scrutinize Line-by-Line (Security &amp;amp; Data Access)&lt;/h2&gt;
&lt;p&gt;AI-generated security code and database queries require the highest scrutiny. The code often &lt;strong&gt;looks&lt;/strong&gt; correct but has subtle vulnerabilities.&lt;/p&gt;
&lt;h3&gt;SQL Injection — AI Gets This Wrong More Than You&#39;d Think&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;# AI-generated — LOOKS fine but has issues
def search_users(query: str, role: str = None):
    sql = &amp;quot;SELECT * FROM users WHERE name ILIKE %s&amp;quot;
    params = [f&amp;quot;%{query}%&amp;quot;]

    if role:
        sql += f&amp;quot; AND role = &#39;{role}&#39;&amp;quot;  # 💀 SQL INJECTION!
        # AI parameterized the first value but not the second

    return db.execute(sql, params)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Auth Logic — Subtle Bugs&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// AI-generated auth middleware — SCRUTINIZE CAREFULLY
async function authMiddleware(req, res, next) {
  const token = req.headers.authorization?.split(&#39; &#39;)[1];

  if (!token) {
    return res.status(401).json({ error: &#39;No token&#39; });
  }

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (err) {
    return res.status(401).json({ error: &#39;Invalid token&#39; });
  }
}

// ⚠️ Issues AI commonly introduces:
// 1. No algorithm restriction — vulnerable to &#39;none&#39; algorithm attack
// 2. No audience/issuer validation
// 3. Token from header only — what about cookies?
// 4. No token revocation check
// 5. Error message might leak information

// CORRECT version:
const decoded = jwt.verify(token, process.env.JWT_SECRET, {
  algorithms: [&#39;HS256&#39;],     // Restrict algorithms
  audience: &#39;my-app&#39;,        // Validate audience
  issuer: &#39;auth.my-app.com&#39;, // Validate issuer
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;The Security Review Checklist for AI Code&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;[ ] All user inputs are parameterized/escaped&lt;/li&gt;
&lt;li&gt;[ ] Authentication checks validate algorithm, audience, and issuer&lt;/li&gt;
&lt;li&gt;[ ] Authorization checks are present (not just authentication)&lt;/li&gt;
&lt;li&gt;[ ] Secrets are not hardcoded or logged&lt;/li&gt;
&lt;li&gt;[ ] Error messages don&#39;t leak internal details&lt;/li&gt;
&lt;li&gt;[ ] Rate limiting is applied to sensitive endpoints&lt;/li&gt;
&lt;li&gt;[ ] CORS is configured restrictively, not &lt;code&gt;*&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Category 5: Rewrite Entirely (Crypto, Financial, Compliance)&lt;/h2&gt;
&lt;p&gt;Some code should never be accepted from AI generation. Period.&lt;/p&gt;
&lt;h3&gt;Code You Should Always Write Yourself&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Cryptographic implementations&lt;/strong&gt; — AI will suggest patterns that look correct but have timing attacks, weak random number generation, or deprecated algorithms&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Financial calculations&lt;/strong&gt; — floating-point errors in currency math create real liability&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Compliance logic&lt;/strong&gt; — GDPR data retention, HIPAA access controls, SOX audit trails&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Security-critical parsers&lt;/strong&gt; — XML/JSON parsers that handle untrusted input&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Consensus algorithms&lt;/strong&gt; — distributed systems coordination logic&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;If the bug causes a security breach, financial loss, or regulatory violation — don&#39;t trust AI-generated code. Write it. Review it. Test it. Then have someone else review it.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;The Decision Framework&lt;/h2&gt;
&lt;p&gt;When AI generates code, ask these five questions:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
1. BLAST RADIUS: If this code has a bug, what&#39;s the worst outcome?
   Low (UI glitch) ──────────────────── High (data breach)
&lt;ol start=&quot;2&quot;&gt;
&lt;li&gt;
&lt;p&gt;DOMAIN SPECIFICITY: How unique is this to MY application?
Generic (CRUD, config) ──────────── Specific (business rules)&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;VERIFIABILITY: Can I quickly verify this is correct?
Easy (run tests) ────────────────── Hard (needs prod data)&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;SECURITY SURFACE: Does this handle untrusted input?
No (internal service) ───────────── Yes (user-facing API)&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;REGULATORY IMPACT: Does this affect compliance?
No ──────────────────────────────── Yes (audit, legal)&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Score each 1-5. Total &amp;gt; 15? Rewrite. Total &amp;lt; 10? Accept.&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Practical Workflow&lt;/h2&gt;
&lt;p&gt;Here&#39;s the workflow that maximizes AI productivity while minimizing risk:&lt;/p&gt;
&lt;h3&gt;Step 1: Generate with Context&lt;/h3&gt;
&lt;p&gt;Give the AI maximum context — relevant files, types, conventions. Vague prompts produce generic code.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;❌ &amp;quot;Write a user service&amp;quot;
✅ &amp;quot;Write a UserService class that uses the existing UserRepository 
    (see src/repos/user.repo.ts) to implement getById, create, and 
    update. Follow the error handling pattern in OrderService. Use 
    Zod for input validation.&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Step 2: Categorize Before Reading&lt;/h3&gt;
&lt;p&gt;Before reading the generated code, determine which category it falls into. This sets your review depth.&lt;/p&gt;
&lt;h3&gt;Step 3: Test First&lt;/h3&gt;
&lt;p&gt;Run the generated code against tests before reading it line by line. If tests pass, you&#39;ve reduced the review surface. If they fail, you know exactly where to look.&lt;/p&gt;
&lt;h3&gt;Step 4: Check for AI&#39;s Common Mistakes&lt;/h3&gt;
&lt;p&gt;Regardless of category, check for these patterns:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Hallucinated APIs&lt;/strong&gt; — methods or packages that don&#39;t exist&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Outdated patterns&lt;/strong&gt; — training data cutoff means AI suggests deprecated approaches&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Missing error handling&lt;/strong&gt; — AI optimizes for the happy path&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Incorrect types&lt;/strong&gt; — especially in TypeScript, types are often &lt;code&gt;any&lt;/code&gt; or wrong&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Copy-paste-itis&lt;/strong&gt; — repeated code that should be abstracted&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;The Bottom Line&lt;/h2&gt;
&lt;p&gt;AI code generation is a tool, not a teammate. It doesn&#39;t understand your business domain, your security requirements, or your production environment. It understands patterns.&lt;/p&gt;
&lt;p&gt;Use it for patterns. Verify it for logic. Rewrite it for security. And always, always test what it gives you.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>GitHub Actions Advanced: Matrix Builds, Reusable Workflows, and OIDC</title>
    <link href="https://techai-explained.netlify.app/articles/github-actions-advanced/"/>
    <updated>2026-03-10T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/github-actions-advanced/</id>
    <content xml:lang="en" type="html">&lt;p&gt;If your GitHub Actions workflows are still a single YAML file with hardcoded values, you&#39;re leaving most of the platform&#39;s power on the table. This guide covers the three patterns that separate basic CI/CD from production-grade pipelines: matrix builds, reusable workflows, and OIDC authentication.&lt;/p&gt;
&lt;h2&gt;Matrix Builds: Test Everything in Parallel&lt;/h2&gt;
&lt;p&gt;Matrix strategies let you run the same job across multiple configurations simultaneously — different OS versions, language versions, or any variable you define.&lt;/p&gt;
&lt;h3&gt;Basic Matrix&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;name: CI
on: [push, pull_request]

jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: [18, 20, 22]
      fail-fast: false  # Don&#39;t cancel other jobs if one fails

    runs-on: ${{ matrix.os }}
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      
      - run: npm ci
      - run: npm test
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This creates &lt;strong&gt;9 parallel jobs&lt;/strong&gt; (3 OS × 3 Node versions). Without matrix, you&#39;d copy-paste the same job 9 times.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
MATRIX EXPANSION: 3 OS × 3 Node = 9 parallel jobs
&lt;p&gt;ubuntu-latest + Node 18  ──► ✅
ubuntu-latest + Node 20  ──► ✅
ubuntu-latest + Node 22  ──► ✅
windows-latest + Node 18 ──► ✅
windows-latest + Node 20 ──► ❌ (test failure)
windows-latest + Node 22 ──► ✅
macos-latest + Node 18   ──► ✅
macos-latest + Node 20   ──► ✅
macos-latest + Node 22   ──► ✅&lt;/p&gt;
&lt;p&gt;fail-fast: false → all 9 run even if one fails
fail-fast: true  → cancel remaining after first failure&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Advanced Matrix: Include and Exclude&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;strategy:
  matrix:
    os: [ubuntu-latest, windows-latest]
    node-version: [18, 20, 22]
    
    # Add specific combinations with extra variables
    include:
      - os: ubuntu-latest
        node-version: 22
        coverage: true        # Only run coverage on one combo
        experimental: false
      
      - os: ubuntu-latest
        node-version: 23      # Add a version not in the base matrix
        experimental: true    # Mark as allowed-to-fail
    
    # Remove specific combinations
    exclude:
      - os: windows-latest
        node-version: 18      # Don&#39;t test Node 18 on Windows
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Using Matrix Variables in Steps&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;steps:
  - run: npm test
  
  - name: Upload coverage
    if: matrix.coverage == true
    run: npm run coverage &amp;amp;&amp;amp; npx codecov
  
  - name: Mark experimental as non-blocking
    if: matrix.experimental == true
    continue-on-error: true
    run: npm run test:experimental
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Dynamic Matrix from JSON&lt;/h3&gt;
&lt;p&gt;For maximum flexibility, generate the matrix dynamically:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;jobs:
  determine-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          # Could read from a file, API, or compute dynamically
          echo &#39;matrix={&amp;quot;include&amp;quot;:[
            {&amp;quot;project&amp;quot;:&amp;quot;api&amp;quot;,&amp;quot;dockerfile&amp;quot;:&amp;quot;api/Dockerfile&amp;quot;},
            {&amp;quot;project&amp;quot;:&amp;quot;web&amp;quot;,&amp;quot;dockerfile&amp;quot;:&amp;quot;web/Dockerfile&amp;quot;},
            {&amp;quot;project&amp;quot;:&amp;quot;worker&amp;quot;,&amp;quot;dockerfile&amp;quot;:&amp;quot;worker/Dockerfile&amp;quot;}
          ]}&#39; &amp;gt;&amp;gt; $GITHUB_OUTPUT

  build:
    needs: determine-matrix
    strategy:
      matrix: ${{ fromJSON(needs.determine-matrix.outputs.matrix) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -f ${{ matrix.dockerfile }} -t ${{ matrix.project }} .
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Reusable Workflows: DRY CI/CD&lt;/h2&gt;
&lt;p&gt;If you have 20 repositories with nearly identical CI workflows, reusable workflows let you define the pipeline once and call it from everywhere.&lt;/p&gt;
&lt;h3&gt;Create a Reusable Workflow&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# .github/workflows/build-and-deploy.yml (in your shared repo)
name: Build and Deploy

on:
  workflow_call:  # This makes it reusable
    inputs:
      environment:
        required: true
        type: string
        description: &amp;quot;Target environment (staging, production)&amp;quot;
      node-version:
        required: false
        type: number
        default: 20
      run-e2e:
        required: false
        type: boolean
        default: true
    secrets:
      DEPLOY_TOKEN:
        required: true
      SLACK_WEBHOOK:
        required: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm
      
      - run: npm ci
      - run: npm run build
      - run: npm test
      
      - name: E2E Tests
        if: inputs.run-e2e
        run: npm run test:e2e
      
      - uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: dist/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-output
          path: dist/
      
      - name: Deploy
        run: ./deploy.sh ${{ inputs.environment }}
        env:
          DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
      
      - name: Notify Slack
        if: always() &amp;amp;&amp;amp; secrets.SLACK_WEBHOOK != &#39;&#39;
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK }} &#92;
            -H &#39;Content-Type: application/json&#39; &#92;
            -d &#39;{&amp;quot;text&amp;quot;:&amp;quot;Deploy to ${{ inputs.environment }}: ${{ job.status }}&amp;quot;}&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Call the Reusable Workflow&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# In any consuming repository
name: Deploy to Staging

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: my-org/shared-workflows/.github/workflows/build-and-deploy.yml@main
    with:
      environment: staging
      node-version: 22
      run-e2e: true
    secrets:
      DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Composite Actions: Reusable Steps&lt;/h3&gt;
&lt;p&gt;For reusing a sequence of steps (not full workflows), use composite actions:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# .github/actions/setup-and-build/action.yml
name: Setup and Build
description: Install dependencies and build the project

inputs:
  node-version:
    description: Node.js version
    default: &#39;20&#39;

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: npm
    
    - run: npm ci
      shell: bash
    
    - run: npm run build
      shell: bash
    
    - run: npm run lint
      shell: bash
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Use it in any workflow:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;steps:
  - uses: actions/checkout@v4
  - uses: my-org/shared-actions/.github/actions/setup-and-build@main
    with:
      node-version: 22
  - run: npm test
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
REUSABLE WORKFLOWS vs COMPOSITE ACTIONS
&lt;p&gt;Reusable Workflows              Composite Actions
────────────────────            ──────────────────
Reuse entire JOBS               Reuse STEPS within a job
Called with &lt;code&gt;uses:&lt;/code&gt; at job      Called with &lt;code&gt;uses:&lt;/code&gt; at step
level                           level&lt;/p&gt;
&lt;p&gt;Can define multiple jobs        Single sequence of steps
Have their own runner           Run on the caller&#39;s runner
Can accept secrets              Cannot accept secrets
Separate workflow file          action.yml in a directory&lt;/p&gt;
&lt;p&gt;USE WHEN:                       USE WHEN:
Full CI/CD pipeline             Shared setup/teardown steps
Multi-job orchestration         Linting, building, caching
Environment-specific deploys    Common action sequences&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;OIDC: Secretless Cloud Deployments&lt;/h2&gt;
&lt;p&gt;OpenID Connect (OIDC) eliminates stored cloud credentials. Instead of storing an AWS access key or Azure service principal secret in GitHub, the workflow requests a short-lived token directly from the cloud provider.&lt;/p&gt;
&lt;h3&gt;How OIDC Works&lt;/h3&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
TRADITIONAL (with secrets)       OIDC (secretless)
──────────────────────────       ─────────────────
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;Store cloud credentials      1. Configure trust between
in GitHub Secrets               GitHub and cloud provider&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Workflow reads secret         2. Workflow requests JWT
from secrets context             from GitHub&#39;s OIDC provider&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Use credential to auth       3. Exchange JWT for short-lived
with cloud provider              cloud credential&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Credential is long-lived     4. Credential expires in minutes
(rotate manually)               (no rotation needed)&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;RISK: Secret leak = full        RISK: Minimal — tokens are
access until rotated            scoped and short-lived&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Azure OIDC Setup&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Step 1: Create a federated credential in Azure&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# Create an app registration
az ad app create --display-name &amp;quot;github-actions-deploy&amp;quot;
APP_ID=$(az ad app list --display-name &amp;quot;github-actions-deploy&amp;quot; --query &#39;[0].appId&#39; -o tsv)

# Create a service principal
az ad sp create --id $APP_ID

# Add federated credential for GitHub Actions
az ad app federated-credential create --id $APP_ID --parameters &#39;{
  &amp;quot;name&amp;quot;: &amp;quot;github-main-branch&amp;quot;,
  &amp;quot;issuer&amp;quot;: &amp;quot;https://token.actions.githubusercontent.com&amp;quot;,
  &amp;quot;subject&amp;quot;: &amp;quot;repo:my-org/my-repo:ref:refs/heads/main&amp;quot;,
  &amp;quot;audiences&amp;quot;: [&amp;quot;api://AzureADTokenExchange&amp;quot;]
}&#39;

# Assign roles
az role assignment create &#92;
  --assignee $APP_ID &#92;
  --role &amp;quot;Contributor&amp;quot; &#92;
  --scope &amp;quot;/subscriptions/YOUR_SUB_ID&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Step 2: Use in your workflow&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;name: Deploy to Azure

on:
  push:
    branches: [main]

permissions:
  id-token: write   # Required for OIDC
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
          # No secret needed! OIDC handles authentication

      - run: az webapp deploy --name myapp --src-path ./dist
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;AWS OIDC Setup&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789:role/github-actions
          aws-region: us-east-1
          # No AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY needed!
      
      - run: aws s3 sync ./dist s3://my-bucket/
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;OIDC Subject Claims&lt;/h3&gt;
&lt;p&gt;Control which workflows can assume which roles:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;repo:my-org/my-repo:ref:refs/heads/main         → Only main branch
repo:my-org/my-repo:environment:production       → Only production env
repo:my-org/my-repo:pull_request                 → Any PR
repo:my-org/my-repo:ref:refs/tags/v*             → Only version tags
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This means a PR workflow &lt;strong&gt;cannot&lt;/strong&gt; assume the production deployment role. Security by design.&lt;/p&gt;
&lt;h2&gt;Putting It All Together&lt;/h2&gt;
&lt;p&gt;Here&#39;s a production pipeline combining all three patterns:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;name: Full Pipeline

on:
  push:
    branches: [main]
  pull_request:

permissions:
  id-token: write
  contents: read

jobs:
  # Matrix: test across platforms
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [20, 22]
      fail-fast: false
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-and-build  # Composite action
        with:
          node-version: ${{ matrix.node }}
      - run: npm test

  # Reusable workflow: deploy to staging
  deploy-staging:
    if: github.ref == &#39;refs/heads/main&#39;
    needs: test
    uses: ./.github/workflows/deploy.yml
    with:
      environment: staging
    secrets: inherit

  # OIDC: deploy to production
  deploy-production:
    if: github.ref == &#39;refs/heads/main&#39;
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: production  # Requires approval
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      - run: ./deploy.sh production
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Common Pitfalls&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Not using &lt;code&gt;fail-fast: false&lt;/code&gt;&lt;/strong&gt; — one flaky test cancels all matrix jobs&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Storing cloud secrets&lt;/strong&gt; — use OIDC instead; it&#39;s more secure and no rotation needed&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Copy-pasting workflows&lt;/strong&gt; — use reusable workflows and composite actions&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No caching&lt;/strong&gt; — always cache &lt;code&gt;node_modules&lt;/code&gt;, &lt;code&gt;~/.m2&lt;/code&gt;, &lt;code&gt;~/.gradle&lt;/code&gt;, etc.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Overly broad OIDC subjects&lt;/strong&gt; — scope to specific branches and environments&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Event-Driven Architecture with Azure Service Bus and .NET</title>
    <link href="https://techai-explained.netlify.app/articles/event-driven-azure-service-bus/"/>
    <updated>2026-03-11T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/event-driven-azure-service-bus/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Event-driven architecture decouples services so they communicate through events instead of direct calls. When a user places an order, the order service publishes an &lt;code&gt;OrderPlaced&lt;/code&gt; event — and every service that cares (inventory, notifications, analytics) reacts independently. No one waits. No one blocks.&lt;/p&gt;
&lt;p&gt;Azure Service Bus is the backbone for these systems in the Microsoft ecosystem. This guide shows you how to build one properly — with dead-letter handling, retry policies, and the patterns that survive production.&lt;/p&gt;
&lt;h2&gt;Why Events Instead of API Calls?&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
SYNCHRONOUS (API Calls)          ASYNCHRONOUS (Events)
───────────────────────          ─────────────────────
&lt;p&gt;Order Service                    Order Service
│                                │
├── POST /inventory/reserve      ├── Publish: OrderPlaced
│   (waits 200ms)                │   (fire and forget)
│                                │
├── POST /email/send             │
│   (waits 150ms)                │
│                                │
└── POST /analytics/track   ┌────┴────┐────────┐
(waits 100ms)           ▼         ▼        ▼
Inventory  Email   Analytics
Total: 450ms + coupling      (subscribe) (sub)   (sub)
If email is down → order&lt;br /&gt;
fails!                       Total: ~5ms (publish only)
If email is down → order
still succeeds!&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Azure Service Bus Concepts&lt;/h2&gt;
&lt;p&gt;Before writing code, understand the two messaging patterns:&lt;/p&gt;
&lt;h3&gt;Queues: Point-to-Point&lt;/h3&gt;
&lt;p&gt;One sender, one receiver. Each message is processed by exactly one consumer.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Producer ──► [  Queue  ] ──► Consumer
              msg msg msg
              (FIFO order)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Use for: background job processing, command execution, work distribution.&lt;/p&gt;
&lt;h3&gt;Topics &amp;amp; Subscriptions: Publish-Subscribe&lt;/h3&gt;
&lt;p&gt;One publisher, many subscribers. Each subscriber gets its own copy of every message.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Publisher ──► [ Topic ]
                 │
         ┌───────┼───────┐
         ▼       ▼       ▼
      [Sub A] [Sub B] [Sub C]
      Email   Inventory Analytics
      Service Service  Service
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Use for: event broadcasting, fan-out patterns, decoupling services.&lt;/p&gt;
&lt;h2&gt;Setting Up Azure Service Bus&lt;/h2&gt;
&lt;h3&gt;Create the Infrastructure&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# Create resource group and namespace
az group create --name rg-events --location eastus

az servicebus namespace create &#92;
  --name sb-myapp &#92;
  --resource-group rg-events &#92;
  --sku Standard

# Create a topic with subscriptions
az servicebus topic create &#92;
  --namespace-name sb-myapp &#92;
  --resource-group rg-events &#92;
  --name order-events

az servicebus topic subscription create &#92;
  --namespace-name sb-myapp &#92;
  --resource-group rg-events &#92;
  --topic-name order-events &#92;
  --name inventory-sub

az servicebus topic subscription create &#92;
  --namespace-name sb-myapp &#92;
  --resource-group rg-events &#92;
  --topic-name order-events &#92;
  --name notification-sub
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;.NET Project Setup&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;dotnet new worker -n OrderProcessor
cd OrderProcessor
dotnet add package Azure.Messaging.ServiceBus
dotnet add package Microsoft.Extensions.Azure
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Publishing Events&lt;/h2&gt;
&lt;h3&gt;Define Your Events&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Events/OrderPlaced.cs
public record OrderPlaced
{
    public required Guid OrderId { get; init; }
    public required Guid CustomerId { get; init; }
    public required List&amp;lt;OrderItem&amp;gt; Items { get; init; }
    public required decimal TotalAmount { get; init; }
    public required DateTimeOffset OccurredAt { get; init; }
}

public record OrderItem
{
    public required string ProductId { get; init; }
    public required int Quantity { get; init; }
    public required decimal UnitPrice { get; init; }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Publish with Retry and Correlation&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Services/EventPublisher.cs
public class EventPublisher : IAsyncDisposable
{
    private readonly ServiceBusSender _sender;

    public EventPublisher(ServiceBusClient client, string topicName)
    {
        _sender = client.CreateSender(topicName);
    }

    public async Task PublishAsync&amp;lt;T&amp;gt;(
        T @event,
        string correlationId,
        CancellationToken ct = default) where T : class
    {
        var message = new ServiceBusMessage(
            BinaryData.FromObjectAsJson(@event))
        {
            ContentType = &amp;quot;application/json&amp;quot;,
            Subject = typeof(T).Name,
            CorrelationId = correlationId,
            MessageId = Guid.NewGuid().ToString(),
            ApplicationProperties =
            {
                [&amp;quot;EventType&amp;quot;] = typeof(T).Name,
                [&amp;quot;Timestamp&amp;quot;] = DateTimeOffset.UtcNow.ToString(&amp;quot;O&amp;quot;),
            }
        };

        await _sender.SendMessageAsync(message, ct);
    }

    public async ValueTask DisposeAsync()
    {
        await _sender.DisposeAsync();
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Usage in Your API&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;app.MapPost(&amp;quot;/api/orders&amp;quot;, async (
    CreateOrderRequest request,
    OrderService orderService,
    EventPublisher events) =&amp;gt;
{
    var order = await orderService.CreateAsync(request);

    await events.PublishAsync(new OrderPlaced
    {
        OrderId = order.Id,
        CustomerId = request.CustomerId,
        Items = order.Items,
        TotalAmount = order.Total,
        OccurredAt = DateTimeOffset.UtcNow,
    }, correlationId: order.Id.ToString());

    return Results.Created($&amp;quot;/api/orders/{order.Id}&amp;quot;, order);
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Consuming Events&lt;/h2&gt;
&lt;h3&gt;The Processor Pattern&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Workers/InventoryWorker.cs
public class InventoryWorker : BackgroundService
{
    private readonly ServiceBusProcessor _processor;
    private readonly IServiceScopeFactory _scopeFactory;
    private readonly ILogger&amp;lt;InventoryWorker&amp;gt; _logger;

    public InventoryWorker(
        ServiceBusClient client,
        IServiceScopeFactory scopeFactory,
        ILogger&amp;lt;InventoryWorker&amp;gt; logger)
    {
        _scopeFactory = scopeFactory;
        _logger = logger;
        _processor = client.CreateProcessor(
            &amp;quot;order-events&amp;quot;,
            &amp;quot;inventory-sub&amp;quot;,
            new ServiceBusProcessorOptions
            {
                MaxConcurrentCalls = 10,
                AutoCompleteMessages = false,
                PrefetchCount = 20,
            });
    }

    protected override async Task ExecuteAsync(CancellationToken ct)
    {
        _processor.ProcessMessageAsync += HandleMessageAsync;
        _processor.ProcessErrorAsync += HandleErrorAsync;

        await _processor.StartProcessingAsync(ct);

        // Keep running until cancellation
        await Task.Delay(Timeout.Infinite, ct);
    }

    private async Task HandleMessageAsync(
        ProcessMessageEventArgs args)
    {
        var eventType = args.Message.Subject;

        using var scope = _scopeFactory.CreateScope();
        var handler = scope.ServiceProvider
            .GetRequiredService&amp;lt;IEventHandler&amp;gt;();

        try
        {
            switch (eventType)
            {
                case nameof(OrderPlaced):
                    var order = args.Message.Body
                        .ToObjectFromJson&amp;lt;OrderPlaced&amp;gt;();
                    await handler.HandleAsync(order);
                    break;

                default:
                    _logger.LogWarning(
                        &amp;quot;Unknown event type: {EventType}&amp;quot;, eventType);
                    break;
            }

            await args.CompleteMessageAsync(args.Message);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex,
                &amp;quot;Failed to process {EventType}, attempt {Count}&amp;quot;,
                eventType, args.Message.DeliveryCount);

            if (args.Message.DeliveryCount &amp;gt;= 3)
            {
                await args.DeadLetterMessageAsync(
                    args.Message,
                    &amp;quot;MaxRetriesExceeded&amp;quot;,
                    ex.Message);
            }
            else
            {
                await args.AbandonMessageAsync(args.Message);
            }
        }
    }

    private Task HandleErrorAsync(ProcessErrorEventArgs args)
    {
        _logger.LogError(args.Exception,
            &amp;quot;Service Bus error: {Source}&amp;quot;, args.ErrorSource);
        return Task.CompletedTask;
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Dead-Letter Queue: Your Safety Net&lt;/h2&gt;
&lt;p&gt;Messages that can&#39;t be processed go to the dead-letter queue (DLQ). This prevents poison messages from blocking your entire pipeline.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
Normal Flow:
Publisher ──► Topic ──► Subscription ──► Consumer ──► ✓ Complete
&lt;p&gt;Failure Flow (after max retries):
Publisher ──► Topic ──► Subscription ──► Consumer ──► ✗ Fail
│
▼ (after 3 retries)
┌──────────────┐
│ Dead-Letter   │
│ Queue (DLQ)   │
│               │
│ Inspect, fix, │
│ and replay    │
└──────────────┘&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Monitoring the Dead-Letter Queue&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Services/DeadLetterMonitor.cs
public class DeadLetterMonitor : BackgroundService
{
    protected override async Task ExecuteAsync(CancellationToken ct)
    {
        var dlqReceiver = _client.CreateReceiver(
            &amp;quot;order-events&amp;quot;,
            &amp;quot;inventory-sub&amp;quot;,
            new ServiceBusReceiverOptions
            {
                SubQueue = SubQueue.DeadLetter,
            });

        while (!ct.IsCancellationRequested)
        {
            var messages = await dlqReceiver
                .ReceiveMessagesAsync(10, TimeSpan.FromSeconds(5), ct);

            foreach (var msg in messages)
            {
                _logger.LogWarning(
                    &amp;quot;DLQ message: {Subject}, Reason: {Reason}, Error: {Error}&amp;quot;,
                    msg.Subject,
                    msg.DeadLetterReason,
                    msg.DeadLetterErrorDescription);

                // Option 1: Fix and re-publish to the main topic
                // Option 2: Store for manual inspection
                // Option 3: Alert the ops team

                await dlqReceiver.CompleteMessageAsync(msg, ct);
            }

            await Task.Delay(TimeSpan.FromMinutes(1), ct);
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Idempotency: Process Once, Even If Delivered Twice&lt;/h2&gt;
&lt;p&gt;Service Bus guarantees &lt;strong&gt;at-least-once&lt;/strong&gt; delivery. Your handlers must be idempotent — processing the same message twice should produce the same result.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;public class IdempotentOrderHandler : IEventHandler
{
    private readonly IDistributedCache _cache;

    public async Task HandleAsync(OrderPlaced @event)
    {
        var messageKey = $&amp;quot;processed:{@event.OrderId}&amp;quot;;

        // Check if already processed
        var existing = await _cache.GetStringAsync(messageKey);
        if (existing != null)
        {
            _logger.LogInformation(
                &amp;quot;Order {OrderId} already processed, skipping&amp;quot;,
                @event.OrderId);
            return;
        }

        // Process the event
        await _inventoryService.ReserveItemsAsync(@event.Items);

        // Mark as processed (with TTL)
        await _cache.SetStringAsync(
            messageKey,
            &amp;quot;true&amp;quot;,
            new DistributedCacheEntryOptions
            {
                AbsoluteExpirationRelativeToNow = TimeSpan.FromHours(24),
            });
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Subscription Filters&lt;/h2&gt;
&lt;p&gt;Not every subscriber needs every message. Use SQL filters to route specific events:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Only receive high-value orders
await adminClient.CreateRuleAsync(
    &amp;quot;order-events&amp;quot;,
    &amp;quot;high-value-sub&amp;quot;,
    new CreateRuleOptions
    {
        Name = &amp;quot;HighValueFilter&amp;quot;,
        Filter = new SqlRuleFilter(&amp;quot;TotalAmount &amp;gt; 1000&amp;quot;),
    });

// Only receive specific event types
await adminClient.CreateRuleAsync(
    &amp;quot;order-events&amp;quot;,
    &amp;quot;notification-sub&amp;quot;,
    new CreateRuleOptions
    {
        Name = &amp;quot;OrderEventsOnly&amp;quot;,
        Filter = new SqlRuleFilter(
            &amp;quot;Subject IN (&#39;OrderPlaced&#39;, &#39;OrderCancelled&#39;)&amp;quot;),
    });
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Production Checklist&lt;/h2&gt;
&lt;h3&gt;Message Design&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;[ ] Events are immutable facts (past tense: &lt;code&gt;OrderPlaced&lt;/code&gt;, not &lt;code&gt;PlaceOrder&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;[ ] Each event includes a correlation ID for tracing&lt;/li&gt;
&lt;li&gt;[ ] Schema versioning strategy defined (add fields, never remove)&lt;/li&gt;
&lt;li&gt;[ ] Message size under 256 KB (use claim-check pattern for large payloads)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Reliability&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;[ ] Dead-letter queue monitoring and alerting configured&lt;/li&gt;
&lt;li&gt;[ ] All handlers are idempotent&lt;/li&gt;
&lt;li&gt;[ ] Retry policy configured (exponential backoff)&lt;/li&gt;
&lt;li&gt;[ ] Circuit breaker on downstream dependencies&lt;/li&gt;
&lt;li&gt;[ ] Max delivery count set appropriately (default is 10)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Observability&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;[ ] Structured logging with correlation IDs&lt;/li&gt;
&lt;li&gt;[ ] Metrics: queue depth, processing rate, DLQ depth, latency&lt;/li&gt;
&lt;li&gt;[ ] Alerts on DLQ growth and processing failures&lt;/li&gt;
&lt;li&gt;[ ] Distributed tracing across publisher → Service Bus → consumer&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Performance&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;[ ] &lt;code&gt;MaxConcurrentCalls&lt;/code&gt; tuned for your workload&lt;/li&gt;
&lt;li&gt;[ ] &lt;code&gt;PrefetchCount&lt;/code&gt; set to batch-optimize network calls&lt;/li&gt;
&lt;li&gt;[ ] Sessions enabled if message ordering is required&lt;/li&gt;
&lt;li&gt;[ ] Partitioning enabled for high-throughput scenarios&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;When NOT to Use Service Bus&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Simple fire-and-forget notifications&lt;/strong&gt; → use Azure Event Grid (cheaper, simpler)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;High-throughput event streaming&lt;/strong&gt; → use Azure Event Hubs or Kafka (millions/sec)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Request-response patterns&lt;/strong&gt; → use HTTP/gRPC (events are one-way by design)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Local development only&lt;/strong&gt; → use an in-memory queue or RabbitMQ in Docker&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Choose Service Bus when you need &lt;strong&gt;reliable, transactional messaging&lt;/strong&gt; with dead-letter support, sessions, and enterprise-grade SLAs.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>From Monolith to Microservices: A Practical Migration Guide</title>
    <link href="https://techai-explained.netlify.app/articles/monolith-to-microservices-guide/"/>
    <updated>2026-03-12T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/monolith-to-microservices-guide/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Most monolith-to-microservices migrations fail. Not because the target architecture is wrong, but because teams try to do it all at once. This guide covers the incremental approach that actually works in production — using the Strangler Fig pattern, domain-driven decomposition, and data migration strategies that don&#39;t require downtime.&lt;/p&gt;
&lt;h2&gt;Should You Even Migrate?&lt;/h2&gt;
&lt;p&gt;Before writing a single line of code, answer these questions honestly:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
MIGRATION DECISION TREE
&lt;p&gt;Is your monolith blocking team velocity?
├─ NO ──► Don&#39;t migrate. Invest in the monolith.
│         A well-structured monolith is fine.
│
└─ YES: Why?
├─ Deployment coupling (one team blocks others)
│   └─► Microservices might help.
│
├─ Scaling (one hot path needs 10x resources)
│   └─► Extract just that service.
│
├─ Technology mismatch (need ML in Python, app is C#)
│   └─► Extract the boundary where tech changes.
│
└─ &amp;quot;Everyone else is doing it&amp;quot;
└─► DON&#39;T migrate. This is not a reason.&lt;/p&gt;
&lt;/div&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Rule of thumb:&lt;/strong&gt; If you can&#39;t articulate which specific bounded context you&#39;d extract first and why, you&#39;re not ready to migrate.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2&gt;Phase 1: Understand Your Monolith&lt;/h2&gt;
&lt;h3&gt;Map the Domain Boundaries&lt;/h3&gt;
&lt;p&gt;Before decomposing, you need to understand what you&#39;re decomposing. Use Event Storming or a simpler approach — map every major feature to its data dependencies.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Feature → Tables Accessed → External APIs Called

User Registration → users, profiles, email_verification → SendGrid
Order Placement → orders, order_items, inventory, payments → Stripe
Product Search → products, categories, reviews → Elasticsearch
Shipping → orders, shipments, addresses → FedEx API
Reporting → orders, users, products, shipments → (none)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Identify Bounded Contexts&lt;/h3&gt;
&lt;p&gt;Group features that share data and change together:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
BOUNDED CONTEXTS (identified from data dependencies)
&lt;p&gt;┌──────────────────┐  ┌──────────────────┐
│   IDENTITY       │  │   CATALOG        │
│                  │  │                  │
│  users           │  │  products        │
│  profiles        │  │  categories      │
│  email_verify    │  │  reviews         │
│  auth_tokens     │  │  search_index    │
│                  │  │                  │
│  Registration    │  │  Product CRUD    │
│  Login/Logout    │  │  Search          │
│  Profile mgmt   │  │  Review system   │
└──────────────────┘  └──────────────────┘&lt;/p&gt;
&lt;p&gt;┌──────────────────┐  ┌──────────────────┐
│   ORDERING       │  │   SHIPPING       │
│                  │  │                  │
│  orders          │  │  shipments       │
│  order_items     │  │  addresses       │
│  payments        │  │  tracking        │
│  inventory       │  │                  │
│                  │  │  Label creation  │
│  Cart            │  │  Tracking        │
│  Checkout        │  │  Delivery notify │
│  Payment         │  │                  │
└──────────────────┘  └──────────────────┘&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Measure Coupling&lt;/h3&gt;
&lt;p&gt;Find the cross-boundary queries. These are the painful parts of migration:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-sql&quot;&gt;-- This query spans Ordering + Catalog + Identity
SELECT o.id, u.name, p.title, o.total
FROM orders o
JOIN users u ON o.user_id = u.id
JOIN order_items oi ON o.id = oi.order_id
JOIN products p ON oi.product_id = p.id
WHERE o.created_at &amp;gt; &#39;2026-01-01&#39;;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Every cross-boundary join becomes an API call after migration. Count them. Prioritize decomposing contexts with the &lt;strong&gt;fewest&lt;/strong&gt; cross-boundary dependencies first.&lt;/p&gt;
&lt;h2&gt;Phase 2: The Strangler Fig Pattern&lt;/h2&gt;
&lt;p&gt;Don&#39;t rewrite the monolith. Strangle it — gradually routing traffic from the monolith to new services until the monolith is empty.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
STRANGLER FIG PATTERN (over time)
&lt;p&gt;STEP 1: Route through a facade
┌──────────┐     ┌──────────┐     ┌──────────────┐
│  Client  │────►│  Facade  │────►│  Monolith    │
│          │     │ (Gateway) │     │  (all code)  │
└──────────┘     └──────────┘     └──────────────┘&lt;/p&gt;
&lt;p&gt;STEP 2: Extract first service
┌──────────┐     ┌──────────┐     ┌──────────────┐
│  Client  │────►│  Facade  │──┬─►│  Monolith    │
│          │     │ (Gateway) │  │  │  (minus      │
└──────────┘     └──────────┘  │  │   catalog)   │
│  └──────────────┘
│  ┌──────────────┐
└─►│  Catalog     │
│  Service     │
└──────────────┘&lt;/p&gt;
&lt;p&gt;STEP 3: Extract more services
┌──────────┐     ┌──────────┐     ┌──────────────┐
│  Client  │────►│  Facade  │──┬─►│  Monolith    │
│          │     │ (Gateway) │  │  │  (shrinking) │
└──────────┘     └──────────┘  │  └──────────────┘
├─►│ Catalog Svc  │
├─►│ Identity Svc │
└─►│ Shipping Svc │&lt;/p&gt;
&lt;p&gt;STEP 4: Monolith is gone
┌──────────┐     ┌──────────┐
│  Client  │────►│  Gateway  │──┬─► Catalog
│          │     │           │  ├─► Identity
└──────────┘     └──────────┘  ├─► Ordering
└─► Shipping&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Implementing the Facade&lt;/h3&gt;
&lt;p&gt;Use an API gateway (NGINX, Kong, or a cloud-native option) as the strangler facade:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# nginx.conf - route by path
upstream monolith {
  server monolith:8080;
}

upstream catalog_service {
  server catalog:8080;
}

server {
  listen 80;

  # New service handles catalog
  location /api/products {
    proxy_pass http://catalog_service;
  }

  location /api/categories {
    proxy_pass http://catalog_service;
  }

  # Everything else goes to monolith
  location / {
    proxy_pass http://monolith;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Phase 3: Extract Your First Service&lt;/h2&gt;
&lt;p&gt;Choose the bounded context with:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Fewest dependencies&lt;/strong&gt; on other contexts&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Clearest data ownership&lt;/strong&gt; (tables belong only to this context)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Highest business value&lt;/strong&gt; for being independently deployable&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Often this is the &lt;strong&gt;Catalog&lt;/strong&gt; or &lt;strong&gt;Identity&lt;/strong&gt; context.&lt;/p&gt;
&lt;h3&gt;Step 1: Create the New Service&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// catalog-service/src/index.ts
import express from &#39;express&#39;;
import { Pool } from &#39;pg&#39;;

const app = express();
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
});

// Replicate the monolith&#39;s API contract exactly
app.get(&#39;/api/products&#39;, async (req, res) =&amp;gt; {
  const { rows } = await pool.query(
    &#39;SELECT * FROM products ORDER BY created_at DESC LIMIT $1&#39;,
    [req.query.limit || 50]
  );
  res.json(rows);
});

app.get(&#39;/api/products/:id&#39;, async (req, res) =&amp;gt; {
  const { rows } = await pool.query(
    &#39;SELECT * FROM products WHERE id = $1&#39;,
    [req.params.id]
  );
  if (rows.length === 0) return res.status(404).json({ error: &#39;Not found&#39; });
  res.json(rows[0]);
});

app.listen(8080);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Step 2: Run Both in Parallel&lt;/h3&gt;
&lt;p&gt;The &lt;strong&gt;parallel run&lt;/strong&gt; pattern validates the new service before cutting over:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// In the API gateway or facade
app.get(&#39;/api/products/:id&#39;, async (req, res) =&amp;gt; {
  // Primary: monolith (still serving traffic)
  const monolithResponse = await fetch(
    `http://monolith:8080/api/products/${req.params.id}`
  );

  // Shadow: new service (results compared, not returned)
  const newServiceResponse = await fetch(
    `http://catalog:8080/api/products/${req.params.id}`
  ).catch(err =&amp;gt; ({ error: err.message }));

  // Compare responses
  if (JSON.stringify(monolithResponse) !== JSON.stringify(newServiceResponse)) {
    metrics.increment(&#39;catalog.migration.mismatch&#39;);
    logger.warn(&#39;Response mismatch&#39;, {
      monolith: monolithResponse,
      newService: newServiceResponse,
    });
  }

  // Still return the monolith response
  res.json(await monolithResponse.json());
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Step 3: Cut Over&lt;/h3&gt;
&lt;p&gt;Once the mismatch rate is zero, switch the gateway to route to the new service.&lt;/p&gt;
&lt;h2&gt;Phase 4: Data Decomposition&lt;/h2&gt;
&lt;p&gt;This is where most migrations stall. The monolith&#39;s database has cross-context foreign keys, shared tables, and reporting queries that span everything.&lt;/p&gt;
&lt;h3&gt;Strategy 1: Shared Database (Temporary)&lt;/h3&gt;
&lt;p&gt;Start by letting the new service read from the monolith&#39;s database:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;┌──────────────┐     ┌──────────────┐
│   Monolith   │     │  Catalog Svc │
│              │     │              │
└──────┬───────┘     └──────┬───────┘
       │                    │
       └────────┬───────────┘
                │
       ┌────────┴────────┐
       │  Shared Database │  ← Temporary!
       │  (monolith DB)   │
       └─────────────────┘
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;This is not the end state.&lt;/strong&gt; It&#39;s a stepping stone. The service works, but you haven&#39;t achieved data independence.&lt;/p&gt;
&lt;h3&gt;Strategy 2: Database per Service (Target)&lt;/h3&gt;
&lt;p&gt;Migrate the service&#39;s tables to its own database:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;┌──────────────┐     ┌──────────────┐
│   Monolith   │     │  Catalog Svc │
│              │     │              │
└──────┬───────┘     └──────┬───────┘
       │                    │
┌──────┴───────┐    ┌──────┴───────┐
│ Monolith DB  │    │ Catalog DB   │
│ (minus       │    │ products     │
│  products)   │    │ categories   │
└──────────────┘    │ reviews      │
                    └──────────────┘
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Data Migration Script&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;# migrate_catalog_data.py
import psycopg2
from datetime import datetime

source = psycopg2.connect(SOURCE_DB_URL)
target = psycopg2.connect(TARGET_DB_URL)

# Phase 1: Copy historical data
with source.cursor() as src, target.cursor() as tgt:
    src.execute(&amp;quot;SELECT * FROM products&amp;quot;)
    for row in src:
        tgt.execute(
            &amp;quot;INSERT INTO products VALUES (%s, %s, %s, %s, %s) &amp;quot;
            &amp;quot;ON CONFLICT (id) DO NOTHING&amp;quot;,
            row
        )
    target.commit()

# Phase 2: Set up CDC (Change Data Capture) for ongoing sync
# Use Debezium, AWS DMS, or a custom trigger
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Handling Cross-Service Queries&lt;/h3&gt;
&lt;p&gt;The reporting query that joined &lt;code&gt;orders&lt;/code&gt;, &lt;code&gt;users&lt;/code&gt;, and &lt;code&gt;products&lt;/code&gt; now spans three services. Options:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;API Composition&lt;/strong&gt; — the gateway calls each service and joins in memory&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;CQRS Read Model&lt;/strong&gt; — a denormalized read database updated by events&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Data Lake&lt;/strong&gt; — services publish events, analytics queries run against the lake&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// API Composition pattern
app.get(&#39;/api/reports/orders&#39;, async (req, res) =&amp;gt; {
  const orders = await orderService.getOrders(req.query);
  
  // Enrich with data from other services
  const userIds = [...new Set(orders.map(o =&amp;gt; o.userId))];
  const productIds = [...new Set(orders.flatMap(o =&amp;gt; o.items.map(i =&amp;gt; i.productId)))];

  const [users, products] = await Promise.all([
    identityService.getUsersByIds(userIds),
    catalogService.getProductsByIds(productIds),
  ]);

  const enriched = orders.map(order =&amp;gt; ({
    ...order,
    user: users.find(u =&amp;gt; u.id === order.userId),
    items: order.items.map(item =&amp;gt; ({
      ...item,
      product: products.find(p =&amp;gt; p.id === item.productId),
    })),
  }));

  res.json(enriched);
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;The Top 5 Migration Mistakes&lt;/h2&gt;
&lt;h3&gt;1. Big Bang Rewrite&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;&amp;quot;Let&#39;s rewrite the entire monolith in microservices over 6 months.&amp;quot;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;This fails because:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Requirements change during the rewrite&lt;/li&gt;
&lt;li&gt;The team maintains two systems simultaneously&lt;/li&gt;
&lt;li&gt;Integration testing the new system is harder than expected&lt;/li&gt;
&lt;li&gt;Stakeholders lose patience before it&#39;s done&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Fix:&lt;/strong&gt; Strangler Fig. Extract one service at a time, validate it works, then move to the next.&lt;/p&gt;
&lt;h3&gt;2. Starting with the Wrong Service&lt;/h3&gt;
&lt;p&gt;Teams often extract the most complex, most coupled service first because &amp;quot;that&#39;s where the pain is.&amp;quot; This maximizes risk.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Fix:&lt;/strong&gt; Start with a low-coupling, low-risk context. Get the infrastructure (CI/CD, service mesh, monitoring) right on something simple.&lt;/p&gt;
&lt;h3&gt;3. Distributed Monolith&lt;/h3&gt;
&lt;p&gt;Services that can&#39;t be deployed independently, that share a database, and that require coordinated releases — you&#39;ve built a distributed monolith. All the complexity of microservices, none of the benefits.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Fix:&lt;/strong&gt; Each service must own its data and be independently deployable. If two services always deploy together, merge them.&lt;/p&gt;
&lt;h3&gt;4. Ignoring Data Consistency&lt;/h3&gt;
&lt;p&gt;In a monolith, database transactions guarantee consistency. In microservices, you need eventual consistency patterns:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Saga pattern for cross-service transactions
class OrderSaga {
  async execute(order: Order) {
    try {
      await inventoryService.reserve(order.items);
      await paymentService.charge(order.total);
      await orderService.confirm(order.id);
    } catch (error) {
      // Compensating transactions
      await inventoryService.release(order.items);
      await paymentService.refund(order.total);
      await orderService.cancel(order.id);
      throw error;
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;5. No Observability&lt;/h3&gt;
&lt;p&gt;You can&#39;t debug a distributed system with &lt;code&gt;console.log&lt;/code&gt;. Before extracting the first service:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Set up distributed tracing (OpenTelemetry)&lt;/li&gt;
&lt;li&gt;Centralize logs with correlation IDs&lt;/li&gt;
&lt;li&gt;Create dashboards for latency, error rates, and throughput per service&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Migration Timeline (Realistic)&lt;/h2&gt;
&lt;p&gt;For a medium-sized monolith (50K-200K lines of code):&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Phase&lt;/th&gt;
&lt;th&gt;Duration&lt;/th&gt;
&lt;th&gt;Deliverable&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Domain analysis&lt;/td&gt;
&lt;td&gt;2-4 weeks&lt;/td&gt;
&lt;td&gt;Bounded context map, dependency graph&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Infrastructure&lt;/td&gt;
&lt;td&gt;4-6 weeks&lt;/td&gt;
&lt;td&gt;CI/CD, gateway, monitoring, service mesh&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;First service&lt;/td&gt;
&lt;td&gt;4-8 weeks&lt;/td&gt;
&lt;td&gt;One service extracted, validated, in production&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Second service&lt;/td&gt;
&lt;td&gt;3-6 weeks&lt;/td&gt;
&lt;td&gt;Second extraction (faster due to patterns)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Remaining services&lt;/td&gt;
&lt;td&gt;3-4 weeks each&lt;/td&gt;
&lt;td&gt;Incremental extraction&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Data decomposition&lt;/td&gt;
&lt;td&gt;Ongoing&lt;/td&gt;
&lt;td&gt;Parallel with service extraction&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Decommission monolith&lt;/td&gt;
&lt;td&gt;2-4 weeks&lt;/td&gt;
&lt;td&gt;Final cleanup, remove monolith&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;Total: 6-18 months&lt;/strong&gt; depending on monolith size and team experience.&lt;/p&gt;
&lt;h2&gt;Checklist Before You Start&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;[ ] Can you articulate why microservices (not &amp;quot;everyone does it&amp;quot;)&lt;/li&gt;
&lt;li&gt;[ ] Have you mapped bounded contexts and data dependencies&lt;/li&gt;
&lt;li&gt;[ ] Do you have a CI/CD pipeline that can deploy services independently&lt;/li&gt;
&lt;li&gt;[ ] Is distributed tracing and centralized logging set up&lt;/li&gt;
&lt;li&gt;[ ] Have you chosen your first service to extract (low coupling)&lt;/li&gt;
&lt;li&gt;[ ] Do stakeholders understand this is a 6-18 month journey&lt;/li&gt;
&lt;li&gt;[ ] Is the team trained on eventual consistency and distributed systems patterns&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;If you can&#39;t check every box, invest in those items before starting the migration. The infrastructure and organizational readiness matter more than the code.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>WebSockets vs Server-Sent Events vs gRPC Streaming in 2026</title>
    <link href="https://techai-explained.netlify.app/articles/websockets-vs-sse-vs-grpc/"/>
    <updated>2026-03-13T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/websockets-vs-sse-vs-grpc/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Choosing a real-time protocol used to be simple: &amp;quot;just use WebSockets.&amp;quot; In 2026, the landscape is more nuanced. Server-Sent Events (SSE) power every major LLM streaming API. gRPC streaming dominates service-to-service communication. And WebSockets remain the go-to for bidirectional real-time apps.&lt;/p&gt;
&lt;p&gt;This guide compares all three with production benchmarks, code examples, and a decision framework.&lt;/p&gt;
&lt;h2&gt;The Quick Comparison&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌──────────────────────────────────────────────────────────┐
│              REAL-TIME PROTOCOL COMPARISON                │
│                                                          │
│  WEBSOCKETS         SSE              gRPC STREAMING      │
│  ───────────        ───              ──────────────      │
│  Bidirectional      Server → Client  Bidirectional       │
│  Binary + Text      Text only        Binary (Protobuf)  │
│  Custom protocol    HTTP/1.1+        HTTP/2              │
│  Manual reconnect   Auto-reconnect   Auto-reconnect     │
│  No built-in auth   Cookie/Bearer    mTLS + tokens      │
│  Any language       Any language     Code-gen required   │
│                                                          │
│  BEST FOR:          BEST FOR:        BEST FOR:          │
│  Chat, gaming,      LLM streaming,   Microservices,     │
│  collaboration      notifications,   high-throughput,   │
│  tools              live feeds       type-safe APIs     │
└──────────────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h2&gt;Protocol 1: WebSockets&lt;/h2&gt;
&lt;p&gt;WebSockets upgrade an HTTP connection to a persistent, full-duplex channel. Both client and server can send messages at any time.&lt;/p&gt;
&lt;h3&gt;How It Works&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;Client                          Server
  │                               │
  │── HTTP GET /ws (Upgrade) ────►│
  │◄─ 101 Switching Protocols ────│
  │                               │
  │◄═══════ Full Duplex ════════►│
  │   Binary or text frames       │
  │   No request/response model   │
  │   Either side can send        │
  │                               │
  │── Close frame ───────────────►│
  │◄─ Close frame ────────────────│
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Server Implementation (Node.js)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { WebSocketServer, WebSocket } from &#39;ws&#39;;

const wss = new WebSocketServer({ port: 8080 });

// Track connected clients
const clients = new Map&amp;lt;string, WebSocket&amp;gt;();

wss.on(&#39;connection&#39;, (ws, req) =&amp;gt; {
  const userId = authenticate(req);
  clients.set(userId, ws);

  ws.on(&#39;message&#39;, (data) =&amp;gt; {
    const message = JSON.parse(data.toString());

    switch (message.type) {
      case &#39;chat&#39;:
        broadcastToRoom(message.room, {
          type: &#39;chat&#39;,
          from: userId,
          text: message.text,
          timestamp: Date.now(),
        });
        break;

      case &#39;typing&#39;:
        broadcastToRoom(message.room, {
          type: &#39;typing&#39;,
          user: userId,
        });
        break;
    }
  });

  ws.on(&#39;close&#39;, () =&amp;gt; {
    clients.delete(userId);
  });

  // Heartbeat to detect dead connections
  ws.isAlive = true;
  ws.on(&#39;pong&#39;, () =&amp;gt; { ws.isAlive = true; });
});

// Ping every 30s, terminate dead connections
setInterval(() =&amp;gt; {
  wss.clients.forEach((ws) =&amp;gt; {
    if (!ws.isAlive) return ws.terminate();
    ws.isAlive = false;
    ws.ping();
  });
}, 30000);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Client Implementation&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;class WebSocketClient {
  private ws: WebSocket | null = null;
  private reconnectDelay = 1000;
  private maxReconnectDelay = 30000;

  connect(url: string) {
    this.ws = new WebSocket(url);

    this.ws.onopen = () =&amp;gt; {
      this.reconnectDelay = 1000; // Reset on successful connect
      console.log(&#39;Connected&#39;);
    };

    this.ws.onmessage = (event) =&amp;gt; {
      const message = JSON.parse(event.data);
      this.handleMessage(message);
    };

    this.ws.onclose = (event) =&amp;gt; {
      if (!event.wasClean) {
        // Reconnect with exponential backoff
        setTimeout(() =&amp;gt; this.connect(url), this.reconnectDelay);
        this.reconnectDelay = Math.min(
          this.reconnectDelay * 2,
          this.maxReconnectDelay
        );
      }
    };
  }

  send(message: object) {
    if (this.ws?.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(message));
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;WebSocket Gotchas&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;No automatic reconnection&lt;/strong&gt; — you must implement reconnect logic yourself&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Load balancer complexity&lt;/strong&gt; — sticky sessions or shared state required&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Proxy issues&lt;/strong&gt; — some corporate proxies terminate WebSocket connections&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No built-in multiplexing&lt;/strong&gt; — one connection = one channel (unlike HTTP/2)&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Protocol 2: Server-Sent Events (SSE)&lt;/h2&gt;
&lt;p&gt;SSE is a one-way channel: server pushes events to the client over a standard HTTP connection. It&#39;s the protocol behind every streaming LLM API (OpenAI, Anthropic, Google).&lt;/p&gt;
&lt;h3&gt;How It Works&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;Client                          Server
  │                               │
  │── GET /events ───────────────►│
  │◄─ 200 OK                     │
  │   Content-Type: text/         │
  │   event-stream                │
  │                               │
  │◄── data: {&amp;quot;temp&amp;quot;: 72}        │
  │◄── data: {&amp;quot;temp&amp;quot;: 73}        │
  │◄── data: {&amp;quot;temp&amp;quot;: 71}        │
  │    ...continuous stream...    │
  │                               │
  │   (Connection drops)          │
  │── GET /events?lastId=42 ────►│  ← Auto-reconnect!
  │◄── data: {&amp;quot;temp&amp;quot;: 74}        │
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Server Implementation (Node.js)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import express from &#39;express&#39;;

const app = express();

app.get(&#39;/events&#39;, (req, res) =&amp;gt; {
  // SSE headers
  res.writeHead(200, {
    &#39;Content-Type&#39;: &#39;text/event-stream&#39;,
    &#39;Cache-Control&#39;: &#39;no-cache&#39;,
    &#39;Connection&#39;: &#39;keep-alive&#39;,
  });

  // Send initial data
  res.write(`data: ${JSON.stringify({ status: &#39;connected&#39; })}&#92;n&#92;n`);

  // Stream updates
  const interval = setInterval(() =&amp;gt; {
    const event = {
      id: Date.now().toString(),
      type: &#39;temperature&#39;,
      data: { value: Math.random() * 100, unit: &#39;F&#39; },
    };

    res.write(`id: ${event.id}&#92;n`);
    res.write(`event: ${event.type}&#92;n`);
    res.write(`data: ${JSON.stringify(event.data)}&#92;n&#92;n`);
  }, 1000);

  // Cleanup on disconnect
  req.on(&#39;close&#39;, () =&amp;gt; {
    clearInterval(interval);
  });
});

// LLM-style streaming endpoint
app.post(&#39;/chat/stream&#39;, async (req, res) =&amp;gt; {
  res.writeHead(200, {
    &#39;Content-Type&#39;: &#39;text/event-stream&#39;,
    &#39;Cache-Control&#39;: &#39;no-cache&#39;,
  });

  const stream = await llm.chat.stream(req.body.messages);

  for await (const chunk of stream) {
    res.write(`data: ${JSON.stringify({
      choices: [{ delta: { content: chunk.text } }]
    })}&#92;n&#92;n`);
  }

  res.write(&#39;data: [DONE]&#92;n&#92;n&#39;);
  res.end();
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Client Implementation&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Native browser API - it&#39;s this simple
const eventSource = new EventSource(&#39;/events&#39;);

// Automatic reconnection is built-in!
eventSource.onmessage = (event) =&amp;gt; {
  const data = JSON.parse(event.data);
  console.log(&#39;Received:&#39;, data);
};

// Named events
eventSource.addEventListener(&#39;temperature&#39;, (event) =&amp;gt; {
  const data = JSON.parse(event.data);
  updateDashboard(data);
});

eventSource.onerror = (event) =&amp;gt; {
  console.log(&#39;Connection lost, reconnecting...&#39;);
  // EventSource auto-reconnects! No code needed.
};

// For POST requests (LLM streaming), use fetch + ReadableStream
async function streamChat(messages: Message[]) {
  const response = await fetch(&#39;/chat/stream&#39;, {
    method: &#39;POST&#39;,
    headers: { &#39;Content-Type&#39;: &#39;application/json&#39; },
    body: JSON.stringify({ messages }),
  });

  const reader = response.body!.getReader();
  const decoder = new TextDecoder();

  while (true) {
    const { done, value } = await reader.read();
    if (done) break;

    const chunk = decoder.decode(value);
    const lines = chunk.split(&#39;&#92;n&#39;).filter(l =&amp;gt; l.startsWith(&#39;data: &#39;));

    for (const line of lines) {
      const data = line.slice(6);
      if (data === &#39;[DONE]&#39;) return;
      const parsed = JSON.parse(data);
      appendToChat(parsed.choices[0].delta.content);
    }
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Why SSE Won for LLM Streaming&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Simplicity&lt;/strong&gt; — standard HTTP, works with every proxy, CDN, and load balancer&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Auto-reconnect&lt;/strong&gt; — &lt;code&gt;EventSource&lt;/code&gt; reconnects automatically with &lt;code&gt;Last-Event-ID&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;One-direction is enough&lt;/strong&gt; — user sends a prompt (POST), server streams the response&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No special infrastructure&lt;/strong&gt; — any HTTP server can do SSE&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Protocol 3: gRPC Streaming&lt;/h2&gt;
&lt;p&gt;gRPC uses HTTP/2 for multiplexed, binary-encoded streaming. It&#39;s the standard for service-to-service communication in microservice architectures.&lt;/p&gt;
&lt;h3&gt;Define the Service (Protobuf)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-protobuf&quot;&gt;syntax = &amp;quot;proto3&amp;quot;;

service StockService {
  // Server streaming - server sends multiple responses
  rpc StreamPrices (PriceRequest) returns (stream PriceUpdate);

  // Client streaming - client sends multiple requests
  rpc UploadTrades (stream Trade) returns (TradesSummary);

  // Bidirectional streaming - both sides stream
  rpc TradeChat (stream TradeMessage) returns (stream TradeMessage);
}

message PriceRequest {
  repeated string symbols = 1;
}

message PriceUpdate {
  string symbol = 1;
  double price = 2;
  int64 timestamp = 3;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Server Implementation (Go)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-go&quot;&gt;func (s *StockServer) StreamPrices(
    req *pb.PriceRequest,
    stream pb.StockService_StreamPricesServer,
) error {
    ticker := time.NewTicker(100 * time.Millisecond)
    defer ticker.Stop()

    for {
        select {
        case &amp;lt;-stream.Context().Done():
            return nil // Client disconnected
        case &amp;lt;-ticker.C:
            for _, symbol := range req.Symbols {
                update := &amp;amp;pb.PriceUpdate{
                    Symbol:    symbol,
                    Price:     getLatestPrice(symbol),
                    Timestamp: time.Now().UnixMilli(),
                }
                if err := stream.Send(update); err != nil {
                    return err
                }
            }
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Client Implementation (TypeScript/Node.js)&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { credentials } from &#39;@grpc/grpc-js&#39;;
import { StockServiceClient } from &#39;./generated/stock_grpc_pb&#39;;

const client = new StockServiceClient(
  &#39;localhost:50051&#39;,
  credentials.createInsecure()
);

const request = new PriceRequest();
request.setSymbolsList([&#39;AAPL&#39;, &#39;GOOG&#39;, &#39;MSFT&#39;]);

const stream = client.streamPrices(request);

stream.on(&#39;data&#39;, (update: PriceUpdate) =&amp;gt; {
  console.log(`${update.getSymbol()}: $${update.getPrice()}`);
});

stream.on(&#39;error&#39;, (err) =&amp;gt; {
  console.error(&#39;Stream error:&#39;, err.message);
});

stream.on(&#39;end&#39;, () =&amp;gt; {
  console.log(&#39;Stream ended&#39;);
});
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;gRPC Streaming Modes&lt;/h3&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
UNARY           SERVER STREAM     CLIENT STREAM     BIDI STREAM
─────           ─────────────     ─────────────     ───────────
&lt;p&gt;Client──►Server  Client──►Server  Client──►Server  Client◄──►Server
Client◄──Server  Client◄──Server  Client◄──Server
Client◄──Server  Client──►Server  Client◄──►Server
Client◄──Server  Client──►Server  Client◄──►Server
Client◄──Server                   Client◄──►Server&lt;/p&gt;
&lt;p&gt;1 request        1 request         N requests       N messages
1 response       N responses       1 response       N messages&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Performance Comparison&lt;/h2&gt;
&lt;p&gt;Real-world benchmarks for streaming 10,000 events/second to 100 clients:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Metric&lt;/th&gt;
&lt;th&gt;WebSocket&lt;/th&gt;
&lt;th&gt;SSE&lt;/th&gt;
&lt;th&gt;gRPC Stream&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Throughput&lt;/td&gt;
&lt;td&gt;95K msg/s&lt;/td&gt;
&lt;td&gt;82K msg/s&lt;/td&gt;
&lt;td&gt;120K msg/s&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Latency (p50)&lt;/td&gt;
&lt;td&gt;1.2ms&lt;/td&gt;
&lt;td&gt;2.1ms&lt;/td&gt;
&lt;td&gt;0.8ms&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Latency (p99)&lt;/td&gt;
&lt;td&gt;8ms&lt;/td&gt;
&lt;td&gt;15ms&lt;/td&gt;
&lt;td&gt;4ms&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Memory/connection&lt;/td&gt;
&lt;td&gt;2.4 KB&lt;/td&gt;
&lt;td&gt;1.8 KB&lt;/td&gt;
&lt;td&gt;3.2 KB&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CPU overhead&lt;/td&gt;
&lt;td&gt;Low&lt;/td&gt;
&lt;td&gt;Lowest&lt;/td&gt;
&lt;td&gt;Medium&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Bandwidth&lt;/td&gt;
&lt;td&gt;Efficient&lt;/td&gt;
&lt;td&gt;Text overhead&lt;/td&gt;
&lt;td&gt;Most efficient&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;gRPC wins on throughput and latency&lt;/strong&gt; due to HTTP/2 multiplexing and binary encoding. &lt;strong&gt;SSE wins on simplicity and memory.&lt;/strong&gt; &lt;strong&gt;WebSocket is the middle ground&lt;/strong&gt; with bidirectional capability.&lt;/p&gt;
&lt;h2&gt;Decision Framework&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
START HERE
│
├─ Do both sides need to send data simultaneously?
│  ├─ YES: Is it service-to-service?
│  │  ├─ YES ──────────────► gRPC Bidirectional Streaming
│  │  └─ NO (browser client) ► WebSocket
│  │
│  └─ NO: Is it server-to-client only?
│     ├─ YES: Is the data high-frequency (&gt;100/sec)?
│     │  ├─ YES ──────────────► gRPC Server Streaming
│     │  └─ NO ───────────────► Server-Sent Events
│     │
│     └─ It&#39;s client-to-server only
│        └─ Use regular HTTP POST/PUT (or gRPC Client Streaming)
&lt;/div&gt;
&lt;h3&gt;Use WebSocket When:&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Building chat, collaboration tools, or multiplayer games&lt;/li&gt;
&lt;li&gt;Both sides send data frequently and unpredictably&lt;/li&gt;
&lt;li&gt;You need binary data in the browser (file transfer, screen sharing)&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Use SSE When:&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Server pushes updates to browser clients&lt;/li&gt;
&lt;li&gt;Building notification systems, live dashboards, or LLM streaming&lt;/li&gt;
&lt;li&gt;You want the simplest possible implementation&lt;/li&gt;
&lt;li&gt;You need automatic reconnection without extra code&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Use gRPC Streaming When:&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Service-to-service communication in a microservice architecture&lt;/li&gt;
&lt;li&gt;You need type safety across services (Protobuf contracts)&lt;/li&gt;
&lt;li&gt;Maximum throughput and minimum latency matter&lt;/li&gt;
&lt;li&gt;You&#39;re already using gRPC for unary calls&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Production Patterns&lt;/h2&gt;
&lt;h3&gt;Pattern 1: SSE + POST (The LLM Pattern)&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;Client sends prompt via POST
Server streams response via SSE
Client sends next prompt via POST
Server streams response via SSE
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is how every LLM API works. POST for the request, SSE for the streaming response. Simple, scalable, works through any proxy.&lt;/p&gt;
&lt;h3&gt;Pattern 2: WebSocket with Room-Based Routing&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Server-side room management
const rooms = new Map&amp;lt;string, Set&amp;lt;WebSocket&amp;gt;&amp;gt;();

function joinRoom(ws: WebSocket, room: string) {
  if (!rooms.has(room)) rooms.set(room, new Set());
  rooms.get(room)!.add(ws);
}

function broadcastToRoom(room: string, message: object) {
  const clients = rooms.get(room);
  if (!clients) return;
  const data = JSON.stringify(message);
  clients.forEach(ws =&amp;gt; {
    if (ws.readyState === WebSocket.OPEN) ws.send(data);
  });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Pattern 3: gRPC with Backpressure&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-go&quot;&gt;// Server respects client&#39;s ability to consume
func (s *Server) StreamData(
    req *pb.DataRequest,
    stream pb.DataService_StreamDataServer,
) error {
    for data := range s.dataChannel {
        // Send blocks if client buffer is full
        // This is automatic backpressure via HTTP/2 flow control
        if err := stream.Send(data); err != nil {
            return err // Client disconnected or can&#39;t keep up
        }
    }
    return nil
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Feature&lt;/th&gt;
&lt;th&gt;WebSocket&lt;/th&gt;
&lt;th&gt;SSE&lt;/th&gt;
&lt;th&gt;gRPC Streaming&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Direction&lt;/td&gt;
&lt;td&gt;Bidirectional&lt;/td&gt;
&lt;td&gt;Server → Client&lt;/td&gt;
&lt;td&gt;Both options&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Protocol&lt;/td&gt;
&lt;td&gt;WS (over HTTP)&lt;/td&gt;
&lt;td&gt;HTTP/1.1+&lt;/td&gt;
&lt;td&gt;HTTP/2&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Data format&lt;/td&gt;
&lt;td&gt;Text + Binary&lt;/td&gt;
&lt;td&gt;Text&lt;/td&gt;
&lt;td&gt;Binary (Protobuf)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Browser support&lt;/td&gt;
&lt;td&gt;✅ Native&lt;/td&gt;
&lt;td&gt;✅ Native&lt;/td&gt;
&lt;td&gt;⚠️ grpc-web proxy&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Auto-reconnect&lt;/td&gt;
&lt;td&gt;❌ Manual&lt;/td&gt;
&lt;td&gt;✅ Built-in&lt;/td&gt;
&lt;td&gt;⚠️ Library-dependent&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Load balancing&lt;/td&gt;
&lt;td&gt;⚠️ Sticky sessions&lt;/td&gt;
&lt;td&gt;✅ Standard HTTP&lt;/td&gt;
&lt;td&gt;✅ HTTP/2 LB&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Proxy friendly&lt;/td&gt;
&lt;td&gt;⚠️ Some issues&lt;/td&gt;
&lt;td&gt;✅ Works everywhere&lt;/td&gt;
&lt;td&gt;⚠️ Needs HTTP/2&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Best use case&lt;/td&gt;
&lt;td&gt;Real-time apps&lt;/td&gt;
&lt;td&gt;Notifications/LLM&lt;/td&gt;
&lt;td&gt;Microservices&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Choose SSE by default for server-to-client streaming. Reach for WebSockets when you need true bidirectional communication. Use gRPC streaming for service-to-service performance-critical paths.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Aspire for .NET Developers: The Missing Dashboard</title>
    <link href="https://techai-explained.netlify.app/articles/aspire-dotnet-missing-dashboard/"/>
    <updated>2026-03-14T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/aspire-dotnet-missing-dashboard/</id>
    <content xml:lang="en" type="html">&lt;p&gt;If you&#39;ve ever juggled Docker Compose files, environment variables, and connection strings just to run your .NET app locally, Aspire is the answer you didn&#39;t know you were waiting for. It gives you a single dashboard to orchestrate, monitor, and debug all your services — databases, caches, message brokers, and your own microservices — from one place.&lt;/p&gt;
&lt;p&gt;This guide walks through Aspire from zero to a fully orchestrated local development environment with live telemetry.&lt;/p&gt;
&lt;h2&gt;What Problem Does Aspire Solve?&lt;/h2&gt;
&lt;p&gt;Modern .NET applications are rarely a single project. A typical system looks like:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────┐
│         TYPICAL .NET MICROSERVICE APP           │
│                                                 │
│  ┌────────┐  ┌────────┐  ┌────────────────┐    │
│  │ Web API │  │ Worker │  │ Blazor Frontend│    │
│  └───┬────┘  └───┬────┘  └───────┬────────┘    │
│      │           │               │              │
│  ┌───┴───────────┴───────────────┴──────────┐   │
│  │          Shared Infrastructure           │   │
│  │  PostgreSQL · Redis · RabbitMQ · Azure   │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  WITHOUT ASPIRE:                                │
│  - 200-line docker-compose.yml                  │
│  - .env files for connection strings            │
│  - Manual health checking                       │
│  - No unified logging                           │
│  - &quot;Works on my machine&quot; syndrome               │
│                                                 │
│  WITH ASPIRE:                                   │
│  - 15 lines of C# in AppHost                    │
│  - Auto-injected connection strings             │
│  - Live dashboard with logs + traces            │
│  - One F5 to start everything                   │
└─────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h2&gt;Setting Up Your First Aspire App&lt;/h2&gt;
&lt;h3&gt;Step 1: Create the AppHost&lt;/h3&gt;
&lt;p&gt;The AppHost is Aspire&#39;s orchestrator. It defines what services your app needs and how they connect.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;dotnet new aspire-starter -n MyApp
cd MyApp
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This creates four projects:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;MyApp/
├── MyApp.AppHost/          # The orchestrator
├── MyApp.ServiceDefaults/  # Shared config (telemetry, health checks)
├── MyApp.ApiService/       # Your Web API
└── MyApp.Web/              # Blazor frontend
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Step 2: Define Your Resources&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;Program.cs&lt;/code&gt; in the AppHost is where the magic happens:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;var builder = DistributedApplication.CreateBuilder(args);

// Infrastructure
var postgres = builder.AddPostgres(&amp;quot;postgres&amp;quot;)
    .WithPgAdmin();

var redis = builder.AddRedis(&amp;quot;cache&amp;quot;);

var rabbitmq = builder.AddRabbitMQ(&amp;quot;messaging&amp;quot;)
    .WithManagementPlugin();

// Application database
var ordersDb = postgres.AddDatabase(&amp;quot;orders&amp;quot;);

// Services
var apiService = builder.AddProject&amp;lt;Projects.MyApp_ApiService&amp;gt;(&amp;quot;api&amp;quot;)
    .WithReference(ordersDb)
    .WithReference(redis)
    .WithReference(rabbitmq);

var workerService = builder.AddProject&amp;lt;Projects.MyApp_Worker&amp;gt;(&amp;quot;worker&amp;quot;)
    .WithReference(ordersDb)
    .WithReference(rabbitmq);

builder.AddProject&amp;lt;Projects.MyApp_Web&amp;gt;(&amp;quot;webfrontend&amp;quot;)
    .WithExternalHttpEndpoints()
    .WithReference(apiService)
    .WithReference(redis);

builder.Build().Run();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;That&#39;s it. &lt;strong&gt;15 lines of C#&lt;/strong&gt; replaces hundreds of lines of Docker Compose YAML, environment variable files, and startup scripts.&lt;/p&gt;
&lt;h3&gt;Step 3: Use the Resources in Your Services&lt;/h3&gt;
&lt;p&gt;In your API service, Aspire automatically injects connection strings:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// In MyApp.ApiService/Program.cs
var builder = WebApplication.CreateBuilder(args);

// These extension methods come from Aspire integrations
builder.AddNpgsqlDbContext&amp;lt;OrdersContext&amp;gt;(&amp;quot;orders&amp;quot;);
builder.AddRedisClient(&amp;quot;cache&amp;quot;);
builder.AddRabbitMQClient(&amp;quot;messaging&amp;quot;);

// Standard service configuration
builder.AddServiceDefaults();

var app = builder.Build();
app.MapDefaultEndpoints();

app.MapGet(&amp;quot;/orders&amp;quot;, async (OrdersContext db) =&amp;gt;
    await db.Orders.ToListAsync());

app.Run();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;No connection strings in &lt;code&gt;appsettings.json&lt;/code&gt;. No environment variables to manage. Aspire handles all service discovery and connection configuration automatically.&lt;/p&gt;
&lt;h2&gt;The Dashboard: Your New Best Friend&lt;/h2&gt;
&lt;p&gt;Press F5 (or &lt;code&gt;dotnet run --project MyApp.AppHost&lt;/code&gt;), and the Aspire dashboard opens in your browser.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────────┐
│                ASPIRE DASHBOARD                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  RESOURCES                        STATE             │
│  ─────────                        ─────             │
│  🟢 postgres (Container)          Running           │
│  🟢 pgadmin  (Container)          Running           │
│  🟢 cache    (Container)          Running           │
│  🟢 messaging (Container)         Running           │
│  🟢 api      (Project)            Running           │
│  🟢 worker   (Project)            Running           │
│  🟢 webfrontend (Project)         Running           │
│                                                     │
│  TABS: Resources │ Console │ Structured Logs │      │
│        Traces │ Metrics                             │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │ STRUCTURED LOGS (live stream)               │    │
│  │                                             │    │
│  │ 10:42:01 [api] GET /orders 200 12ms         │    │
│  │ 10:42:01 [api] Cache HIT orders:list        │    │
│  │ 10:42:02 [worker] Processing order #4521    │    │
│  │ 10:42:02 [worker] Published OrderCompleted  │    │
│  │ 10:42:03 [webfrontend] SignalR connected    │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h3&gt;What the Dashboard Shows&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Resources tab&lt;/strong&gt; — all containers and projects with status, endpoints, and environment variables&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Console logs&lt;/strong&gt; — stdout/stderr from every service, filterable and searchable&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Structured logs&lt;/strong&gt; — OpenTelemetry-powered structured logging across all services&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Traces&lt;/strong&gt; — distributed traces showing request flow across services&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Metrics&lt;/strong&gt; — real-time metrics for every service (request rate, error rate, latency)&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The traces view is particularly powerful. Click on any trace to see the full request path:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
DISTRIBUTED TRACE: GET /orders
&lt;p&gt;webfrontend ──────────────────────────────────────► 245ms total
│
├─ HTTP GET api/orders ──────────────────────► 42ms
│    │
│    ├─ Redis GET orders:list ───► 1ms (MISS)
│    │
│    ├─ PostgreSQL SELECT ────────► 18ms
│    │   query: SELECT * FROM orders WHERE...
│    │
│    └─ Redis SET orders:list ───► 2ms
│
└─ Render Blazor component ─────────────────► 12ms&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Adding Integrations&lt;/h2&gt;
&lt;p&gt;Aspire has integrations (NuGet packages) for dozens of services:&lt;/p&gt;
&lt;h3&gt;Database Integrations&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// AppHost
var sql = builder.AddSqlServer(&amp;quot;sql&amp;quot;).AddDatabase(&amp;quot;catalog&amp;quot;);
var mongo = builder.AddMongoDB(&amp;quot;mongo&amp;quot;).AddDatabase(&amp;quot;analytics&amp;quot;);
var cosmos = builder.AddAzureCosmosDB(&amp;quot;cosmos&amp;quot;).AddDatabase(&amp;quot;events&amp;quot;);

// Service
builder.AddSqlServerDbContext&amp;lt;CatalogContext&amp;gt;(&amp;quot;catalog&amp;quot;);
builder.AddMongoDBClient(&amp;quot;analytics&amp;quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Cloud Service Integrations&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Azure
var storage = builder.AddAzureStorage(&amp;quot;storage&amp;quot;);
var blobs = storage.AddBlobs(&amp;quot;blobs&amp;quot;);
var queues = storage.AddQueues(&amp;quot;queues&amp;quot;);
var servicebus = builder.AddAzureServiceBus(&amp;quot;sb&amp;quot;);

// AWS (community integration)
var sqs = builder.AddAWSSQSQueue(&amp;quot;orders-queue&amp;quot;);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Observability Integrations&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;ServiceDefaults&lt;/code&gt; project configures OpenTelemetry automatically:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// In ServiceDefaults/Extensions.cs
public static IHostApplicationBuilder AddServiceDefaults(
    this IHostApplicationBuilder builder)
{
    builder.ConfigureOpenTelemetry();
    builder.AddDefaultHealthChecks();
    builder.Services.AddServiceDiscovery();
    builder.Services.ConfigureHttpClientDefaults(http =&amp;gt;
    {
        http.AddStandardResilienceHandler();
        http.AddServiceDiscovery();
    });
    return builder;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This gives every service:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Distributed tracing with automatic span propagation&lt;/li&gt;
&lt;li&gt;Structured logging with correlation IDs&lt;/li&gt;
&lt;li&gt;Health check endpoints (&lt;code&gt;/health&lt;/code&gt;, &lt;code&gt;/alive&lt;/code&gt;)&lt;/li&gt;
&lt;li&gt;HTTP client resilience (retries, circuit breakers, timeouts)&lt;/li&gt;
&lt;li&gt;Service discovery (no hardcoded URLs)&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Custom Resources&lt;/h2&gt;
&lt;p&gt;Need something Aspire doesn&#39;t have a built-in integration for? Create a custom resource:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;// Add any container as a resource
var jaeger = builder.AddContainer(&amp;quot;jaeger&amp;quot;, &amp;quot;jaegertracing/all-in-one&amp;quot;)
    .WithHttpEndpoint(port: 16686, targetPort: 16686, name: &amp;quot;ui&amp;quot;)
    .WithEndpoint(port: 4317, targetPort: 4317, name: &amp;quot;otlp&amp;quot;);

// Add an executable
var mockServer = builder.AddExecutable(&amp;quot;mock-api&amp;quot;, &amp;quot;node&amp;quot;,
    workingDirectory: &amp;quot;../mock-server&amp;quot;,
    &amp;quot;index.js&amp;quot;)
    .WithHttpEndpoint(port: 3001);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Deployment: From Local to Cloud&lt;/h2&gt;
&lt;p&gt;Aspire isn&#39;t just for local development. The same AppHost model deploys to Azure:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# Install the Azure Developer CLI
az extension add --name containerapp

# Deploy to Azure Container Apps
azd init
azd up
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Aspire generates the infrastructure automatically:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Each project becomes a Container App&lt;/li&gt;
&lt;li&gt;PostgreSQL becomes Azure Database for PostgreSQL&lt;/li&gt;
&lt;li&gt;Redis becomes Azure Cache for Redis&lt;/li&gt;
&lt;li&gt;RabbitMQ becomes Azure Service Bus&lt;/li&gt;
&lt;li&gt;Connection strings are injected via managed identity&lt;/li&gt;
&lt;/ul&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
LOCAL (Aspire)              AZURE (azd up)
──────────────              ──────────────
postgres container    ───►  Azure Database for PostgreSQL
redis container       ───►  Azure Cache for Redis
rabbitmq container    ───►  Azure Service Bus
api project           ───►  Container App (api)
worker project        ───►  Container App (worker)
webfrontend project   ───►  Container App (webfrontend)
&lt;p&gt;Same code. Same AppHost. Different environment.&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Testing with Aspire&lt;/h2&gt;
&lt;p&gt;Aspire includes a testing framework for integration tests:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;[Fact]
public async Task GetOrders_ReturnsOk()
{
    // Arrange - spin up the full distributed app
    var appHost = await DistributedApplicationTestingBuilder
        .CreateAsync&amp;lt;Projects.MyApp_AppHost&amp;gt;();

    await using var app = await appHost.BuildAsync();
    await app.StartAsync();

    // Act
    var httpClient = app.CreateHttpClient(&amp;quot;api&amp;quot;);
    var response = await httpClient.GetAsync(&amp;quot;/orders&amp;quot;);

    // Assert
    response.EnsureSuccessStatusCode();
    var orders = await response.Content
        .ReadFromJsonAsync&amp;lt;List&amp;lt;Order&amp;gt;&amp;gt;();
    Assert.NotNull(orders);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This spins up the &lt;strong&gt;entire distributed application&lt;/strong&gt; — all containers, all projects — runs your test, and tears everything down. Integration tests that actually test integration.&lt;/p&gt;
&lt;h2&gt;Common Patterns&lt;/h2&gt;
&lt;h3&gt;Health-Check-Driven Startup&lt;/h3&gt;
&lt;p&gt;Aspire can wait for dependencies before starting services:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;var apiService = builder.AddProject&amp;lt;Projects.MyApp_ApiService&amp;gt;(&amp;quot;api&amp;quot;)
    .WithReference(ordersDb)
    .WaitFor(ordersDb)     // Don&#39;t start API until DB is healthy
    .WaitFor(redis);       // Don&#39;t start API until cache is ready
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Environment-Specific Configuration&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;if (builder.Environment.IsDevelopment())
{
    var postgres = builder.AddPostgres(&amp;quot;postgres&amp;quot;)
        .WithPgAdmin();  // Only add PgAdmin in dev
}
else
{
    var postgres = builder.AddAzurePostgresFlexibleServer(&amp;quot;postgres&amp;quot;);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Shared Projects Across Services&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-csharp&quot;&gt;var sharedLib = builder.AddProject&amp;lt;Projects.MyApp_Shared&amp;gt;(&amp;quot;shared&amp;quot;);

var api = builder.AddProject&amp;lt;Projects.MyApp_ApiService&amp;gt;(&amp;quot;api&amp;quot;)
    .WithReference(sharedLib);

var worker = builder.AddProject&amp;lt;Projects.MyApp_Worker&amp;gt;(&amp;quot;worker&amp;quot;)
    .WithReference(sharedLib);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Aspire vs. Docker Compose&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Feature&lt;/th&gt;
&lt;th&gt;Docker Compose&lt;/th&gt;
&lt;th&gt;Aspire&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Language&lt;/td&gt;
&lt;td&gt;YAML&lt;/td&gt;
&lt;td&gt;C#&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Service discovery&lt;/td&gt;
&lt;td&gt;Manual DNS/env vars&lt;/td&gt;
&lt;td&gt;Automatic&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Connection strings&lt;/td&gt;
&lt;td&gt;Manual env vars&lt;/td&gt;
&lt;td&gt;Auto-injected&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Health checks&lt;/td&gt;
&lt;td&gt;Manual configuration&lt;/td&gt;
&lt;td&gt;Built-in&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Observability&lt;/td&gt;
&lt;td&gt;Add Jaeger/Prometheus manually&lt;/td&gt;
&lt;td&gt;Built-in dashboard&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Debugging&lt;/td&gt;
&lt;td&gt;Attach debugger manually&lt;/td&gt;
&lt;td&gt;F5 debugging&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Cloud deployment&lt;/td&gt;
&lt;td&gt;Write Terraform/Bicep separately&lt;/td&gt;
&lt;td&gt;&lt;code&gt;azd up&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;IDE integration&lt;/td&gt;
&lt;td&gt;None&lt;/td&gt;
&lt;td&gt;Full IntelliSense&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;When NOT to Use Aspire&lt;/h2&gt;
&lt;p&gt;Aspire is excellent for .NET-centric applications, but it&#39;s not for everything:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Polyglot teams&lt;/strong&gt; — if your services are Python, Go, and Java, Aspire adds minimal value&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Existing Docker Compose setups&lt;/strong&gt; — if your compose file works and your team knows it, migration has a cost&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Non-Azure deployments&lt;/strong&gt; — while Aspire works locally for any stack, cloud deployment is Azure-first&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Simple single-project apps&lt;/strong&gt; — Aspire adds orchestration overhead that a single API doesn&#39;t need&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Getting Started Today&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# Install the Aspire workload
dotnet workload install aspire

# Create a new Aspire app
dotnet new aspire-starter -n MyApp

# Run it
cd MyApp
dotnet run --project MyApp.AppHost
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The dashboard opens automatically. You&#39;ll see your services starting, logs streaming, and traces appearing — all in under a minute.&lt;/p&gt;
&lt;p&gt;Aspire is the developer experience .NET always deserved. It takes the pain out of distributed development and replaces it with a dashboard that makes you feel like you&#39;re actually in control.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Reactive Programming in 2026: What Changed</title>
    <link href="https://techai-explained.netlify.app/articles/reactive-programming-2026/"/>
    <updated>2026-03-15T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/reactive-programming-2026/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Reactive programming in 2016 meant wrapping everything in Observables and writing chains of &lt;code&gt;.pipe()&lt;/code&gt;, &lt;code&gt;.map()&lt;/code&gt;, and &lt;code&gt;.switchMap()&lt;/code&gt;. In 2026, the landscape has fundamentally shifted. Signals have replaced Observables for UI state, structured concurrency has solved the cancellation problem, and the &amp;quot;reactive&amp;quot; label now covers a much broader set of patterns.&lt;/p&gt;
&lt;p&gt;This article maps out what changed, what survived, and what modern reactive code actually looks like.&lt;/p&gt;
&lt;h2&gt;The 2016 Reactive Stack vs. 2026&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
2016 REACTIVE                    2026 REACTIVE
─────────────                    ─────────────
RxJS / RxJava / Reactor          Signals (Angular, Solid, Vue)
Observable.pipe(...)             effect() / computed()
Subjects everywhere              Stores / State machines
Manual subscription mgmt         Auto-disposal / ownership
.subscribe() memory leaks        Compiler-managed lifecycles
Marble diagrams                  Dependency graphs
Backpressure (manual)            Structured concurrency
&lt;p&gt;SURVIVED:&lt;br /&gt;
Event streams (WebSockets, SSE)&lt;br /&gt;
Data pipeline transformations&lt;br /&gt;
Complex async coordination&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;What Died: The Observable-Everything Era&lt;/h2&gt;
&lt;p&gt;From roughly 2015-2022, the dominant pattern was: &amp;quot;make everything an Observable.&amp;quot; User clicks? Observable. HTTP requests? Observable. Form values? Observable. Router changes? Observable.&lt;/p&gt;
&lt;p&gt;This led to code like:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// 2016-era Angular component (the old way)
this.searchResults$ = this.searchInput.valueChanges.pipe(
  debounceTime(300),
  distinctUntilChanged(),
  switchMap(term =&amp;gt; this.searchService.search(term)),
  catchError(err =&amp;gt; {
    console.error(err);
    return of([]);
  }),
  shareReplay(1)
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;The problems became clear at scale:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Subscription management&lt;/strong&gt; — every &lt;code&gt;.subscribe()&lt;/code&gt; without &lt;code&gt;.unsubscribe()&lt;/code&gt; was a memory leak&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Debugging&lt;/strong&gt; — stack traces through RxJS operators were unreadable&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Learning curve&lt;/strong&gt; — developers needed to learn 100+ operators to be productive&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Overuse&lt;/strong&gt; — simple state was wrapped in unnecessary reactive complexity&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;What Won: Signals&lt;/h2&gt;
&lt;p&gt;Signals are synchronous, reactive primitives that track dependencies automatically. Angular, Solid, Vue, Svelte (runes), and Preact all adopted signal-based reactivity.&lt;/p&gt;
&lt;h3&gt;How Signals Work&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// A signal holds a value and tracks who reads it
const count = signal(0);

// A computed signal derives from other signals
const doubled = computed(() =&amp;gt; count() * 2);

// An effect runs when its dependencies change
effect(() =&amp;gt; {
  console.log(`Count is ${count()}, doubled is ${doubled()}`);
});

// Updating the signal triggers the effect
count.set(1);  // Logs: &amp;quot;Count is 1, doubled is 2&amp;quot;
count.set(5);  // Logs: &amp;quot;Count is 5, doubled is 10&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
SIGNAL DEPENDENCY GRAPH
&lt;p&gt;┌──────────┐     ┌──────────────┐     ┌──────────────┐
│  count   │────►│   doubled    │────►│   effect()   │
│ signal(0)│     │ computed(    │     │ console.log  │
│          │     │  count * 2)  │     │              │
└──────────┘     └──────────────┘     └──────────────┘
│                                      ▲
└──────────────────────────────────────┘&lt;/p&gt;
&lt;p&gt;When count changes:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;count notifies its dependents&lt;/li&gt;
&lt;li&gt;doubled recalculates&lt;/li&gt;
&lt;li&gt;effect re-runs
All synchronous. No subscriptions. No leaks.&lt;/li&gt;
&lt;/ol&gt;
&lt;/div&gt;
&lt;h3&gt;Why Signals Beat Observables for UI State&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Factor&lt;/th&gt;
&lt;th&gt;Observables&lt;/th&gt;
&lt;th&gt;Signals&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Reading values&lt;/td&gt;
&lt;td&gt;&lt;code&gt;.subscribe()&lt;/code&gt; or &lt;code&gt;async&lt;/code&gt; pipe&lt;/td&gt;
&lt;td&gt;Direct function call: &lt;code&gt;count()&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Memory management&lt;/td&gt;
&lt;td&gt;Manual unsubscribe&lt;/td&gt;
&lt;td&gt;Automatic (ownership-based)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Glitch-free updates&lt;/td&gt;
&lt;td&gt;Requires &lt;code&gt;shareReplay&lt;/code&gt; + care&lt;/td&gt;
&lt;td&gt;Built-in (topological sort)&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Debugging&lt;/td&gt;
&lt;td&gt;Opaque operator chains&lt;/td&gt;
&lt;td&gt;Clear dependency graphs&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Learning curve&lt;/td&gt;
&lt;td&gt;100+ operators&lt;/td&gt;
&lt;td&gt;3 primitives: signal, computed, effect&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Synchronous reads&lt;/td&gt;
&lt;td&gt;Not easily&lt;/td&gt;
&lt;td&gt;Yes, always&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;What Survived: Event Streams&lt;/h2&gt;
&lt;p&gt;Observables didn&#39;t die — they just found their correct niche. RxJS is still the right tool for:&lt;/p&gt;
&lt;h3&gt;1. WebSocket / SSE Streams&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Real-time data stream — still perfect for Observables
const priceStream$ = webSocket&amp;lt;StockPrice&amp;gt;(&#39;wss://prices.example.com&#39;)
  .pipe(
    filter(p =&amp;gt; p.symbol === &#39;AAPL&#39;),
    bufferTime(1000),
    map(prices =&amp;gt; prices[prices.length - 1]),
    retry({ count: 3, delay: 2000 })
  );
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2. Complex Async Coordination&lt;/h3&gt;
&lt;p&gt;When you need to coordinate multiple async operations with timing, Observables remain unmatched:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Typeahead search with debounce, cancellation, and caching
const search$ = fromEvent(input, &#39;input&#39;).pipe(
  map(e =&amp;gt; (e.target as HTMLInputElement).value),
  debounceTime(250),
  distinctUntilChanged(),
  switchMap(term =&amp;gt;
    term.length &amp;lt; 2
      ? of([])
      : fromFetch(`/api/search?q=${term}`).pipe(
          switchMap(r =&amp;gt; r.json()),
          catchError(() =&amp;gt; of([]))
        )
  )
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3. Data Pipelines&lt;/h3&gt;
&lt;p&gt;Server-side data processing pipelines — ETL, log processing, event sourcing — still benefit from reactive streams:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Node.js data pipeline
source$.pipe(
  bufferCount(100),
  concatMap(batch =&amp;gt; processInDatabase(batch)),
  scan((total, result) =&amp;gt; total + result.count, 0),
  tap(total =&amp;gt; metrics.gauge(&#39;processed_total&#39;, total))
);
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;The New Pattern: Structured Concurrency&lt;/h2&gt;
&lt;p&gt;The biggest shift in 2026 isn&#39;t about reactive streams — it&#39;s about &lt;strong&gt;structured concurrency&lt;/strong&gt;. This pattern ensures that async operations are always scoped, cancellable, and don&#39;t leak.&lt;/p&gt;
&lt;h3&gt;The Problem It Solves&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// BAD: Fire-and-forget async — who cancels this?
async function loadDashboard() {
  const users = fetchUsers();       // Running...
  const metrics = fetchMetrics();   // Running...
  const alerts = fetchAlerts();     // Running...
  
  // If the component unmounts, these keep running!
  // If one fails, the others keep running!
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Structured Concurrency Solution&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// GOOD: Structured concurrency — everything is scoped
async function loadDashboard(scope: Scope) {
  // All tasks are children of this scope
  // If scope is cancelled, all tasks are cancelled
  // If any task fails, sibling tasks are cancelled
  
  const [users, metrics, alerts] = await scope.all([
    fetchUsers,
    fetchMetrics,
    fetchAlerts,
  ]);
  
  return { users, metrics, alerts };
}
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
STRUCTURED CONCURRENCY TREE
&lt;p&gt;Component Lifecycle
│
┌──────┴──────┐
│  Dashboard  │
│   Scope     │
└──────┬──────┘
│
┌───────┼───────┐
▼       ▼       ▼
fetchUsers fetchMetrics fetchAlerts&lt;/p&gt;
&lt;p&gt;GUARANTEE: When Dashboard scope ends,
ALL child tasks are cancelled automatically.
No leaks. No orphaned promises.&lt;/p&gt;
&lt;/div&gt;
&lt;h3&gt;Real-World: AbortController + AsyncContext&lt;/h3&gt;
&lt;p&gt;JavaScript&#39;s native approach uses &lt;code&gt;AbortController&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;class DashboardController {
  private controller = new AbortController();
  
  async load() {
    const { signal } = this.controller;
    
    try {
      const [users, metrics] = await Promise.all([
        fetch(&#39;/api/users&#39;, { signal }).then(r =&amp;gt; r.json()),
        fetch(&#39;/api/metrics&#39;, { signal }).then(r =&amp;gt; r.json()),
      ]);
      
      this.render(users, metrics);
    } catch (e) {
      if (e instanceof DOMException &amp;amp;&amp;amp; e.name === &#39;AbortError&#39;) {
        // Clean cancellation — expected
        return;
      }
      throw e;
    }
  }
  
  destroy() {
    this.controller.abort(); // Cancels ALL in-flight requests
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;State Machines: The Reactive Architecture&lt;/h2&gt;
&lt;p&gt;Another 2026 pattern is using &lt;strong&gt;state machines&lt;/strong&gt; instead of ad-hoc reactive state:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;import { createMachine, assign } from &#39;xstate&#39;;

const fetchMachine = createMachine({
  id: &#39;fetch&#39;,
  initial: &#39;idle&#39;,
  context: { data: null, error: null, retries: 0 },
  states: {
    idle: {
      on: { FETCH: &#39;loading&#39; }
    },
    loading: {
      invoke: {
        src: &#39;fetchData&#39;,
        onDone: {
          target: &#39;success&#39;,
          actions: assign({ data: (_, event) =&amp;gt; event.data })
        },
        onError: [
          {
            target: &#39;loading&#39;,
            guard: (ctx) =&amp;gt; ctx.retries &amp;lt; 3,
            actions: assign({ retries: (ctx) =&amp;gt; ctx.retries + 1 })
          },
          {
            target: &#39;failure&#39;,
            actions: assign({ error: (_, event) =&amp;gt; event.data })
          }
        ]
      }
    },
    success: { type: &#39;final&#39; },
    failure: {
      on: { RETRY: { target: &#39;loading&#39;, actions: assign({ retries: 0 }) } }
    }
  }
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;State machines make impossible states impossible. You can&#39;t be &amp;quot;loading&amp;quot; and &amp;quot;error&amp;quot; at the same time. You can&#39;t &amp;quot;retry&amp;quot; from the &amp;quot;idle&amp;quot; state.&lt;/p&gt;
&lt;h2&gt;The Modern Reactive Stack (2026)&lt;/h2&gt;
&lt;p&gt;Here&#39;s what a well-architected reactive system looks like today:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────┐
│                  UI LAYER                       │
│  Signals for component state                    │
│  signal(), computed(), effect()                 │
├─────────────────────────────────────────────────┤
│              STATE MANAGEMENT                   │
│  State machines for complex flows               │
│  Stores for shared state                        │
├─────────────────────────────────────────────────┤
│              DATA LAYER                         │
│  Structured concurrency for async ops           │
│  AbortController for cancellation               │
├─────────────────────────────────────────────────┤
│            EVENT STREAMS                        │
│  RxJS / Observables for WebSockets, SSE         │
│  Backpressure for data pipelines                │
├─────────────────────────────────────────────────┤
│              INFRASTRUCTURE                     │
│  Server-sent events, message queues             │
│  Reactive databases (Supabase, Convex)          │
└─────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h2&gt;Migration Guide: From RxJS-Everything to Modern Reactive&lt;/h2&gt;
&lt;h3&gt;Step 1: Replace BehaviorSubjects with Signals&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// BEFORE
private count$ = new BehaviorSubject&amp;lt;number&amp;gt;(0);
get count(): Observable&amp;lt;number&amp;gt; { return this.count$.asObservable(); }
increment() { this.count$.next(this.count$.value + 1); }

// AFTER
count = signal(0);
increment() { this.count.update(n =&amp;gt; n + 1); }
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Step 2: Replace Computed Observables with Computed Signals&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// BEFORE
total$ = combineLatest([this.price$, this.quantity$]).pipe(
  map(([price, qty]) =&amp;gt; price * qty),
  shareReplay(1)
);

// AFTER
total = computed(() =&amp;gt; this.price() * this.quantity());
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Step 3: Keep RxJS for Event Streams&lt;/h3&gt;
&lt;p&gt;Don&#39;t replace WebSocket handling or complex async coordination. Those are RxJS&#39;s strength.&lt;/p&gt;
&lt;h3&gt;Step 4: Add Structured Concurrency for Async Operations&lt;/h3&gt;
&lt;p&gt;Replace &lt;code&gt;Promise.all&lt;/code&gt; with scoped operations that support cancellation.&lt;/p&gt;
&lt;h2&gt;What&#39;s Coming Next&lt;/h2&gt;
&lt;p&gt;The reactive space continues to evolve:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;TC39 Signals proposal&lt;/strong&gt; — a native JavaScript signals API, currently Stage 1&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;AsyncContext&lt;/strong&gt; — propagating cancellation and context through async boundaries&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Observable as a web standard&lt;/strong&gt; — the DOM Observable proposal brings basic reactive primitives to the browser&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Reactive databases&lt;/strong&gt; — databases that push changes to clients (Supabase Realtime, Convex, Electric SQL)&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The core insight of reactive programming — &lt;strong&gt;declaring relationships between data rather than manually updating state&lt;/strong&gt; — is more relevant than ever. The tools have just gotten better at making it practical.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Kubernetes Autoscaling: A Visual Guide</title>
    <link href="https://techai-explained.netlify.app/articles/kubernetes-autoscaling-visual-guide/"/>
    <updated>2026-03-16T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/kubernetes-autoscaling-visual-guide/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Kubernetes autoscaling sounds simple: &amp;quot;add more pods when traffic goes up.&amp;quot; In reality, there are four different autoscaling mechanisms, each solving a different problem. Picking the wrong one — or misconfiguring the right one — leads to either wasted money or dropped requests.&lt;/p&gt;
&lt;p&gt;This visual guide covers all four autoscalers, when to use each, and the configuration patterns that work in production.&lt;/p&gt;
&lt;h2&gt;The Autoscaling Landscape&lt;/h2&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────────┐
│            KUBERNETES AUTOSCALING STACK              │
│                                                     │
│  ┌─────────────────────────────────────────────┐    │
│  │         CLUSTER AUTOSCALER                  │    │
│  │     Adds/removes NODES from the cluster     │    │
│  └─────────────────────┬───────────────────────┘    │
│                        │                            │
│  ┌─────────────────────┼───────────────────────┐    │
│  │      NODE           │          NODE          │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐   │    │
│  │  │   HPA    │  │   VPA    │  │   KEDA   │   │    │
│  │  │ Scales   │  │ Resizes  │  │ Event-   │   │    │
│  │  │ pod      │  │ pod      │  │ driven   │   │    │
│  │  │ COUNT    │  │ RESOURCES│  │ scaling  │   │    │
│  │  └──────────┘  └──────────┘  └──────────┘   │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;HPA&lt;/strong&gt; (Horizontal Pod Autoscaler): scales the &lt;strong&gt;number&lt;/strong&gt; of pods&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;VPA&lt;/strong&gt; (Vertical Pod Autoscaler): scales the &lt;strong&gt;resources&lt;/strong&gt; of each pod&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cluster Autoscaler&lt;/strong&gt;: scales the &lt;strong&gt;number of nodes&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;KEDA&lt;/strong&gt;: event-driven autoscaling based on &lt;strong&gt;external metrics&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;1. Horizontal Pod Autoscaler (HPA)&lt;/h2&gt;
&lt;p&gt;The HPA watches a metric (CPU, memory, or custom metrics) and adjusts the replica count of a Deployment or StatefulSet.&lt;/p&gt;
&lt;h3&gt;How It Works&lt;/h3&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
                    Metrics Server
                         │
                    ┌────┴────┐
                    │ Current │
                    │ CPU: 80%│
                    │ Target: │
                    │ 50%     │
                    └────┬────┘
                         │
                    ┌────┴────┐
                    │   HPA   │
                    │ Formula:│
                    │ desired │
                    │= ceil(  │
                    │ current │
                    │/ target │
                    │* replicas│
                    │)        │
                    └────┬────┘
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
       ┌────────┐  ┌────────┐  ┌────────┐
       │ Pod 1  │  │ Pod 2  │  │ Pod 3  │
       │(exists)│  │(exists)│  │ (NEW)  │
       └────────┘  └────────┘  └────────┘
&lt;/div&gt;
&lt;p&gt;The scaling formula: &lt;code&gt;desiredReplicas = ceil(currentMetricValue / targetMetricValue × currentReplicas)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;If you have 2 pods at 80% CPU with a target of 50%, the HPA calculates: &lt;code&gt;ceil(80/50 × 2) = ceil(3.2) = 4 pods&lt;/code&gt;.&lt;/p&gt;
&lt;h3&gt;Basic HPA Configuration&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web-app-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web-app
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 25
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Percent
          value: 100
          periodSeconds: 30
        - type: Pods
          value: 4
          periodSeconds: 60
      selectPolicy: Max
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Critical: The Behavior Section&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;behavior&lt;/code&gt; section controls &lt;strong&gt;how fast&lt;/strong&gt; scaling happens. Without it, the HPA uses defaults that can cause oscillation.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Scale-up strategy:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;React quickly — 30-second stabilization window&lt;/li&gt;
&lt;li&gt;Allow doubling (100% increase) or adding 4 pods, whichever is more&lt;/li&gt;
&lt;li&gt;Use &lt;code&gt;selectPolicy: Max&lt;/code&gt; to pick the more aggressive option&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Scale-down strategy:&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;React slowly — 300-second (5 minute) stabilization window&lt;/li&gt;
&lt;li&gt;Remove at most 25% of pods per minute&lt;/li&gt;
&lt;li&gt;This prevents thrashing during traffic fluctuations&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;HPA with Custom Metrics&lt;/h3&gt;
&lt;p&gt;CPU and memory aren&#39;t always the right signals. For a web API, requests-per-second is more meaningful:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;metrics:
  - type: Pods
    pods:
      metric:
        name: http_requests_per_second
      target:
        type: AverageValue
        averageValue: 1000
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This scales based on actual traffic rather than resource utilization.&lt;/p&gt;
&lt;h2&gt;2. Vertical Pod Autoscaler (VPA)&lt;/h2&gt;
&lt;p&gt;While HPA adds more pods, VPA makes existing pods bigger (or smaller). It adjusts CPU and memory requests and limits.&lt;/p&gt;
&lt;h3&gt;When VPA Wins Over HPA&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Stateful workloads&lt;/strong&gt; — databases, caches, queues that can&#39;t easily be horizontally scaled&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Batch jobs&lt;/strong&gt; — jobs that need varying resources based on input size&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Right-sizing&lt;/strong&gt; — finding the correct resource requests for your workloads&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: database-vpa
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: postgres
  updatePolicy:
    updateMode: &amp;quot;Auto&amp;quot;  # or &amp;quot;Off&amp;quot; for recommendation-only
  resourcePolicy:
    containerPolicies:
      - containerName: postgres
        minAllowed:
          cpu: 250m
          memory: 512Mi
        maxAllowed:
          cpu: 4
          memory: 8Gi
        controlledResources: [&amp;quot;cpu&amp;quot;, &amp;quot;memory&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Warning&lt;/strong&gt;: VPA and HPA should &lt;strong&gt;not&lt;/strong&gt; both target CPU/memory on the same workload. They&#39;ll fight each other. If you need both, use HPA with custom metrics and VPA for resource right-sizing.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3&gt;VPA Modes&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Mode&lt;/th&gt;
&lt;th&gt;Behavior&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;Off&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Only provides recommendations; doesn&#39;t change anything&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;Initial&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Sets resources only when pods are first created&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;Auto&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Evicts and recreates pods with updated resources&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;strong&gt;Best practice&lt;/strong&gt;: Start with &lt;code&gt;Off&lt;/code&gt; to see recommendations, then move to &lt;code&gt;Auto&lt;/code&gt; once you trust the suggestions.&lt;/p&gt;
&lt;h2&gt;3. Cluster Autoscaler&lt;/h2&gt;
&lt;p&gt;HPA and VPA scale pods. But what if there aren&#39;t enough nodes to schedule those pods? The Cluster Autoscaler handles this.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌────────────────────────────────────────────────┐
│               CLUSTER AUTOSCALER               │
│                                                │
│  SCALE UP trigger:                             │
│  Pod is &quot;Pending&quot; because no node has enough   │
│  resources to schedule it.                     │
│                                                │
│  ┌──────┐ ┌──────┐ ┌──────┐                   │
│  │Node 1│ │Node 2│ │Node 3│  ← All full       │
│  │[████]│ │[████]│ │[████]│                    │
│  └──────┘ └──────┘ └──────┘                    │
│                              ┌──────┐          │
│  Pending pod ──────────────► │Node 4│ NEW!     │
│                              │[█   ]│          │
│                              └──────┘          │
│                                                │
│  SCALE DOWN trigger:                           │
│  Node utilization &lt; 50% for 10+ minutes AND    │
│  all pods can be rescheduled on other nodes.   │
└────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h3&gt;Cloud Provider Integration&lt;/h3&gt;
&lt;p&gt;The Cluster Autoscaler works with cloud provider node pools:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# AKS example: node pool with autoscaling
az aks nodepool add &#92;
  --resource-group myRG &#92;
  --cluster-name myCluster &#92;
  --name workerpool &#92;
  --node-count 3 &#92;
  --min-count 2 &#92;
  --max-count 20 &#92;
  --enable-cluster-autoscaler &#92;
  --node-vm-size Standard_D4s_v3
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Key Configuration Options&lt;/h3&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;# Cluster Autoscaler ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-autoscaler-config
data:
  # Wait 10 minutes before removing underutilized nodes
  scale-down-unneeded-time: &amp;quot;10m&amp;quot;
  
  # Node must be below 50% utilization to be considered
  scale-down-utilization-threshold: &amp;quot;0.5&amp;quot;
  
  # Don&#39;t scale down if it would remove the last node
  skip-nodes-with-system-pods: &amp;quot;true&amp;quot;
  
  # Maximum time to wait for a node to become ready
  max-node-provision-time: &amp;quot;15m&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;4. KEDA: Event-Driven Autoscaling&lt;/h2&gt;
&lt;p&gt;KEDA (Kubernetes Event-Driven Autoscaling) is the most flexible option. It scales based on external event sources — message queues, databases, cron schedules, and 60+ other scalers.&lt;/p&gt;
&lt;h3&gt;When KEDA Shines&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Queue-based workloads&lt;/strong&gt;: scale workers based on queue depth&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Event processing&lt;/strong&gt;: scale consumers based on event lag&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scheduled scaling&lt;/strong&gt;: scale up before known traffic spikes&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Scale to zero&lt;/strong&gt;: shut down completely when there&#39;s no work&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: order-processor
spec:
  scaleTargetRef:
    name: order-processor
  minReplicaCount: 0     # Scale to zero!
  maxReplicaCount: 50
  cooldownPeriod: 60
  triggers:
    - type: azure-servicebus
      metadata:
        queueName: orders
        messageCount: &amp;quot;5&amp;quot;  # 1 pod per 5 messages
        connectionFromEnv: SB_CONNECTION
    - type: cron
      metadata:
        timezone: America/Los_Angeles
        start: &amp;quot;0 8 * * 1-5&amp;quot;   # 8 AM weekdays
        end: &amp;quot;0 20 * * 1-5&amp;quot;    # 8 PM weekdays
        desiredReplicas: &amp;quot;3&amp;quot;   # Pre-warm during business hours
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;KEDA Architecture&lt;/h3&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌──────────────┐     ┌──────────────┐
│  External    │     │    KEDA      │
│  Source       │────►│   Metrics    │
│  (Queue,     │     │   Adapter    │
│   DB, etc.)  │     └──────┬───────┘
└──────────────┘            │
                            ▼
                    ┌──────────────┐
                    │     HPA      │
                    │  (created by │
                    │   KEDA)      │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
         ┌────────┐  ┌────────┐  ┌────────┐
         │Worker 1│  │Worker 2│  │Worker 3│
         └────────┘  └────────┘  └────────┘
&lt;/div&gt;
&lt;p&gt;KEDA creates and manages an HPA under the hood, feeding it with metrics from external sources.&lt;/p&gt;
&lt;h2&gt;Decision Framework&lt;/h2&gt;
&lt;p&gt;Use this flowchart to pick the right autoscaler:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
Is your workload stateless?
├── YES: Can it be horizontally scaled?
│   ├── YES: Does it respond to external events?
│   │   ├── YES ──────────► KEDA
│   │   └── NO: Is CPU/memory the right metric?
│   │       ├── YES ──────► HPA (resource metrics)
│   │       └── NO ───────► HPA (custom metrics)
│   └── NO ───────────────► VPA
└── NO ───────────────────► VPA + Manual planning
&lt;p&gt;Always pair with Cluster Autoscaler for node-level scaling.&lt;/p&gt;
&lt;/div&gt;
&lt;h2&gt;Production Checklist&lt;/h2&gt;
&lt;p&gt;Before going live with autoscaling, verify these items:&lt;/p&gt;
&lt;h3&gt;Resource Requests and Limits&lt;/h3&gt;
&lt;p&gt;Autoscaling only works if your pods have proper resource requests:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;resources:
  requests:
    cpu: 250m       # Used by HPA for calculations
    memory: 256Mi   # Used by VPA for recommendations
  limits:
    cpu: 1000m      # Hard ceiling
    memory: 512Mi   # OOM-killed if exceeded
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Without requests, HPA has nothing to measure.&lt;/strong&gt;&lt;/p&gt;
&lt;h3&gt;Pod Disruption Budgets&lt;/h3&gt;
&lt;p&gt;Prevent autoscaling from killing too many pods at once:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web-app-pdb
spec:
  minAvailable: &amp;quot;75%&amp;quot;
  selector:
    matchLabels:
      app: web-app
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Readiness Probes&lt;/h3&gt;
&lt;p&gt;New pods must only receive traffic when they&#39;re actually ready:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-yaml&quot;&gt;readinessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5
  failureThreshold: 3
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Monitoring Dashboards&lt;/h3&gt;
&lt;p&gt;Track these metrics:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Replica count over time&lt;/strong&gt; — are you scaling smoothly or oscillating?&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pod startup time&lt;/strong&gt; — slow starts mean scaling can&#39;t keep up with traffic spikes&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pending pods&lt;/strong&gt; — if pods are pending, the Cluster Autoscaler might be too slow&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cost per request&lt;/strong&gt; — autoscaling should decrease this number, not increase it&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Common Mistakes&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Setting minReplicas to 1&lt;/strong&gt; — no high availability during scale-up delays&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No stabilization window&lt;/strong&gt; — causes rapid scaling up and down (thrashing)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Ignoring pod startup time&lt;/strong&gt; — if pods take 60s to start, your scale-up is always 60s too late&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;VPA + HPA on same CPU metric&lt;/strong&gt; — they conflict and create feedback loops&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Not setting maxReplicas&lt;/strong&gt; — a traffic spike could autoscale you into a $50K cloud bill&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Summary&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Autoscaler&lt;/th&gt;
&lt;th&gt;Scales&lt;/th&gt;
&lt;th&gt;Based On&lt;/th&gt;
&lt;th&gt;Key Config&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;HPA&lt;/td&gt;
&lt;td&gt;Pod count&lt;/td&gt;
&lt;td&gt;CPU, memory, custom metrics&lt;/td&gt;
&lt;td&gt;&lt;code&gt;behavior&lt;/code&gt;, &lt;code&gt;minReplicas&lt;/code&gt;, &lt;code&gt;maxReplicas&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;VPA&lt;/td&gt;
&lt;td&gt;Pod resources&lt;/td&gt;
&lt;td&gt;Historical usage&lt;/td&gt;
&lt;td&gt;&lt;code&gt;updateMode&lt;/code&gt;, &lt;code&gt;minAllowed&lt;/code&gt;, &lt;code&gt;maxAllowed&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Cluster Autoscaler&lt;/td&gt;
&lt;td&gt;Node count&lt;/td&gt;
&lt;td&gt;Pending pods&lt;/td&gt;
&lt;td&gt;&lt;code&gt;min-count&lt;/code&gt;, &lt;code&gt;max-count&lt;/code&gt;, provider config&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;KEDA&lt;/td&gt;
&lt;td&gt;Pod count (to zero)&lt;/td&gt;
&lt;td&gt;External events&lt;/td&gt;
&lt;td&gt;&lt;code&gt;triggers&lt;/code&gt;, &lt;code&gt;cooldownPeriod&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;Start with HPA for stateless web services, add KEDA for event-driven workloads, use VPA for stateful services, and always enable the Cluster Autoscaler. Layer them intentionally, monitor aggressively, and iterate based on real traffic patterns.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Building Your First Copilot Extension in 30 Minutes</title>
    <link href="https://techai-explained.netlify.app/articles/building-copilot-extension/"/>
    <updated>2026-03-17T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/building-copilot-extension/</id>
    <content xml:lang="en" type="html">&lt;p&gt;GitHub Copilot Extensions let you build custom AI-powered tools that integrate directly into the Copilot chat experience. Instead of your users switching context to a separate tool, they can invoke your extension with &lt;code&gt;@your-extension&lt;/code&gt; right inside their editor or CLI.&lt;/p&gt;
&lt;p&gt;This tutorial walks you through building a working extension from scratch. By the end, you&#39;ll have a deployed Copilot Extension that answers questions about any GitHub repository&#39;s structure.&lt;/p&gt;
&lt;h2&gt;What You&#39;ll Build&lt;/h2&gt;
&lt;p&gt;A &lt;strong&gt;Repo Explorer&lt;/strong&gt; extension that lets users type &lt;code&gt;@repo-explorer what&#39;s the architecture of this project?&lt;/code&gt; and get an intelligent summary of the repository structure, key files, and technology stack.&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────────┐
│              USER IN COPILOT CHAT               │
│                                                 │
│  @repo-explorer what does this project do?      │
│                                                 │
│         │                                       │
│         ▼                                       │
│  ┌─────────────────┐    ┌──────────────────┐    │
│  │ Copilot Platform │───►│ Your Extension   │   │
│  │ (Routes request) │    │ (Node.js server) │   │
│  └─────────────────┘    └──────────────────┘    │
│                                │                │
│                                ▼                │
│                     ┌──────────────────┐        │
│                     │  Response with   │        │
│                     │  repo analysis   │        │
│                     └──────────────────┘        │
└─────────────────────────────────────────────────┘
&lt;/div&gt;
&lt;h2&gt;Prerequisites&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Node.js 20+&lt;/li&gt;
&lt;li&gt;A GitHub account&lt;/li&gt;
&lt;li&gt;Basic familiarity with Express.js&lt;/li&gt;
&lt;li&gt;GitHub CLI (&lt;code&gt;gh&lt;/code&gt;) installed&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Step 1: Scaffold the Project&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;mkdir copilot-repo-explorer
cd copilot-repo-explorer
npm init -y
npm install express @octokit/rest
npm install -D typescript @types/node @types/express tsx
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Initialize TypeScript:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npx tsc --init --target ES2022 --module NodeNext &#92;
  --moduleResolution NodeNext --outDir dist &#92;
  --rootDir src --strict
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Create the project structure:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;copilot-repo-explorer/
├── src/
│   ├── index.ts          # Express server
│   ├── handler.ts        # Copilot message handler
│   └── repo-analyzer.ts  # GitHub API integration
├── package.json
└── tsconfig.json
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 2: Build the Message Handler&lt;/h2&gt;
&lt;p&gt;Copilot Extensions receive messages via Server-Sent Events (SSE). Your server receives a POST request with the user&#39;s message and responds with a stream.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// src/handler.ts
import { IncomingMessage, ServerResponse } from &amp;quot;node:http&amp;quot;;

interface CopilotMessage {
  role: &amp;quot;user&amp;quot; | &amp;quot;assistant&amp;quot;;
  content: string;
}

interface CopilotRequest {
  messages: CopilotMessage[];
}

export async function handleCopilotRequest(
  req: IncomingMessage,
  res: ServerResponse,
  body: CopilotRequest
) {
  // Set SSE headers
  res.writeHead(200, {
    &amp;quot;Content-Type&amp;quot;: &amp;quot;text/event-stream&amp;quot;,
    &amp;quot;Cache-Control&amp;quot;: &amp;quot;no-cache&amp;quot;,
    Connection: &amp;quot;keep-alive&amp;quot;,
  });

  const userMessage = body.messages
    .filter((m) =&amp;gt; m.role === &amp;quot;user&amp;quot;)
    .pop()?.content;

  if (!userMessage) {
    sendSSEMessage(res, &amp;quot;I didn&#39;t receive a message. Try again!&amp;quot;);
    res.end();
    return;
  }

  // Analyze the repo
  const analysis = await analyzeCurrentRepo(userMessage);
  sendSSEMessage(res, analysis);
  res.end();
}

function sendSSEMessage(res: ServerResponse, content: string) {
  const data = JSON.stringify({
    choices: [{
      index: 0,
      delta: { content, role: &amp;quot;assistant&amp;quot; },
    }],
  });
  res.write(`data: ${data}&#92;n&#92;n`);
  res.write(&amp;quot;data: [DONE]&#92;n&#92;n&amp;quot;);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 3: Build the Repo Analyzer&lt;/h2&gt;
&lt;p&gt;This is the core logic. It reads the repository tree from GitHub and generates an intelligent summary.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// src/repo-analyzer.ts
import { Octokit } from &amp;quot;@octokit/rest&amp;quot;;

interface RepoStructure {
  languages: string[];
  keyFiles: string[];
  directories: string[];
  framework: string | null;
}

export async function analyzeRepo(
  owner: string,
  repo: string,
  token: string
): Promise&amp;lt;RepoStructure&amp;gt; {
  const octokit = new Octokit({ auth: token });

  // Get repository tree
  const { data: tree } = await octokit.git.getTree({
    owner, repo,
    tree_sha: &amp;quot;HEAD&amp;quot;,
    recursive: &amp;quot;true&amp;quot;,
  });

  const files = tree.tree
    .filter((item) =&amp;gt; item.type === &amp;quot;blob&amp;quot;)
    .map((item) =&amp;gt; item.path!);

  // Detect languages
  const extensions = files
    .map((f) =&amp;gt; f.split(&amp;quot;.&amp;quot;).pop())
    .filter(Boolean);
  const langCounts = extensions.reduce((acc, ext) =&amp;gt; {
    acc[ext!] = (acc[ext!] || 0) + 1;
    return acc;
  }, {} as Record&amp;lt;string, number&amp;gt;);

  const languages = Object.entries(langCounts)
    .sort(([, a], [, b]) =&amp;gt; b - a)
    .slice(0, 5)
    .map(([ext]) =&amp;gt; extensionToLanguage(ext));

  // Detect framework
  const framework = detectFramework(files);

  // Find key files
  const keyPatterns = [
    /readme/i, /contributing/i, /dockerfile/i,
    /docker-compose/i, /&#92;.github&#92;/workflows/,
    /package&#92;.json$/, /go&#92;.mod$/, /cargo&#92;.toml$/,
  ];
  const keyFiles = files
    .filter((f) =&amp;gt; keyPatterns.some((p) =&amp;gt; p.test(f)))
    .slice(0, 10);

  // Top-level directories
  const directories = [...new Set(
    files.filter((f) =&amp;gt; f.includes(&amp;quot;/&amp;quot;))
      .map((f) =&amp;gt; f.split(&amp;quot;/&amp;quot;)[0])
  )].slice(0, 15);

  return { languages, keyFiles, directories, framework };
}

function detectFramework(files: string[]): string | null {
  if (files.some((f) =&amp;gt; f.includes(&amp;quot;next.config&amp;quot;))) return &amp;quot;Next.js&amp;quot;;
  if (files.some((f) =&amp;gt; f.includes(&amp;quot;angular.json&amp;quot;))) return &amp;quot;Angular&amp;quot;;
  if (files.some((f) =&amp;gt; f.includes(&amp;quot;nuxt.config&amp;quot;))) return &amp;quot;Nuxt&amp;quot;;
  if (files.some((f) =&amp;gt; /&#92;.csproj$/.test(f))) return &amp;quot;.NET&amp;quot;;
  if (files.some((f) =&amp;gt; f.includes(&amp;quot;go.mod&amp;quot;))) return &amp;quot;Go&amp;quot;;
  if (files.some((f) =&amp;gt; f.includes(&amp;quot;Cargo.toml&amp;quot;))) return &amp;quot;Rust&amp;quot;;
  return null;
}

function extensionToLanguage(ext: string): string {
  const map: Record&amp;lt;string, string&amp;gt; = {
    ts: &amp;quot;TypeScript&amp;quot;, js: &amp;quot;JavaScript&amp;quot;, py: &amp;quot;Python&amp;quot;,
    go: &amp;quot;Go&amp;quot;, rs: &amp;quot;Rust&amp;quot;, cs: &amp;quot;C#&amp;quot;, java: &amp;quot;Java&amp;quot;,
    rb: &amp;quot;Ruby&amp;quot;, php: &amp;quot;PHP&amp;quot;, swift: &amp;quot;Swift&amp;quot;,
    kt: &amp;quot;Kotlin&amp;quot;, md: &amp;quot;Markdown&amp;quot;, yml: &amp;quot;YAML&amp;quot;,
  };
  return map[ext] || ext;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 4: Wire Up the Express Server&lt;/h2&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// src/index.ts
import express from &amp;quot;express&amp;quot;;
import { handleCopilotRequest } from &amp;quot;./handler.js&amp;quot;;

const app = express();
app.use(express.json());

// Health check
app.get(&amp;quot;/&amp;quot;, (_req, res) =&amp;gt; {
  res.json({ status: &amp;quot;ok&amp;quot;, extension: &amp;quot;repo-explorer&amp;quot; });
});

// Copilot Extension endpoint
app.post(&amp;quot;/agent&amp;quot;, async (req, res) =&amp;gt; {
  try {
    await handleCopilotRequest(req, res, req.body);
  } catch (error) {
    console.error(&amp;quot;Handler error:&amp;quot;, error);
    res.status(500).json({ error: &amp;quot;Internal server error&amp;quot; });
  }
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () =&amp;gt; {
  console.log(`Repo Explorer extension running on port ${PORT}`);
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Add the start script to &lt;code&gt;package.json&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-json&quot;&gt;{
  &amp;quot;scripts&amp;quot;: {
    &amp;quot;dev&amp;quot;: &amp;quot;tsx watch src/index.ts&amp;quot;,
    &amp;quot;build&amp;quot;: &amp;quot;tsc&amp;quot;,
    &amp;quot;start&amp;quot;: &amp;quot;node dist/index.js&amp;quot;
  }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Step 5: Test Locally&lt;/h2&gt;
&lt;p&gt;Start the development server:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;npm run dev
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Test with curl:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;curl -X POST http://localhost:3000/agent &#92;
  -H &amp;quot;Content-Type: application/json&amp;quot; &#92;
  -d &#39;{
    &amp;quot;messages&amp;quot;: [
      {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: &amp;quot;What is this project about?&amp;quot;}
    ]
  }&#39;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;You should see an SSE response with the analysis.&lt;/p&gt;
&lt;h2&gt;Step 6: Register as a GitHub App&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Go to &lt;strong&gt;Settings → Developer Settings → GitHub Apps → New GitHub App&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;Fill in:
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Name:&lt;/strong&gt; &lt;code&gt;repo-explorer-extension&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Homepage URL:&lt;/strong&gt; Your deployment URL&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Callback URL:&lt;/strong&gt; &lt;code&gt;https://your-domain.com/auth/callback&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Webhook URL:&lt;/strong&gt; &lt;code&gt;https://your-domain.com/webhook&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Under &lt;strong&gt;Copilot&lt;/strong&gt;, enable the extension and set the endpoint to &lt;code&gt;https://your-domain.com/agent&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Set permissions: &lt;code&gt;Repository contents: Read&lt;/code&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Step 7: Deploy&lt;/h2&gt;
&lt;p&gt;For quick deployment, use any Node.js hosting platform:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-bash&quot;&gt;# Build for production
npm run build

# Deploy (example with a container)
docker build -t repo-explorer .
docker push your-registry/repo-explorer
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Here&#39;s a minimal &lt;code&gt;Dockerfile&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-dockerfile&quot;&gt;FROM node:20-slim
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY dist/ ./dist/
EXPOSE 3000
CMD [&amp;quot;node&amp;quot;, &amp;quot;dist/index.js&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;How the Extension Protocol Works&lt;/h2&gt;
&lt;p&gt;Understanding the protocol helps you debug issues:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
1. User types: @repo-explorer analyze this repo
                    │
2. Copilot Platform │ POST /agent
   sends request ───┘ { messages: [...] }
                    │
3. Your server      │ SSE Response
   streams back ────┘ data: {&quot;choices&quot;:[...]}
                    │  data: [DONE]
                    │
4. Copilot renders  │
   the response ────┘ Markdown in chat UI
&lt;/div&gt;
&lt;p&gt;Key points:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Authentication&lt;/strong&gt;: The platform sends a &lt;code&gt;X-GitHub-Token&lt;/code&gt; header you can use to make API calls on behalf of the user&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Streaming&lt;/strong&gt;: Use SSE format — the response renders progressively in the chat&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Markdown&lt;/strong&gt;: Your response is rendered as Markdown, so use formatting, code blocks, and lists&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Adding Slash Commands&lt;/h2&gt;
&lt;p&gt;Make your extension more powerful with slash commands:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-typescript&quot;&gt;// Parse slash commands from the user message
function parseCommand(message: string): { command: string; args: string } {
  const match = message.match(/^&#92;/(&#92;w+)&#92;s*(.*)/);
  if (match) {
    return { command: match[1], args: match[2] };
  }
  return { command: &amp;quot;default&amp;quot;, args: message };
}

// In your handler:
const { command, args } = parseCommand(userMessage);

switch (command) {
  case &amp;quot;structure&amp;quot;:
    return analyzeStructure(args);
  case &amp;quot;deps&amp;quot;:
    return analyzeDependencies(args);
  case &amp;quot;security&amp;quot;:
    return securityScan(args);
  default:
    return generalAnalysis(args);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Users can then type:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;@repo-explorer /structure&lt;/code&gt; — show project structure&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@repo-explorer /deps&lt;/code&gt; — analyze dependencies&lt;/li&gt;
&lt;li&gt;&lt;code&gt;@repo-explorer /security&lt;/code&gt; — run security checks&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Common Pitfalls&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Forgetting SSE format&lt;/strong&gt; — Copilot expects Server-Sent Events, not a regular JSON response&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Not handling the token&lt;/strong&gt; — use &lt;code&gt;X-GitHub-Token&lt;/code&gt; for API calls; don&#39;t hardcode tokens&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Slow responses&lt;/strong&gt; — stream partial results so users see progress&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;No error handling&lt;/strong&gt; — always send a user-friendly error message instead of crashing&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Next Steps&lt;/h2&gt;
&lt;p&gt;You now have a working Copilot Extension. Here&#39;s where to go next:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Add &lt;strong&gt;caching&lt;/strong&gt; to avoid redundant GitHub API calls&lt;/li&gt;
&lt;li&gt;Implement &lt;strong&gt;conversation memory&lt;/strong&gt; so follow-up questions work&lt;/li&gt;
&lt;li&gt;Add more &lt;strong&gt;slash commands&lt;/strong&gt; for specialized analysis&lt;/li&gt;
&lt;li&gt;Deploy behind a &lt;strong&gt;CDN&lt;/strong&gt; for global performance&lt;/li&gt;
&lt;li&gt;Submit to the &lt;strong&gt;GitHub Marketplace&lt;/strong&gt; for public distribution&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The Copilot Extensions ecosystem is new and growing fast. Building an extension now means you&#39;re in early — when the marketplace scales, your extension is already there.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
  <entry>
    <title>Understanding AI Agents: Architecture Patterns That Work</title>
    <link href="https://techai-explained.netlify.app/articles/ai-agents-architecture-patterns/"/>
    <updated>2026-03-18T00:00:00Z</updated>
    <id>https://techai-explained.netlify.app/articles/ai-agents-architecture-patterns/</id>
    <content xml:lang="en" type="html">&lt;p&gt;Every major tech company is shipping AI agents in 2026. But behind the marketing buzzwords, what actually makes an AI agent work? And more importantly — what architecture patterns separate the demos from production systems?&lt;/p&gt;
&lt;p&gt;This guide breaks down the four dominant agent architecture patterns, with code examples you can adapt to your own projects.&lt;/p&gt;
&lt;h2&gt;What Is an AI Agent, Really?&lt;/h2&gt;
&lt;p&gt;An AI agent is software that uses a language model to &lt;strong&gt;decide&lt;/strong&gt; what actions to take, &lt;strong&gt;execute&lt;/strong&gt; those actions using tools, and &lt;strong&gt;iterate&lt;/strong&gt; based on the results. Unlike a simple chatbot that responds to prompts, an agent has a loop:&lt;/p&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌─────────────────────────────────────────────┐
│              THE AGENT LOOP                 │
│                                             │
│   Observe ──► Think ──► Act ──► Observe     │
│      ▲                            │         │
│      └────────────────────────────┘         │
│                                             │
│   The agent keeps looping until the task    │
│   is complete or a stop condition is met.   │
└─────────────────────────────────────────────┘
&lt;/div&gt;
&lt;p&gt;The key distinction: &lt;strong&gt;a chatbot responds once; an agent loops until the job is done.&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Three properties define a true agent:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Autonomy&lt;/strong&gt; — it decides which tools to use and in what order&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tool use&lt;/strong&gt; — it can call APIs, run code, read files, query databases&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Memory&lt;/strong&gt; — it maintains context across its action loop&lt;/li&gt;
&lt;/ol&gt;
&lt;h2&gt;Pattern 1: The ReAct Loop&lt;/h2&gt;
&lt;p&gt;The simplest and most widely-used pattern is &lt;strong&gt;ReAct&lt;/strong&gt; (Reasoning + Acting). The model alternates between reasoning about what to do and taking an action.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;class ReActAgent:
    def __init__(self, llm, tools):
        self.llm = llm
        self.tools = {t.name: t for t in tools}
        self.max_steps = 10

    def run(self, task: str) -&amp;gt; str:
        messages = [{&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: task}]
        
        for step in range(self.max_steps):
            # THINK: Ask the LLM what to do
            response = self.llm.chat(messages, tools=self.tools)
            
            if response.is_final_answer:
                return response.content
            
            # ACT: Execute the chosen tool
            tool_name = response.tool_call.name
            tool_args = response.tool_call.arguments
            result = self.tools[tool_name].execute(**tool_args)
            
            # OBSERVE: Feed result back
            messages.append({&amp;quot;role&amp;quot;: &amp;quot;tool&amp;quot;, &amp;quot;content&amp;quot;: result})
        
        return &amp;quot;Max steps reached without resolution.&amp;quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;When to Use ReAct&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Single-purpose tasks: &amp;quot;Find the bug in this function&amp;quot;&lt;/li&gt;
&lt;li&gt;Linear workflows where each step informs the next&lt;/li&gt;
&lt;li&gt;Prototyping — it&#39;s the fastest pattern to implement&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;Limitations&lt;/h3&gt;
&lt;p&gt;ReAct struggles with tasks that require &lt;strong&gt;parallel work&lt;/strong&gt; or &lt;strong&gt;planning ahead&lt;/strong&gt;. It&#39;s purely reactive — the model only thinks one step at a time.&lt;/p&gt;
&lt;h2&gt;Pattern 2: Plan-and-Execute&lt;/h2&gt;
&lt;p&gt;This pattern separates &lt;strong&gt;planning&lt;/strong&gt; from &lt;strong&gt;execution&lt;/strong&gt;. A planner model creates a full plan upfront, then an executor works through each step.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;class PlanAndExecuteAgent:
    def __init__(self, planner_llm, executor_llm, tools):
        self.planner = planner_llm
        self.executor = executor_llm
        self.tools = tools

    def run(self, task: str) -&amp;gt; str:
        # Phase 1: Create the plan
        plan = self.planner.chat([
            {&amp;quot;role&amp;quot;: &amp;quot;system&amp;quot;, &amp;quot;content&amp;quot;: &amp;quot;Create a step-by-step plan.&amp;quot;},
            {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: task}
        ])
        
        steps = parse_plan(plan.content)
        results = []
        
        # Phase 2: Execute each step
        for step in steps:
            result = self.executor.chat([
                {&amp;quot;role&amp;quot;: &amp;quot;system&amp;quot;, &amp;quot;content&amp;quot;: f&amp;quot;Execute: {step}&amp;quot;},
                {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: f&amp;quot;Prior results: {results}&amp;quot;}
            ], tools=self.tools)
            results.append(result)
        
        # Phase 3: Synthesize
        return self.planner.summarize(task, results)
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
┌──────────┐     ┌──────────┐     ┌──────────┐
│ PLANNER  │────►│ EXECUTOR │────►│ EXECUTOR │──► ...
│          │     │ Step 1   │     │ Step 2   │
│ Creates  │     │          │     │          │
│ full plan│     │ Uses     │     │ Uses     │
│ upfront  │     │ tools    │     │ results  │
└──────────┘     └──────────┘     └──────────┘
                                       │
                                       ▼
                                 ┌──────────┐
                                 │SYNTHESIZE│
                                 │ Final    │
                                 │ answer   │
                                 └──────────┘
&lt;/div&gt;
&lt;h3&gt;When to Use Plan-and-Execute&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Complex multi-step tasks: &amp;quot;Refactor this module and update all tests&amp;quot;&lt;/li&gt;
&lt;li&gt;When you need &lt;strong&gt;predictability&lt;/strong&gt; — the user can review the plan before execution&lt;/li&gt;
&lt;li&gt;Tasks where total cost matters (the plan constrains token usage)&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Pattern 3: Multi-Agent Orchestration&lt;/h2&gt;
&lt;p&gt;For complex systems, a single agent isn&#39;t enough. Multi-agent orchestration uses &lt;strong&gt;specialized agents&lt;/strong&gt; coordinated by a &lt;strong&gt;router&lt;/strong&gt; or &lt;strong&gt;orchestrator&lt;/strong&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;class MultiAgentOrchestrator:
    def __init__(self, agents: dict, router_llm):
        self.agents = agents  # {&amp;quot;coder&amp;quot;: CoderAgent, &amp;quot;reviewer&amp;quot;: ReviewAgent, ...}
        self.router = router_llm

    def run(self, task: str) -&amp;gt; str:
        # Router decides which agent handles the task
        routing = self.router.chat([
            {&amp;quot;role&amp;quot;: &amp;quot;system&amp;quot;, &amp;quot;content&amp;quot;: self.routing_prompt()},
            {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: task}
        ])
        
        agent_name = routing.selected_agent
        agent = self.agents[agent_name]
        
        # Delegate to the specialist
        result = agent.run(task)
        
        # Optionally pass to another agent for review
        if routing.needs_review:
            review = self.agents[&amp;quot;reviewer&amp;quot;].run(
                f&amp;quot;Review this output:&#92;n{result}&amp;quot;
            )
            return f&amp;quot;{result}&#92;n&#92;nReview: {review}&amp;quot;
        
        return result
&lt;/code&gt;&lt;/pre&gt;
&lt;div class=&quot;diagram-box&quot;&gt;
                    ┌──────────────┐
       Task ───────►│   ROUTER     │
                    │              │
                    │ Analyzes the │
                    │ task and     │
                    │ delegates    │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │  CODER   │ │ RESEARCHER│ │ REVIEWER │
        │  Agent   │ │  Agent   │ │  Agent   │
        │          │ │          │ │          │
        │ Writes &amp; │ │ Searches │ │ Checks   │
        │ edits    │ │ docs &amp;   │ │ quality  │
        │ code     │ │ data     │ │ &amp; style  │
        └──────────┘ └──────────┘ └──────────┘
&lt;/div&gt;
&lt;h3&gt;Real-World Example: Code Review Pipeline&lt;/h3&gt;
&lt;p&gt;A production code review system might use three agents:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;Security Agent&lt;/strong&gt; — scans for vulnerabilities, credential leaks, injection risks&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Logic Agent&lt;/strong&gt; — checks for bugs, race conditions, edge cases&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Style Agent&lt;/strong&gt; — enforces conventions, naming patterns, documentation&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Each agent has different system prompts, different tools, and different evaluation criteria. The orchestrator merges their feedback into a single review.&lt;/p&gt;
&lt;h2&gt;Pattern 4: Reflexion (Self-Correcting Agents)&lt;/h2&gt;
&lt;p&gt;The most advanced pattern adds &lt;strong&gt;self-evaluation&lt;/strong&gt;. After producing output, the agent critiques its own work and iterates.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;class ReflexionAgent:
    def __init__(self, actor_llm, critic_llm, tools):
        self.actor = actor_llm
        self.critic = critic_llm
        self.tools = tools
        self.max_reflections = 3

    def run(self, task: str) -&amp;gt; str:
        attempt = self.actor.chat(
            [{&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: task}],
            tools=self.tools
        )
        
        for i in range(self.max_reflections):
            # Self-evaluate
            critique = self.critic.chat([{
                &amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;,
                &amp;quot;content&amp;quot;: f&amp;quot;Task: {task}&#92;nAttempt:&#92;n{attempt}&#92;n&#92;n&amp;quot;
                           f&amp;quot;Is this correct and complete? &amp;quot;
                           f&amp;quot;If not, what needs to change?&amp;quot;
            }])
            
            if critique.is_satisfactory:
                return attempt.content
            
            # Retry with feedback
            attempt = self.actor.chat([
                {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: task},
                {&amp;quot;role&amp;quot;: &amp;quot;assistant&amp;quot;, &amp;quot;content&amp;quot;: attempt.content},
                {&amp;quot;role&amp;quot;: &amp;quot;user&amp;quot;, &amp;quot;content&amp;quot;: f&amp;quot;Feedback: {critique.content}&amp;quot;}
            ], tools=self.tools)
        
        return attempt.content
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;When to Use Reflexion&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;High-stakes outputs: code generation, data analysis, report writing&lt;/li&gt;
&lt;li&gt;When you can define clear &lt;strong&gt;evaluation criteria&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;When the cost of iteration is lower than the cost of errors&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Choosing the Right Pattern&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Pattern&lt;/th&gt;
&lt;th&gt;Complexity&lt;/th&gt;
&lt;th&gt;Best For&lt;/th&gt;
&lt;th&gt;Watch Out&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;ReAct&lt;/td&gt;
&lt;td&gt;Low&lt;/td&gt;
&lt;td&gt;Single tasks, prototypes&lt;/td&gt;
&lt;td&gt;Gets stuck on complex multi-step work&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Plan-and-Execute&lt;/td&gt;
&lt;td&gt;Medium&lt;/td&gt;
&lt;td&gt;Predictable workflows&lt;/td&gt;
&lt;td&gt;Plans can become stale mid-execution&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Multi-Agent&lt;/td&gt;
&lt;td&gt;High&lt;/td&gt;
&lt;td&gt;Complex systems, specialized domains&lt;/td&gt;
&lt;td&gt;Coordination overhead, debugging difficulty&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Reflexion&lt;/td&gt;
&lt;td&gt;Medium-High&lt;/td&gt;
&lt;td&gt;Quality-critical outputs&lt;/td&gt;
&lt;td&gt;Token cost multiplies with iterations&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;Production Considerations&lt;/h2&gt;
&lt;h3&gt;1. Guard Rails Are Non-Negotiable&lt;/h3&gt;
&lt;p&gt;Every production agent needs:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Token budgets&lt;/strong&gt; — cap the maximum loop iterations and total tokens&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Tool allowlists&lt;/strong&gt; — restrict which tools the agent can call&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Output validation&lt;/strong&gt; — schema validation on structured outputs&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Human-in-the-loop&lt;/strong&gt; — escalation paths for uncertain decisions&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2. Observability&lt;/h3&gt;
&lt;p&gt;You cannot debug agents with &lt;code&gt;console.log&lt;/code&gt;. You need:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Trace IDs&lt;/strong&gt; across every LLM call and tool invocation&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Step-by-step logging&lt;/strong&gt; of the agent&#39;s reasoning&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Cost tracking&lt;/strong&gt; per task (tokens consumed, API calls made)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Latency breakdowns&lt;/strong&gt; showing where time is spent&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;3. Evaluation&lt;/h3&gt;
&lt;p&gt;Before deploying, build an eval suite:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&quot;language-python&quot;&gt;eval_cases = [
    {&amp;quot;input&amp;quot;: &amp;quot;Find all Python files with SQL injection risks&amp;quot;,
     &amp;quot;expected_tools&amp;quot;: [&amp;quot;grep&amp;quot;, &amp;quot;file_read&amp;quot;],
     &amp;quot;expected_output_contains&amp;quot;: [&amp;quot;parameterized&amp;quot;, &amp;quot;sanitize&amp;quot;]},
]

for case in eval_cases:
    result = agent.run(case[&amp;quot;input&amp;quot;])
    assert all(t in result.tools_used for t in case[&amp;quot;expected_tools&amp;quot;])
    assert all(k in result.output for k in case[&amp;quot;expected_output_contains&amp;quot;])
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;What&#39;s Next&lt;/h2&gt;
&lt;p&gt;The agent landscape is moving fast. Key trends to watch:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;MCP (Model Context Protocol)&lt;/strong&gt; — standardizing how agents connect to tools&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Agent-to-agent communication&lt;/strong&gt; — agents delegating to other agents across organizational boundaries&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Persistent memory&lt;/strong&gt; — agents that learn from past interactions and improve over time&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Formal verification&lt;/strong&gt; — mathematical guarantees about agent behavior&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The pattern you choose matters less than the guardrails you put around it. Start with ReAct, graduate to multi-agent when your use case demands it, and always instrument everything.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;Published by the TechAI Explained Team.&lt;/em&gt;&lt;/p&gt;
</content>
  </entry>
</feed>
